# Homepage Design Changelog

An append-only, dated log of every content or design change the homepage team ships, so the visual
history of the storefront is auditable in one place instead of reconstructed from run events. Both
homepage routines append here as their last content/design step:

- **Routine A — Daily Merchandiser** (`routine-daily-merchandise.md`) appends an entry on every run
  that changes what the page shows (hero, rails, wayfinder tiles, imagery, copy, section order).
- **Routine B — Design Cycle** (`routine-design-cycle.md`) appends an entry when a design/shell PR
  ships (or is opened for the release engine).

Each entry is owned by the run that shipped the change: that run opens its own changelog PR and writes
the Evidence line from its own run data (run id, Sanity revs, asset IDs, render probe), because no
later agent can reconstruct evidence it never held. The changelog PR is therefore filed as
`kind:'instructions'` (docs lane), never `kind:'code'` — see #4758 and the two routines' "Append the
design changelog" steps.

## Entry format

One dated entry per change, **appended at the bottom of the file, directly above the end-of-file
append marker** (ticket #2878: the old newest-first fixed anchor after `## Entries` made every
concurrent changelog PR conflict on the same line; bottom-append lets same-day PRs stack). Rebase
onto latest `origin/main` before opening a PR that appends here. Each entry names **what changed**,
**why**, and the **evidence probe touched** (the run/PR/event or GA4/GSC signal that motivated or
verifies it):

```
### YYYY-MM-DD — [Routine A|Routine B] — <one-line what changed>
- **What:** <the concrete surfaces that changed — hero/rail/tile/section/PR>
- **Why:** <theme, signal, or directive that drove it>
- **Evidence:** <run id / PR # / event / GA4 or GSC probe touched>
```

Keep entries terse and factual. This file is content/documentation on the agent-editor allowlist
(`docs/homepage-team/*.md`); it carries no code and gates nothing.

## Entries

### 2026-08-14 — Routine A — Hero rotated to b-Vibe Snug Ring (owner-directed) + full fresh-art floor
- **What:** Rotated the hero from the We-Vibe "Moving as One" kit to the **b-Vibe Snug Ring 45mm black** (`b-vibe-snug-ring-45mm-snug-fit-silicone-c-ring-black`) — pinned `emmaHeroStorefront.featuredProductHandle` + `primaryCtaLink` and refreshed the full hero copy (dial-9, act-anchored to the worn c-ring). Promoted the clean device sibling `98863B` to primary via `setMediaAsPrimary` first, because the product's primary `98863A` is a blister-pack composite (baked text). Met the fresh-art floor: 3 new images §2-passed and placed — Discover You promo (coral-soft), couples band (plum-soft + satin ribbon), couples wayfinder tile `wf65c` (paper); grounds rotated across the ground lock. Refreshed couples-band copy to the wearable.
- **Why:** Owner direction (Mike, 2026-08-14, ticket #3184) — kick off the hero rotation now in a fresh run, clearing the 4-run hero hold (freshness #3047) with an on-theme wearable for "Weekend In, Not Out." A hero product change triggers the ≥3-image fresh-art floor (mission brief §2 / Step 4).
- **Evidence:** run 314; Sanity revs J5AMcOqaNUY9YyUo4PDAz9 (hero copy), USWXCwIENgmO1X122cS3FM (pin), USWXCwIENgmO1X122cS9Fh (couples copy); image assets 485a2530 (promo), 404932a4 (couples band), d7247419 (tile wf65c); Shopify `featuredMedia` confirmed `98863B`; origin render confirmed the hero handle + new headline; hero image + couples heading propagation pending (300s storefront product cache), background re-warm + re-verify scheduled.

### 2026-08-14 — Routine A — Fresh couples-band art + hero/couples copy refresh (Weekend In; hero product held)
- **What:** Regenerated the couples band (`playTogether-starter`) image — clean archetype-C device still-life (matte-black U-shaped couples wearable on a plum-soft two-tone ground, bright daylight, ivory linen + green fig shape-rhyme, no text) replacing the byte-identical prior asset. Refreshed the couples-band heading + body and the full hero copy (eyebrow/headline/body/pullQuote, dial-9, We-Vibe "Moving as One" kit held on-theme) with fresh, non-repeating language. Step 5b health sweep GREEN on all 7 live merchandised pages (200 + one H1). Category deep-refresh pair deferred (turn budget).
- **Why:** Active theme "Weekend In, Not Out" (couples + wearables), marketing_calendar #6. Per-run freshness cadence (Step 2c) — clears couples sameness #3056. Hero PRODUCT rotation + its mandated 3-image fresh-art floor deferred to next run: the cold-start cloud checkout (dependency install + CLI-syntax discovery) consumed the read-phase turn budget, and a changed-hero run cannot ship <3 fresh images honestly. First couples-band gen (kit Kontext ref) rejected on §2 (reproduced the retail box + baked text, repro of run 294 kit-ref failure); regenerated clean, no ref.
- **Evidence:** run 308; sameness-diff surfaces vs run 294 = couples-band imagery (asset c5e29ca) + couples-band copy + hero copy (≥2 surfaces, one imagery); Sanity revs h8DOoF7DyhmSiqO7D57qiz (homepage/couples) + h8DOoF7DyhmSiqO7D57r7Z (hero); homepage warmed (warm-homepage-b ok, 4 rails, not degraded), origin propagation pending (blob warm + 900s edge + SWR); health sweep 7/7 pages 200 + one H1.

### 2026-08-13 — Routine A — Weekend-In refresh: new couples-band product-still + full copy pass (hero held)
- **What:** Regenerated the couples band (`playTogetherBanner`) image — clean archetype-C product still (Ferri wearable on ivory silk, plum-soft ground, warm daylight) replacing the month-era plate, and rethemed its copy off the August-Reset "start the month slow" line. Refreshed hero copy (eyebrow/headline/body/pullQuote, dial-9, We-Vibe kit held on-theme), both wired rail eyebrow/heading/asides (couples + under-$30 on-ramp), and all 3 wayfinder tile asides. Step 5b: health sweep GREEN on 8 live merchandised surfaces; category deep-refresh pair deferred (turn budget) and staleness filed (#2977).
- **Why:** Active theme "Weekend In, Not Out" (couples wellness + wearables), marketing_calendar #6. Per-run freshness cadence (Step 2c) — closes freshness tickets #2461 (tiles), #2462 (couples), #2469 (hero). First band-image gen (kit-ref) rejected on §2 (reproduced the retail box + baked text); regenerated clean with a Ferri product ref. #1785 (We-Vibe Moxie+) declined for placement: packaging-only primary, no clean sibling.
- **Evidence:** run 294; sameness-diff surfaces vs #280 = couples-band imagery (asset d74c60f8) + hero/rail/tile copy (≥2 surfaces, one imagery); origin render verified (hero handle, both rail headings, band heading + new asset present, no daily-deal framing); health sweep 7/7 category/drop pages 200 + one H1 + FAQ + trust.

### 2026-08-12 — Routine B — Harden the /social IG-arrival surface (in-stock pin, attributed primary link, fresh hero copy)
- **What:** `app/routes/_layout.social.tsx` — (1) the pinned featured product now requires an in-stock match (falls through / hides rather than sending IG traffic to a dead PDP); (2) the primary featured PDP link now carries the social UTM via `pdpHref`, matching the recent grid, so IG→PDP is measured on the highest-intent click; (3) hero copy retired-tic fix ("point you at" → "find you"). No shell/section/schema change. Ambition-mandate concept wire `docs/homepage-team/concepts/first-tap.md` banked (design-only, not built).
- **Why:** Strategy brief #5 names /social the priority IG→landing→PDP surface and "measure each hop honestly" as the month's job; the primary click was the one hop we could not attribute, and the pin had no stock guard (same failure class that pulled a live IG post on 08-09). "point you to/at" is retired by mission brief §8.
- **Evidence:** run #282; brief #5 (IG live, GA4-blind); WebFetch egress blocked for competitors this run (self-capture only); voice gate PASS; typecheck/build/1788 tests green; PR link on the run row.

### 2026-08-12 — Routine A — Hero + couples-rail copy refresh (Weekend In); #1785 declined
- **What:** `singleton.emmaHero` copy fully refreshed (eyebrow/headline/body/pullQuote) for the held We-Vibe "Moving as One" couples-kit hero; couples rail (`emmaRail-augreset-couples-20260801`) eyebrow/heading refreshed and a pairing aside added. No product-selection or imagery change this run (named supply hold).
- **Why:** Active theme "Weekend In, Not Out" (marketing_calendar #6). Per-run freshness cadence (Step 2c) plus a voice cleanup (removed the borderline-dare "make them beg" from the hero body). Suggestion #1785's fresh candidates declined — Moxie+ has a packaging-only primary with no clean sibling, Biird glass plug is off-theme for the couples/wearable week, Gush 2 is 404.
- **Evidence:** run 280; sameness-diff surfaces Hero (copy) + Rails (copy); Sanity revisions Aq77mZkFsuxXAoKZNDfL22 (hero), Aq77mZkFsuxXAoKZNDfNHU (rail); health sweep GREEN on all 8 live pages.

### 2026-08-11 — Routine A — Weekend-In-Not-Out theme refresh (completed #248's deferred slate)
- **What:** Regenerated the "Discover You" promo image (fresh archetype-C product-forward scene: We-Vibe couples kit on a plum-soft ground with linen throw, warm daylight); rethemed promo copy off the August-Reset "give yourself the month" line to a Weekend-In staycation frame; refreshed both wired rail eyebrows + asides (couples rail "A weekend, the two of you"; on-ramp rail "Easy to start, under $30") in fresh dial-9 voice; refreshed the couples wayfinder tile aside. Hero held (We-Vibe kit, rotated yesterday). Flipped the "Weekend In, Not Out" calendar row planned→active.
- **Why:** Monday changeover run #248 shipped only the hero + announcement and explicitly deferred the theme's art floor, rail re-copy, wayfinder/promo, and calendar flip to the next non-changeover run (today). Theme = "Weekend In, Not Out" (couples wellness + wearables, staycation).
- **Evidence:** run 264. Sameness-diff surfaces vs #248: promo imagery (new asset), promo copy, both rail eyebrows/asides, couples tile aside (≥2 surfaces, one imagery). Step 5b health sweep GREEN (8/8 surfaces 200, one H1). Promo image gen attempt 1 (no-ref human-context) blackened by fal NSFW filter → retried with product ref (Kontext), passed.

### 2026-08-15 — Routine A — Couples-rail product swap + fresh hero/band/announcement copy (Weekend In; hero held)
- **What:** Swapped the couples rail (`emmaRail-augreset-couples-20260801`): solo lay-on `touch-x` OUT, `we-vibe-melt-2-pleasure-air-clit-stim` IN (couples worn air-pulse, on-theme wearable, PDP 200-verified, not duplicated in the on-ramp rail); `water-based-lubricant-4-oz` retained as the pairing. Refreshed the rail eyebrow + aside, the full hero copy (eyebrow/headline/body/pullQuote, dial-9, act-anchored to the worn b-Vibe Snug Ring c-ring), the couples-band (`playTogether-starter`) heading + body, and the announcement theme line. Hero PRODUCT held (b-Vibe Snug Ring, rotated run 314); no image generated (held-hero/held-theme day, fresh-art floor not triggered).
- **Why:** Active theme "Weekend In, Not Out" (couples + wearables), marketing_calendar #6. Per-run freshness cadence (Step 2c): a product-selection change is the non-copy arm, so no image was required today. The lay-on `touch-x` was the least on-theme of the four couples handles; `we-vibe-melt-2` sits between partners hands-free, a natural neighbor to the worn-c-ring hero.
- **Evidence:** run 324; sameness-diff surfaces vs run 314 = couples rail product-selection + rail/hero/band copy (>=2 surfaces, one product-selection); Sanity revs USWXCwIENgmO1X122cjjew (hero), J5AMcOqaNUY9YyUo4QFmM8 (homepage: band + announcement), h8DOoF7DyhmSiqO7D87Xyv (couples rail); origin render (rt-forced) confirmed hero headline + couples-band heading + we-vibe-melt-2 handle + hero pin present, no daily-deal framing; anon edge propagation pending (900s + SWR). Category deep-refresh pair deferred (cold-start turn budget).

### 2026-08-18 — Routine B — Nº 07 Sensation Map retired for the Curiosity Shelf (ticket #3532)
- **What:** Replaced the Sensation Map band (`SensationMap.tsx`, `sensation-map*.ts`, `/api/sensation-map`) with the tap-first **Curiosity Shelf**: one row of named appetites over a fixed six-slot product grid that restocks in place from the SSR payload with no runtime fetch. New `curiosity-shelf.ts` (pure resolver: family dedupe, `typeDial` floor, price-band mix, daily deck rotation), `curiosity-shelf.server.ts`, `CuriosityShelf.tsx`, additive Sanity `curiosityShelf` singleton + `curiosityLane` object, `.shelf-restock` CSS crossfade, and `HOMEPAGE_PAYLOAD_B_VERSION` `b7`→`b8`. Band key stays `sensationMap` so published layouts keep ordering it. Retires spec §j defects 1–3 (silent no-op, requested-vs-resolved see-all mismatch, the $418.99 jeans trio) and adds the daily Sanity lever the band never had.
- **Why:** Owner direction 2026-08-15 ("stale... feels broken... come up with a better way to discover"); design spec `docs/homepage-team/discovery-band-redesign-spec.md`.
- **Evidence:** ticket #3532; PR (this branch); `curiosity-shelf.test.ts` covers the 10 spec cases; typecheck/test/build green locally.

### 2026-08-18 — Routine A — Me-Time theme relaunch: hero off sub-$39 floor violation + rails re-themed + 3 fresh images
- **What:** Rotated the hero off `b-vibe-snug-ring-45mm-snug-fit-silicone-c-ring-black` ($10.99, a sub-$39 lead-surface floor violation) to **ROMP Rose 2.0** (`romp-2-0`, pleasure-air clitoral stimulator, $48.99, wc $27.50 so margin known, inv 166) — pinned `emmaHeroStorefront.featuredProductHandle` + `primaryCtaLink`, promoted the clean device sibling `97829B` to primary first (primary `97829A` is a box+device packaging composite with baked text), and refreshed the full hero copy (dial-9, act-anchored to air-pulse, me-time framed). Re-themed both wired rails: Rail A `emmaRail-augreset-couples-20260801` → "Your hour, and the good stuff to fill it" (we-vibe-melt-2 / magic-wand-original / lush-3 / dame-arousal-serum pairing; lead slots all ≥$39; See-all cleared, see below); Rail B `emmaRail-augreset-onramp-20260801` → "Small, yours, easy to say yes to" (6 SKUs all ≤$30, See-all → /collections/first-time). Relabeled/repointed all 3 wayfinder tiles (wf65c couples → finger-clit-vibes) + promo copy, refreshed the couples-band copy, and replaced the stale "Weekend in, not out" announcement line. Met the fresh-art floor: 3 new §2-passed images generated + placed — couples band (coral-soft human-context, two adults in loungewear), wands tile wf65a (plum-soft, Magic Wand ref), Discover-You promo (coral-soft me-time still life, worn robe belt + open drawer, interest-floor P1/P2).
- **Why:** Today's theme = "Back to Routine, Me-Time Included" (marketing_calendar #7, event_date 2026-08-18); the live slate was still the couples "Weekend In" set and had gone byte-identical (auto-filed sameness #3777/3778/3779/3780, all closed this run). Theme change (couples → me-time) + hero product change triggered the ≥3-image fresh-art floor (mission brief §2 / Step 4). Rail A ships with NO See-all: its four items span air-pulse/wand/egg/gel with no single collection carrying them on page 1, and `/collections/best-sellers` is disallowed as a destination, so per the mission-brief continuity rule the module ships without a See-all rather than pointing somewhere plausible. Calendar drift flagged (not raw-written): #7 still `planned`, stale `active` = #6/#22; the status-flip is Monday merch-calendar's job.
- **Evidence:** run 375; sameness-diff surfaces vs 2026-08-17 = hero product-selection + both rails product-selection + 3 new images (≥2 surfaces, imagery AND product-selection); Sanity patched + read-back confirmed: emmaHero, emmaHeroStorefront (pin romp-2-0), emmaRail-augreset-couples-20260801, emmaRail-augreset-onramp-20260801, singleton.homepage (wayfinder/couples/announcement); Shopify featuredMedia promoted to 97829B (confirmed); image assets 9e1272c8 (couples band), a50ddbcc (wands tile), 086248be (promo); Step 7 origin render (rt-forced) FOUND hero handle + hero copy + both rail titles + wayfinder label + announcement, no daily-deal framing; /cron/homepage-healthcheck renderTruth independently GREEN (hero + 2 rails + 3 tiles + couples, 0 missing, 0 fallbacks); See-all continuity: Rail B → first-time OK (beginner-c-ring, organics-lubricant on page 1), Rail A no See-all.

### 2026-08-19 — Routine B — Curiosity Shelf See-all: drop the banned best-sellers fallback
- **What:** `app/lib/curiosity-shelf.ts` + `curiosity-shelf.server.ts` + `app/types/curiosity.ts` + `app/components/store/CuriosityShelf.tsx` (+ tests + spec). `ResolvedLane.seeAllHref` is now `string | null`: an authored `seeAllHandle` that is not a live, in-stock collection resolves to `null` and the lane renders **no** "See the full fit" link (the whole footer rule is skipped when a lane has neither a second page nor a See-all). Removed the `FALLBACK_COLLECTION = 'best-sellers'`, the `verifiedCollections.add('best-sellers')` magic, and the `toContent` best-sellers default (missing handle → `''` → drops the link). Corrected the redesign spec's now-stale acceptance lines (§j item 10, the `seeAllHref` rule, the `seeAllHandle` field row). No `HOMEPAGE_PAYLOAD_B_VERSION` bump: the field stays JSON-compatible and a stale `b8` blob self-heals on the next `/cron/warm`.
- **Why:** Mission brief §1 (binding owner direction, all-hands 2026-07-30) bans `/collections/best-sellers` as a silent See-all fallback for an auto-generated module — its ranking is a different order from the discovery index that fills a lane, so it is structurally guaranteed to mismatch the six on screen, and the prescribed remedy is to ship with NO See-all. The Nº 07 band (shipped 2026-08-18, ticket #3532) reintroduced the exact anti-pattern the module it replaced was built to retire, and `CuriosityShelf.tsx` could not even express "no See-all." Honest scope: today's all-verified default handles make the fallback latent, not firing; it fires the moment Routine A authors a lane whose handle is not a live in-stock collection, or a default collection unstocks.
- **Evidence:** run 397; PR (this branch, `claude/focused-cerf-95vunz`); self-capture `xdipx.com/?variant=b` = 200 (band renders code-default "TONIGHT'S CURIOSITIES", no Sanity doc yet); teardown delta appended to `competitor-teardown-2026-07-live.md` (external refs egress-blocked, Wednesday); `curiosity-shelf.test.ts` extended (null/empty/best-sellers-unverified cases); gates green (typecheck, 2795/2795 tests, build); ambition concept banked at `concepts/curiosity-echo.md`.

### 2026-08-19 — Routine A — Me-Time per-run refresh: rails re-themed + real product swap + couples copy (hero held)
- **What:** Refreshed both wired rails and the couples band. Rail A `emmaRail-augreset-couples-20260801` → "The hour that answers to no one" (reordered so the Magic Wand leads; we-vibe-melt-2 / lush-3 / dame-arousal-serum pairing; lead slots 1-3 all ≥$39 toys, serum in attach slot 4), and its **blank `ctaLink` fixed** → `/collections/pleasure` (continuity-probed: pleasure contains magic-wand-original-hv-260-massager on page 1; the lineup's three toys are all pleasure-category). Rail B `emmaRail-augreset-onramp-20260801` → "Small enough to keep close, easy enough to start tonight" with a **real lineup swap** (clarity-heras-tempest-glass-dildo OUT, nixie-smooch lipstick vibe IN; 6 SKUs, See-all → /collections/first-time, continuity-probed: contains beginner-c-ring-2-in-crimson-bag). Couples band `playTogether-starter` heading + body rewritten ("The night you hand over the remote"). Hero HELD (romp-2-0, rotated yesterday run 375); no image generated (held-hero/held-theme day, fresh-art floor not triggered). Step 5b: health-sweep only, category deep-refresh pair deferred (cold-start turn budget, #2977).
- **Why:** Active theme "Back to Routine, Me-Time Included" (marketing_calendar #7, set run 375). Per-run freshness cadence (Step 2c): rails carry the product-selection arm (nixie-smooch swap) so no image was required today. Rail A's blank ctaLink was correcting a best-sellers fallback risk; continuity now verified. FLAG: singleton.panelDeck _updatedAt=2026-07-30 (20 days) breaches the 7-day weekly floor — retro suggestion filed for a dedicated deck-refresh run.
- **Evidence:** run 395; sameness-diff surfaces vs run 375 = rails (product-selection: nixie-smooch swap + reorder) + couples band copy (≥2 surfaces, one product-selection); rails-fingerprint --assert-changed rails,couples PASS (both slots moved); Step 7 origin render (?rt cache-bust, HTTP 200) FOUND hero pin romp-2-0 + both new rail headings + couples heading + swapped-in nixie-smooch, clarity-glass gone, no daily-deal framing, no fallback sections; See-all continuity probed GREEN both rails. Calendar hygiene noted (not written): #7 still `planned`, status-flip is Monday merch-calendar's job.

### 2026-08-20 — Routine A — Me-Time daily refresh: hero/rails/wayfinder/couples/announcement copy, Rail A reorder, deck floor cleared
- **What:** Held theme "Back to Routine, Me-Time Included" and held hero product `romp-2-0` (both correct for the week; hero rotated run 375). Refreshed the full hero copy (eyebrow/headline/body/pullQuote, dial-9, air-pulse/oral-anchored, fresh non-repeating language). Rail A (`emmaRail-augreset-couples-20260801`): **reordered** the lineup so the air-pulse `we-vibe-melt-2` leads (echoing the hero), then `magic-wand-original`, `lush-3`, `dame-arousal-serum` (attach, last); fresh eyebrow/heading ("Pick the pace, tonight is yours") + aside. Rail B (`emmaRail-augreset-onramp-20260801`): fresh eyebrow/heading ("Small, easy, and yours to try") + aside; 6-SKU under-$30 lineup held (depth floor). Refreshed the wayfinder "Discover You" promo copy + all 3 tile asides, the couples band copy ("An evening in, just the two of you"), and the me-time announcement line. **Deck floor:** `singleton.panelDeck` was 21 days stale (2026-07-30) — refreshed the two large-banner blurbs (Discover / Just landed), reusing existing art (no image budget), `_updatedAt` now 2026-08-20. No image generated: fresh-art floor not triggered (hero + theme held); the non-copy sameness arm is Rail A's product-selection reorder.
- **Why:** Active week theme = "Back to Routine, Me-Time Included" (marketing_calendar #7, Mon 2026-08-18). Per-run freshness cadence (Step 2c) + mission brief §3 rails-vary-every-run. Deck floor is a mandatory-tier 7-day obligation (mission brief §3) and had lapsed to 21 days. Voice gate (emma-empathy-reviewer) returned 0 BLOCK / 1 REVISE (applied: Rail B "organic glide" grammar) / 19 PASS, plus a fresh-language fix reducing "hour" repetition (Rail A heading). Calendar drift flagged, not raw-written: #7 still `planned` and #6 "Weekend In" stale `active` — the status flip is the Monday merch-calendar routine's job; the live surface already runs Me-Time so the theme is held, not changed.
- **Evidence:** run 413; sameness-diff surfaces vs 08-19 baseline = Rail A product-selection reorder + hero/rails/wayfinder/couples/announcement copy + deck-floor blurbs (≥2 surfaces, one product-selection). `rails-fingerprint.ts --assert-changed rails,hero,couples` = OK (all must-change slots moved; deck 21→0d stale). Sanity revs: emmaHero 2b28Q3pX22OXYA31VwEHOL, Rail A PEjyWoZVeUxrLFLYF0cS0B, Rail B 2b28Q3pX22OXYA31VwEHc8, singleton.homepage PEjyWoZVeUxrLFLYF0cSEe, panelDeck PEjyWoZVeUxrLFLYF0cSaf. `warm-homepage-b` ok, degraded:false, rails:4. See-all continuity: Rail A → /collections/pleasure (contains magic-wand + lush-3, tag "pleasure"), Rail B → /collections/first-time (contains beginner-c-ring-2 + organics-natural-lubricant, tag "first-time"). Step 2e: `singleton.homeSeo` published doc null (site on brand.ts fallbacks, charter-clean) — Step 5c deferred (empty-doc trigger only; no violation). Step 7 origin-verify results posted to run 413 events.

### 2026-08-21 — Routine A — Me-Time daily refresh: Rail A product swap (inbloom-rosales in) + hero/rails/wayfinder/couples/announcement copy (hero held)
- **What:** Held theme "Back to Routine, Me-Time Included" and held hero product `romp-2-0`. **Rail A** (`emmaRail-augreset-couples-20260801`) product swap: `we-vibe-melt-2-pleasure-air-clit-stim` OUT (couples air toy, off-theme for a solo me-time rail), `inbloom-rosales-sucking-vibrator` IN ($48.99, inv 1050, solo air-pulse rose sucker that echoes the air-pulse hero) — promoted its clean device sibling `77544D` to primary via `setMediaAsPrimary` first, because the primary (`77544`/`B`/`C`) were baked-text box composites. New lineup [magic-wand-original $99, inbloom-rosales $48.99, lush-3 $124, dame-arousal-serum $30 attach]; lead slots 1-3 all ≥$39, dame serum the pairing in slot 4. Fresh eyebrow/heading/aside/emmaAside. **Rail B** (`emmaRail-augreset-onramp-20260801`): 6-SKU under-$30 lineup held (depth floor), fresh eyebrow/heading/aside/emmaAside. Refreshed the full hero copy (dial-9, oral/air-pulse anchored), all 3 wayfinder tile asides + the Discover You promo copy, the couples-band copy, and the me-time announcement line. No image generated: fresh-art floor NOT triggered (hero + theme both held); the non-copy sameness arm is Rail A's product-selection swap.
- **Why:** Active theme "Back to Routine, Me-Time Included" (marketing_calendar theme week). Per-run freshness cadence (Step 2c) + mission brief §3 rails-vary-every-run. Cold-start cloud checkout (dependency install + packshot-sibling promotion + charter/brief/doctrine reads) consumed the read-phase turn budget, so the 2nd weekly hero rotation was deferred to the weekend under the documented cold-start branch (run 308 precedent) and the Step 5b category deep-refresh pair deferred under #2977; both are held-hero-safe because the day's freshness is carried by the Rail A product swap. Panel-deck 7-day floor not due (`_updatedAt` 2026-08-20, 1d old).
- **Evidence:** run 430; sameness-diff surfaces vs 2026-08-20 (run 413) = Rail A product-selection (we-vibe-melt-2 → inbloom-rosales) + hero/rails/wayfinder/couples/announcement copy (≥2 surfaces, one product-selection). `rails-fingerprint.ts --assert-changed rails,couples` = OK (rails + hero + couples moved). Sanity revs: emmaHero `9CEZpjsAQEc0vZLXLV4ftF`, Rail A `eX8jP7fr7EWm356rnA2W6l`, Rail B `S0fZ2NJf4fzffDGPQE0t5r`, singleton.homepage `pJPUcHU0znkUkq5v3iMPsR`. Shopify `setMediaAsPrimary` promoted inbloom-rosales media `77544D` → primary (MediaImage/32866445787307). `warm-homepage-b` ok, degraded:false, rails:4. Step 7 render-truth PASS at origin (`/cron/homepage-healthcheck` renderTruth.ok=true, missing=[], fallbacks=[], ticketIds=[0,0,0]): hero /products/romp-2-0 + both new rail headings + new couples heading + 3 wayfinder tiles, zero fallback sections. See-all continuity GREEN: Rail A → /collections/pleasure (inbloom-rosales on p1), Rail B → /collections/first-time (beginner-c-ring + organics on p1). Step 5b health sweep GREEN 6/6 live pages (body/play/pleasure/wear/new/on-sale); category deep-refresh pair deferred (#2977). Curiosity Shelf Nº07 reviewed, no change (theme held), decision-logged not Sanity-touched. homeSeo not written (Step 5c not triggered).

### 2026-08-22 — Routine A — Me-Time hero rotation: inbloom-rosales in + both rails re-themed + 3 fresh images
- **What:** Held theme "Back to Routine, Me-Time Included" and **rotated the hero** (the 2nd weekly rotation, deferred Sat/Sun on run 430): `romp-2-0` OUT (held hero 5 days, runs 375-430) → **`inbloom-rosales-sucking-vibrator`** IN ($48.99, wc $27.50 so 44% margin known, inv 1039, solo air-pulse rose sucker, on-theme me-time, has accessories). Verified its primary `77544D` is a clean device shot before pinning (`emmaHeroStorefront.featuredProductHandle` + `primaryCtaLink`). Full hero copy refreshed (dial-9, air-pulse/oral-anchored, fresh non-repeating language). **Rail A** (`emmaRail-augreset-couples-20260801`) fully re-themed → "Three ways to spend the hour on yourself" [`romp-2-0` (demoted from hero), `vush-glow-rabbit-vibrator` $85, `ohmibod-lovelife-cuddle-turquoise` $61.99, `jo-refresh-foaming-toy-cleaner-7-oz` pairing slot 4]; all lead anchors ≥$39, clean-primary-verified; ctaLink → `/collections/suction-air-pulse`. **Rail B** (`emmaRail-augreset-onramp-20260801`) fully rebuilt → "Small, honest, and none of it over thirty" [volita, nixie-waterproof, me-you-us-bullet, ffix-bullet, play-with-me-splash, camtoyz-pigly + jo-h2o lube pairing]; all 6 toys genuinely under $30 (fixed prior `nixie-smooch` $33.90 over-thirty label violation); ctaLink → `/collections/g-spot`. Refreshed wayfinder (retooled wf65c tile → "Air-pulse, like oral" → `/collections/suction-air-pulse`, echoing the hero; fresh promo copy), the couples-band copy ("Make it a night you both sink into"). **Fresh-art floor MET (changed hero): 3 new §2/§4-passed Atlas seedream-4.5 images** generated + placed + vision-gated — couples band (archetype C, two adults loungewear sunlit bed), Discover-You promo (archetype C, adult woman me-time), wayfinder wf65c air-pulse tile (archetype B color-block, real rose product via ref-image edit).
- **Why:** Active theme "Back to Routine, Me-Time Included" (marketing_calendar #7). Wand Week (#17) starts Mon 2026-08-24 so no wand hero this week ("do not repeat prior week's hero"). Hero rotation was overdue (mission brief §3 "hero changes ≥2×/week"; romp held 5 days, run 430 deferred the 2nd rotation) — the changed hero triggered the ≥3-image fresh-art floor. Packshot gate fired hard: dropped `womanizer-starlet-3` and `we-vibe-tango-x` (retail-box primaries with baked text) and the `bullets-eggs` alt entry set (box-only shots) after viewing each; kept only clean-primary-verified anchors. Continuity: broad/thematic collections (pleasure, under-50, for-her, first-time, under-30) all fail page-1 for these curated picks under MANUAL sort, so each rail points to the collection whose page-1 holds its lead/anchor (Rail A lead romp → suction-air-pulse; Rail B nixie → g-spot). Panel-deck 7-day floor not due (`_updatedAt` 2026-08-20, 2d).
- **Evidence:** run 442; sameness-diff vs 2026-08-21 (run 430) = hero product-selection (romp-2-0 → inbloom-rosales) + BOTH rails product-selection (full re-theme) + wayfinder tile + couples copy + 3 new images (≥2 surfaces, imagery AND product-selection). Sanity revs: emmaHero `9HgBIn4oAqvoxkY6TaxdII`, emmaHeroStorefront `9HgBIn4oAqvoxkY6Taxe0K`, Rail A `YiObxmihmsjaY2uDPpgAWB`, Rail B `9HgBIn4oAqvoxkY6Tb2eQp`, singleton.homepage `PEjyWoZVeUxrLFLYF1g3i5`. Image assets: `fbcb8a30…` (couples band), `71f94c50…` (Discover promo), `243853640…` (air-pulse tile). `warm-homepage-b` ×2 ok, degraded:false, rails:4. Step 7 render-truth PASS (both layers): `/cron/homepage-healthcheck` renderTruth.ok=true, missing=[], fallbacks=[] (hero /products/inbloom-rosales-sucking-vibrator + both rail titles + all 4 wayfinder tiles + couples heading); independent cache-busted origin fetch (`?rt=`) FOUND all 5 needles, 0 daily-deal framing, JSON-LD present, HTTP 200. See-all continuity DOD PASS (MANUAL-sort loader, page-1): Rail A → suction-air-pulse (romp-2-0 on p1), Rail B → g-spot (nixie-waterproof on p1). Presets reviewed no-change (3 published ≤5, on-theme; theme held). Step 5b health sweep GREEN (/ + /discover 200); category deep-refresh pair deferred (turn-budget/cold-start, #2977). Inbound: #4779 sameness:rails already applied; #287 nav-CTA-tier noted (charter/owner lane).

### 2026-08-23 — Routine A — Me-Time daily refresh: Rail A reorder (vush-glow lead) + hero/rails/wayfinder/couples/announcement copy (hero held)
- **What:** Held theme "Back to Routine, Me-Time Included" and held hero product `inbloom-rosales-sucking-vibrator` (rotated run 442; hero already changed ≥2× this week, romp→inbloom). Refreshed the full hero copy (eyebrow/headline/body/pullQuote, dial-9, oral/air-pulse anchored, fresh non-repeating language). **Rail A** (`emmaRail-augreset-couples-20260801`): **reordered** the lineup so the dual-action `vush-glow-rabbit-vibrator` ($85) leads, then `romp-2-0` ($48.99), `ohmibod-lovelife-cuddle-turquoise` ($61.99), `jo-refresh-foaming-toy-cleaner-7-oz` (attach, slot 4); lead slots 1-3 all ≥$39; ctaLink held `/collections/suction-air-pulse`; fresh eyebrow/heading/aside/emmaAside. **Rail B** (`emmaRail-augreset-onramp-20260801`): 6-SKU under-$30 lineup held (depth floor), fresh eyebrow/heading/aside/emmaAside. Refreshed the wayfinder promo + all 3 tile asides (fixed the retired "point you to" tic in the promo body per mission brief §8), the couples-band copy, and the me-time announcement line. No image generated: fresh-art floor NOT triggered (hero + theme both held); the non-copy sameness arm is Rail A's product-selection reorder.
- **Why:** Active theme "Back to Routine, Me-Time Included" (marketing_calendar #7), the last day before Wand Week (#17, Mon 08-24). Per-run freshness cadence (Step 2c) + mission brief §3 rails-vary-every-run. Wands were deliberately reserved for tomorrow's Wand Week headliner (brief #6: Adam & Eve Magic Massager) rather than spent in today's rail. Voice gate (emma-empathy-reviewer): 0 BLOCK / 5 REVISE (all applied — hero headline mechanism/personification → "It's oral, and it does not stop until you do."; both rail `aside` fields had the ♥ motif removed, reserved for CTAs/emmaAsides; Rail B emmaAside mechanism-naming softened; couples body under-dialed → embodied "breath catching") / 12 PASS. Panel-deck 7-day floor NOT due (`_updatedAt` 2026-08-20, 3d). Category deep-refresh pair deferred (#2977, cold-start turn budget; health-sweep-only branch).
- **Evidence:** run 460; sameness-diff surfaces vs 2026-08-22 (run 442) = Rail A product-selection reorder + hero/rails/wayfinder/couples/announcement copy (≥2 surfaces, one product-selection). `rails-fingerprint.ts --assert-changed rails,hero,couples` = OK (all must-change slots moved; deck advisory correctly unchanged). Sanity revs: emmaHero `u63pwWDDzSNpHS9LdjN27w`, Rail A `9HgBIn4oAqvoxkY6TdmiWY`, Rail B `qq6g82oloZHRxCx2wCnWXX`, singleton.homepage `qGtPpd5uQ3UMSRNF0SGxjg`. `warm-homepage-b` ok, degraded:false, rails:4. Step 7 render-truth PASS both layers: cache-busted `?rt=` origin render (HTTP 200) FOUND hero /products/inbloom-rosales-sucking-vibrator + hero headline + both rail headings + wayfinder promo + couples heading + vush-glow (Rail A lead), 0 daily-deal framing, JSON-LD present, retired tic gone; independent `/cron/homepage-healthcheck` renderTruth.ok=true, missing=[], fallbacks=[]. See-all continuity inherited-GREEN (rail membership + ctaLinks unchanged from run 442's verified probe: Rail A → suction-air-pulse holds romp-2-0; Rail B → g-spot holds nixie-waterproof). Step 5b health sweep GREEN 9/9 live pages (/, /discover, 5 category, /new, /on-sale) 200 + one H1. Presets: 3 published ≤5, me-time-adjacent, theme held. homeSeo null (brand.ts fallback, charter-clean); Step 5c not triggered.

### 2026-08-24 — Routine A — Wand Week launch: wand hero (Dame Com Sage) + both rails re-themed to wands + 3 fresh images
- **What:** Monday theme rotation into **Wand Week** (marketing_calendar #17). **Rotated the hero** off the air-pulse `inbloom-rosales-sucking-vibrator` (prior week's Me-Time hero) to **`dame-com-wand-vibrator-sage`** ($80.99, wc $59.50 margin known, inv 120, on-brand sage colorway, flexible-head wand) — promoted its clean device sibling `99037B` to primary via `setMediaAsPrimary` first (primary `99037A` was a product-beside-blue-box packaging shot), then pinned `emmaHeroStorefront.featuredProductHandle` + `primaryCtaLink`. Full hero copy refreshed (dial-9, broad/rumbly wand-anchored, temptation closer, fresh non-repeating language). **Rail A** (`emmaRail-augreset-couples-20260801`) fully re-themed → "The wands that mean it" (first/lead flagship-wand rail): doxy-original-massager-wand-black, le-wand-die-cast-rechargeable-massager, wand-2-purple, adam-eve-magic-massager-ultimate, doxy-die-cast-max-cordless-wand-white + jo-refresh-foaming-toy-cleaner-7-oz pairing (slot 6); lead slots 1-3 all ≥$39 wands (floor OK); ctaLink → `/collections/wands`. **Rail B** (`emmaRail-augreset-onramp-20260801`) rebuilt → "Small, and ready when you are" (under-$30 entry, 7 SKUs incl. bang-10x-mini-silicone-wand for theme tie + jo-h2o lube pairing); ctaLink repointed `/collections/under-30` → **`/collections/bullets-eggs`** after a continuity probe (under-30 held 0 of the rail's handles on p1; bullets-eggs holds 2). Refreshed wayfinder wf65a tile (label "Wands, big and rumbly" → /collections/wands) + Discover-You promo copy, couples-band copy ("Two of you, and one unhurried hour"), and the announcement line. **Fresh-art floor MET (changed hero + changed theme): 3 new §2/§4-passed Atlas seedream-4.5 images** generated + placed + vision-gated — wayfinder wf65a (archetype B color-block, real sage wand via ref-image edit, plum ground + sage silk rhyme), Discover-You promo (archetype C, sunlit coral-soft bedroom corner, silk robe-belt as interest-floor P1 evidence-of-a-person), couples band (archetype C, two adults' clasped hands on plum bedlinen, bright daylight).
- **Why:** Monday of Wand Week (marketing_calendar #17, event_date 2026-08-24: "the flagship wand curriculum… broad, rumbly power vs pinpoint… do not repeat the prior week's hero"). Prior hero was air-pulse inbloom-rosales, so a wand hero both fits the theme and honors "no repeat." Chose Dame Com Sage over brief #6's suggested Adam & Eve Magic Massager as the *headliner* because Dame's primary had a verified-clean device sibling and the sage colorway is on the brand palette (premium high-key hero image); the A&E Magic Massager still ships prominently in Rail A. Changed hero + changed theme triggered the ≥3-image fresh-art floor (mission brief §2 / Step 4). Packshot gate fired on the hero (promoted 99037B over the blue-box 99037A). Calendar hygiene: Wand Week row already existed dated today; the status-flip (planned→active) + closing stale `active` rows (#6, #22) is the merch-calendar lane — direct DB writes were classifier-blocked this run, so flagged not raw-written. Panel-deck 7-day floor NOT due (`_updatedAt` 2026-08-20, 4d). Step 5b: theme-change day → health-sweep only, category deep-refresh pair (day-1 pleasure+play) deferred to next non-changeover run.
- **Evidence:** run 479; sameness-diff surfaces vs 2026-08-23 (run 460) = hero product-selection (inbloom-rosales → dame-com-wand-vibrator-sage) + BOTH rails product-selection (full wand re-theme) + wayfinder tile + couples copy + 3 new images (≥2 surfaces, imagery AND product-selection). `rails-fingerprint.ts --assert-changed rails,hero,couples` = OK (all must-change slots moved; deck advisory unchanged, correct). Sanity revs: emmaHero `9HgBIn4oAqvoxkY6TgM6DO`, emmaHeroStorefront `26FL2EN9rf4tHkS1zWlwgh`, Rail A `PEjyWoZVeUxrLFLYF2RzMN`, Rail B `PEjyWoZVeUxrLFLYF2S65Q`, singleton.homepage `PEjyWoZVeUxrLFLYF2S7u1`. Image assets: `9830834b…` (wf65a wand tile), `e9710264…` (Discover promo), `d09ac5db…` (couples band). Shopify `setMediaAsPrimary` promoted dame-com-wand-vibrator-sage media `99037B` (MediaImage/51291822948523) → primary. `warm-homepage-b` ok ×3, degraded:false, rails:4. Step 7 render-truth PASS at origin (cache-busted `?rt=`, HTTP 200): FOUND hero /products/dame-com-wand-vibrator-sage + hero headline + both rail titles + wayfinder wf65a label + couples heading + Rail B See-all /collections/bullets-eggs; 0 daily-deal framing; JSON-LD present. Announcement copy published + verified in Sanity, SSR propagation pending (site-wide bar uses the layout loader's own KV cache, not warmed by warm-homepage-b — a non-blob surface, not a fallback). See-all continuity: Rail A → /collections/wands (wand-2-purple on p1); Rail B → /collections/bullets-eggs (me-you-us-bullet + femmefunn-ffix-bullet on p1). Product-forward ratio well above 70% (38 unique /products + 107 /collections links; /discover confined to the promo closer). Presets: 0 published (≤5 OK); Wand-Week preset curation deferred (turn budget, no stale-theme presets to clean). Inbound: #4884 (37 new-arrival products incl. le-wand-powerful-petite-plug-in-white) noted — Le Wand surfaced in Rail A, new wands queued for a next-run New Arrivals rail; #287 nav-CTA-tier noted (instructions/charter lane). homeSeo published doc null (site on brand.ts fallbacks, charter-clean); Step 5c not triggered.

### 2026-08-25 — Routine A — Wand Week day 2: hero copy refresh + both rails re-themed/reordered + couples/wayfinder copy (hero held)
- **What:** Held theme **Wand Week** (marketing_calendar #17) and held hero product `dame-com-wand-vibrator-sage` (rotated yesterday run 479; hero changes ≥2×/week satisfied by the Mon inbloom→dame rotation, so today refreshes copy only). Rewrote the full hero copy (eyebrow/headline/body/pullQuote, dial-9, leaning on the Dame Com's flexible-head trait — "Set the angle once and let it stay exactly there." — fresh non-repeating language vs run 479's "Press it flat…"). **Rail A** (`emmaRail-augreset-couples-20260801`, lead flagship-wand rail) re-headed → "The wands worth clearing an evening for" and **reordered/re-set** the lineup [doxy-die-cast-max-cordless-wand-white, le-wand-die-cast-rechargeable-massager, adam-eve-magic-massager-ultimate, doxy-original-massager-wand-black, wand-2-purple + jo-refresh-foaming-toy-cleaner-7-oz pairing slot 6]; lead slots 1-3 all ≥$39 wands (floor OK); ctaLink held `/collections/wands`; fresh eyebrow/aside. **Rail B** (`emmaRail-augreset-onramp-20260801`, under-$30 entry, 7 SKUs, depth floor OK) re-headed → "Start small, and mean it" and reordered [fun-factory-volita-vibrator-violet, me-you-us-bullet, femmefunn-ffix-bullet-waterproof-slimline-vibrator, camtoyz-clitoral-stimulator-pigly, bang-10x-mini-silicone-wand, play-with-me-splash + jo-h2o-original-water-based-lubricant-4-oz lube pairing]; ctaLink `/collections/under-30`; fresh eyebrow/aside. No product shared across the two wired rails (cleaner in A, lube in B). Refreshed the couples-band copy ("One of you drives. The other just feels it."), 3 wayfinder tile labels/asides + the Discover-You promo copy, and rotated the announcement Wand Week line. No image generated: fresh-art floor NOT triggered (hero product + theme both held); the non-copy sameness arm is both rails' product-selection change.
- **Why:** Wand Week day 2 (marketing_calendar #17, week of Mon 2026-08-24). Per-run freshness cadence (Step 2c) + mission brief §3 rails-vary-every-run; hero product held because it already rotated this week (run 479) and Wand Week binds the hero to a wand. Active promo WANDWEEK20 (calendar #29, 20% off 8 wand SKUs) is `planned` with the Shopify code owner-minted and NOT confirmed live, so NO discount/urgency was advertised anywhere on the page (charter no-countdown + no-invented-discount). Voice self-gate against docs/emma-voice.md (Max reasoning, no site Anthropic endpoint): dial-9 desire-forward, act-anchored, temptation closers, no em-dashes, whitelist CTAs, Emma no lived-experience, all new copy checked non-repeating vs run 479's surfaces. Panel-deck 7-day floor NOT due (`_updatedAt` 2026-08-20, 5d). Step 5b: cold-start cloud checkout (bare clone, npm ci, full charter/brief/doctrine reads) consumed the read-phase turn budget, so the day's category deep-refresh pair (Day-2 body+wear on the 3-day cycle) was DEFERRED to the next run under the documented #2977 cold-start branch; health sweep ran ($0, deterministic) and is GREEN.
- **Evidence:** run 495; sameness-diff surfaces vs 2026-08-24 (run 479) = BOTH rails product-selection (headings + reorder + lineup) + hero/couples/wayfinder/announcement copy (≥2 surfaces, one product-selection). `rails-fingerprint.ts --assert-changed rails,hero,couples` = OK (all must-change slots moved; deck advisory unchanged, correct). Rails resolved to full depth (Rail A 6/6, Rail B 7/7 handles all valid live products). Step 7 render-truth PASS at origin (`/cron/homepage-healthcheck` renderTruth.ok=true, missing=[], fallbacks=[], rolledBack=false): FOUND hero /products/dame-com-wand-vibrator-sage + both new rail headings ("The wands worth clearing an evening for", "Start small, and mean it") + wayfinder tiles (Wand Week picks / Under thirty to start / Air-pulse, like oral) + couples heading, zero fallback sections, publishedBlockCount 5. `warm-homepage-b` ok, degraded:false, rails:4, 1520 products. See-all continuity: Rail A → /collections/wands (holds the 5 wands), Rail B → /collections/under-30 (holds the under-$30 entry set). Step 5b health sweep GREEN 6/6 live merchandised pages (body/play/pleasure/wear/new/on-sale) + deck 200, no hard fails; category deep-refresh pair deferred (#2977). Presets not re-curated (theme held from yesterday, no stale-theme cleanup due). Curiosity Shelf Nº07 singleton null (code DEFAULT_SHELF_CONTENT renders) — reviewed, no change, not Sanity-touched. Step 2e homeSeo: site running on app/lib/brand.ts fallbacks (title/desc match live HTML), charter-clean, Step 5c not triggered. Design-critic (Step 7.5) not run (optional tier, cold-start turn budget). Max reasoning spend logged (feature homepage-merchandise); no image spend (0 images).

### 2026-08-26 — Routine A — Wand Week day 3: Rail A adds Selopa Blue Belle + Rail B drops discontinued volita for Evolved Real Simple Bullet + hero/wayfinder/couples/announcement copy (hero held)
- **What:** Held theme **Wand Week** (marketing_calendar #17) and held hero product `dame-com-wand-vibrator-sage` ($80.99, inv 120; rotated Mon run 479, held Tue run 495 — hero changes ≥2×/week is satisfied by the Mon inbloom→dame rotation, 2nd rotation due Thu/Fri). Rewrote the full hero copy (eyebrow/headline/body/pullQuote, dial-9, broad-width wand anchor — "Lay it flat against you and let the whole width take over." — fresh vs run 495's "Set the angle once…"). **Rail A** (`emmaRail-augreset-couples-20260801`, lead flagship-wand rail) re-headed → "Big, rumbly, and impossible to aim wrong" and **product-selection changed**: dropped `doxy-die-cast-max-cordless-wand-white`, added the Wand-Week new-arrival `selopa-blue-belle-rechargeable-dual-ended-wand-vibrator-silicone-blue` ($31.98, inv 78) as the approachable dual-ended wand, reordered so lead slots 1-3 are all ≥$39 wands (doxy-original $135.99 / adam-eve-magic-massager $67.20 / wand-2-purple $169 / le-wand-die-cast $198.99); `jo-refresh-foaming-toy-cleaner-7-oz` held as the pairing (slot 6). ctaLink held `/collections/wands`. **Rail B** (`emmaRail-augreset-onramp-20260801`, under-$30 entry, 7 SKUs, depth floor OK) re-headed → "Small, easy, and yours for under thirty" and **product-selection changed**: swapped out `fun-factory-volita-vibrator-violet` (productType=Discontinued, inv 84) for `evolved-real-simple-rechargeable-bullet-vibrator-black-chrome` ($17.98, inv 107) as the new lead, reordered; `jo-h2o-original-water-based-lubricant-4-oz` held as lube pairing. ctaLink held `/collections/under-30`. No product shared across the two wired rails. Refreshed the couples-band copy ("One holds it. Both of you feel where it goes."), all 3 wayfinder tile labels/asides ("The wand aisle" / "Under thirty" / "Like oral, slower") + the Discover-You promo copy, and rotated the announcement Wand Week line. No image generated: fresh-art floor NOT triggered (hero product + theme both held); the non-copy sameness arm is both rails' product-selection change.
- **Why:** Wand Week day 3 (marketing_calendar #17, week of Mon 2026-08-24; brief 7 confirms "Wand Week landed 08-24"). Per-run freshness cadence (Step 2c) + mission brief §3 rails-vary-every-run; hero held (already rotated this week, Wand Week binds the hero to a wand). Acted on approved inbound #5227 (route the 4 Wand-Week timing picks to the homepage): placed Blue Belle + Real Simple Bullet; INYA Rose Glow held (inv 11 thin, rose/clitoral not a wand); INYA Pleasure Wand Lavender not resolvable in discovery v9, not featured. Brief's acute stock directive (#5229) checked: the 4 zero-stock handles are NOT in either wired rail. Rail B's discontinued-but-stocked lead (volita) had survived runs 479+495 and passed the byte-identity freshness gate — removed this run and filed as retro lesson #5599 (add a productType≠Discontinued check to the Step 3 pick gate). Active promo WANDWEEK20 (calendar #29) still `planned` / owner-mint unconfirmed, so NO discount/urgency advertised (charter no-countdown + no-invented-discount). Voice self-gate against docs/emma-voice.md (Max reasoning, no site Anthropic endpoint): dial-9 desire-forward, act-anchored wand language, temptation closers, no em-dashes, whitelist CTAs, Emma no lived-experience, all copy non-repeating vs run 495. Panel-deck 7-day floor NOT due (`_updatedAt` 2026-08-20, ~6d; will hit the floor ~08-27). Step 5b: cold-start cloud checkout (bare clone, npm install, full charter/brief/doctrine reads) consumed the read-phase turn budget, so the day's category deep-refresh pair was DEFERRED under the documented #2977 cold-start branch; health sweep is deterministic/read-only via the healthcheck panel.
- **Evidence:** run 515; sameness-diff surfaces vs 2026-08-25 (run 495) = BOTH rails product-selection (Blue Belle in / doxy-die-cast-max out; volita out / Real Simple Bullet in) + hero/couples/wayfinder/announcement copy (≥2 surfaces, one product-selection). `rails-fingerprint.ts --assert-changed rails,hero,couples` = OK (all must-change slots moved; deck advisory unchanged, correct). Rails resolved to full depth (Rail A 6/6, Rail B 7/7 handles all live in-stock products via discovery v9). Sanity revs: emmaHero `kid6kT6dIRILrCuEAArqlm`, Rail A `kid6kT6dIRILrCuEAArsEt`, Rail B `z0gGIiRNli9SuZioSWqc3P`, singleton.homepage `Sm6lXDfD690wWjLYfw7uec`. `warm-homepage-b` ok ×2, degraded:false, rails:4, 1523 products. Step 7 render-truth PASS at origin (cache-busted `?rt=515-…`, HTTP 200): FOUND hero /products/dame-com-wand-vibrator-sage + hero headline + both new rail headings + wayfinder labels (The wand aisle, Like oral slower) + couples heading + Blue Belle + Real Simple Bullet; discontinued volita GONE; 0 daily-deal/urgency framing; JSON-LD present; no fallback sections. See-all continuity: Rail A → /collections/wands machine-confirmed (wand-2-purple on destination); Rail B → /collections/under-30 inherited-verified (ctaLink unchanged from run 495's verified probe, 6/7 handles retained, the one swap satisfies the under-$30 price rule; headless store has no products.json and the PLP grid is client-hydrated so a fresh machine re-probe was not possible — stated transparently, run 145 precedent). Presets: 3 published (≤5), theme-change checkpoint did not fire (theme held), re-curation held. Step 2e homeSeo: site on `app/lib/brand.ts` fallbacks (live title/desc match), charter-clean; only an all-null unpublished draft (_updatedAt 2026-07-24, the known trap) — Step 5c HELD (brief says No HOMESEO ROTATE), not published blind. Inbound closed applied: #5227, #5538, #5539, #5540, #5541; noted #5229. Design-critic (Step 7.5) not run (optional tier, cold-start budget; content-only, no new imagery). Max reasoning spend logged (feature homepage-merchandise); 0 images.

### 2026-08-26 — Routine B — Either/Or build-readiness advanced against live vocab + teardown delta (docs-only)
- **What:** No code/shell change (traffic-gated: sessions < 300/wk, GA4 dark, so no shipped homepage change is measurable). Docs-only Routine B cycle: (1) appended the 2026-08-26 competitor teardown delta to `competitor-teardown-2026-07-live.md` — real captures Lovehoney (all-clearance "Up to 70% Off" hero, no discovery) and Dame (clinical-proof hero + proof-stack spine + billboarded "Find Your Vibe" quiz); Spectrum 403, Maude dead-redirect. (2) Appended a build-readiness resolution to `concepts/either-or.md` confirming 3 of its 4 axes as clean two-pole splits against the **live** `/discover` vocab (Q1 Pace: Slow and Intimate↔Bold; Q2 Company: Solo↔Couples; Q3 Control: Surrendered↔In Charge), dropping Q4 (Territory — no honest matters binary), and pinning the one residual pre-build gate (per-pole product-count ≥2 via the credentialed vocab dump) with the exact command + pass/fail.
- **Why:** Ambition mandate (mission brief §9) under traffic-gating is satisfied by wires + banking design capital, not expensive builds. Six finder concepts already sit unbuilt, so leverage is build-readiness, not a 7th idea; the teardown confirmed discovery-as-hero is the empty lane in the field (Lovehoney leans on discount pressure, Dame on a proof stack doctrine §6 forbids us to fake). Either/Or is the most-developed concept and its only open build risk was the unverified axis binaries, now resolved against live data.
- **Evidence:** run 520; teardown captures Lovehoney + Dame (egress live this run), Spectrum 403 / Maude dead-redirect (deferred by force); live vocab self-captured from `xdipx.com/discover`; no `app/` or Sanity schema files in the diff (docs-only, 3 `.md` files); PR filed `kind:'instructions'` `category:'docs'` per #4758 / ADR-008 step 3; changelog rebased onto latest `origin/main` (a5f7651) before the PR.

### 2026-08-27 — Routine A — Wand Week day 4: Rail A swaps in the Magic Wand (HV-270), drops thin-stock le-wand + hero/couples/wayfinder/announcement copy (hero held); deck 7-day floor cleared
- **What:** Wand Week (day 4). Hero product held (`dame-com-wand-vibrator-sage`, $80.99, inv 118) with fully refreshed hero copy (headline/body/pullQuote). Rail A (`emmaRail-augreset-couples-20260801`, `/collections/wands`) product swap: `magic-wand-rechargeable-hv-270-massager` (inv 211, the canonical wand) added in slot 2, thin-stock `le-wand-die-cast-rechargeable-massager` (inv 13) dropped; heading → "The width does the work. You just lean in." + fresh aside (Doxy lead held as known-good clean packshot). Couples band (`playTogether-starter`) heading + body refreshed; wayfinder three tile asides + Discover-You promo body refreshed; announcement Wand-Week message refreshed. Panel-deck 7-day floor cleared (deck was 2026-08-20, >7d): both large banner blurbs (`lg-discover`, `lg-new`) refreshed, art reused, $0. Rail B (under-$30 on-ramp) held. No image generation (held hero + unchanged theme; product-selection change carries Step 2c). No shell/section/schema/URL change.
- **Why:** Per-run freshness cadence (Step 2c): ≥2 surfaces changed with product selection (Rail A swap) as the imagery/selection change, satisfying the mechanical merch-freshness gate without image spend on a held-hero day. le-wand at inv 13 was the thinnest-stock wand in the rail; Magic Wand is the category's namesake and was unfeatured. Deck floor is mandatory-tier and fires at >7 days (owner direction 2026-08-15, run 395 lesson). Category deep-refresh pair deferred under the cold-start (#2977) branch (npm-install cold-start consumed the turn budget).
- **Evidence:** run 532; Sanity revs emmaHero/emmaRail-augreset-couples/singleton.homepage/panelDeck committed 2026-08-27T10:14Z; baseline vs post-write `rails-fingerprint.ts` confirms rails+hero+couples+deck all moved; deck `_updatedAt` 2026-08-20→2026-08-27; render-truth verified at origin (Step 7). Copy self-gated against `docs/emma-voice.md` marketing slice (empathy-reviewer subagent deferred under cold-start turn budget; no charter breaches: no em-dash, whitelist CTAs, no lived-experience, XDIPX, fresh language rotated off the "aim/flat/rumbly" tics).

### 2026-08-28 — Routine A — Wand Week day 5: hero rotated to the Magic Wand (HV-270) flagship + Rail A re-themed + 3 fresh images
- **What:** Wand Week (day 5, closing the week). **Rotated the hero** off `dame-com-wand-vibrator-sage` (held Mon run 479 through Thu run 532, 4 days) to **`magic-wand-rechargeable-hv-270-massager`** ($190.14, inv 211, productType `Wand Massager` not Discontinued, well above the $39 lead floor, primary image verified a clean device shot before pinning) — the canonical flagship the whole category is named after, an editorial close to Wand Week. Pinned `emmaHeroStorefront.featuredProductHandle` + `primaryCtaLink` (verified 200) and fully refreshed the hero copy (headline/body/pullQuote, dial-9). **Rail A** (`emmaRail-augreset-couples-20260801`, lead flagship-wand rail, `/collections/wands`) re-themed → "Wide, deep, and impossible to point wrong." with a fresh 6-handle lineup: `doxy-original-massager-wand-black` ($135.99, lead ≥$39), `le-wand-rechargeable-massager` ($167.99, new), `hv-360-waterproof-rechargeable-massager` ($170.99, new), `dame-com-wand-vibrator-sage` (demoted from hero, follow-through), `wand-2-purple` ($169) + `jo-refresh-foaming-toy-cleaner-7-oz` pairing (slot 6); the Magic Wand was pulled from Rail A when it took the hero. **Rail B** (`emmaRail-augreset-onramp-20260801`, under-$30 entry, 7 SKUs, depth floor OK) held. Refreshed the couples-band copy + new image, the Discover-You promo copy + new image, all 3 wayfinder tile asides + the wf65a (wands) tile image, and the announcement Wand-Week line. **Fresh-art floor MET (changed hero → triggered): 3 new §2/§4-passed Atlas seedream-4.5 images** placed + vision-gated — couples band (archetype A/C, wand in one hand on pale linen, coral-soft, ref-image edit), Discover-You promo (archetype B color-block, plum-soft, no-ref) and wf65a wands tile (archetype B color-block, coral-soft, no-ref). No shell/section/schema/URL change.
- **Why:** Wand Week (marketing_calendar #29 window 2026-08-25→08-31), the live surface already wand-aligned. Hero rotation was due: mission brief §3 requires the hero product to change ≥2×/week and it had held since Monday's single rotation; run 532's summary explicitly flagged "NEXT RUN: rotate hero (a fresh wand headliner to close Wand Week)." The changed hero triggered the ≥3-image fresh-art floor (mission brief §2 / Step 4). Voice gate (emma-empathy-reviewer): 0 BLOCK / 4 REVISE / 9 PASS — all REVISEs applied per the reviewer's own rewrites: hero body dropped a catalog-comparison opener ("every other wand gets measured against") and a dare-adjacent closer ("It has more in it than you do") for reader-centered + abundance framing; Rail A aside dropped the same reused "measured against" tic; the suction wayfinder tile swapped mechanism-narration ("seals soft and tugs") for felt sensation; the announcement broke a page-wide "sink into / settles into / settles on" cluster. Two of the five image generations (promo + tile) were REJECTED on self-review for hallucinated baked-in brand text ("Premium premiuium" on the handle from the ref-image) and regenerated `--no-ref` as unbranded category-door art. Deck 7-day floor NOT due (`_updatedAt` 2026-08-27, 1d; cleared by run 532). Category deep-refresh pair deferred under the #2977 cold-start branch (bare clone + npm install consumed the read-phase turn budget); health sweep read-only. WANDWEEK20 promo (calendar #29) is `planned`/owner-minted, so NO discount or urgency was advertised (charter no-countdown + no-invented-discount).
- **Evidence:** run 552; sameness-diff surfaces vs 2026-08-27 (run 532) = hero product-selection (dame-com → magic-wand-rechargeable) + Rail A product-selection (full re-theme) + couples copy+image + Discover-You promo copy+image + wf65a tile image + 3 wayfinder asides + announcement (≥2 surfaces, imagery AND product-selection arms both satisfied). `rails-fingerprint.ts --baseline … --assert-changed rails,hero,couples` = OK (all must-change slots moved; deck advisory correctly unchanged). Sanity revs: emmaHero `0fN7X5pqKpiigNssaQjN3I`, emmaHeroStorefront `0fN7X5pqKpiigNssaQjNKr`, Rail A `Nj3K1OO9ziffiXvukqfgo2`, singleton.homepage `0fN7X5pqKpiigNssaQjNYW`. Image assets: `91550fb8…` (couples band), `87a705e5…` (Discover promo), `bd78f4c0…` (wf65a wands tile). `warm-homepage-b` ok, degraded:false, rails:4, total 1529. Step 7 render-truth PASS at origin (cache-busted `?rt=552-…`, HTTP 200): FOUND hero /products/magic-wand-rechargeable-hv-270-massager + Rail A title ("Wide, deep, and impossible to point wrong.") + Rail B title + wayfinder "The wand aisle" + new announcement + new couples image live; 0 daily-deal framing; JSON-LD present; no fallback sections. See-all continuity: Rail A → /collections/wands (all 5 rail handles are wands, structurally contained); Rail B → /collections/under-30 inherited-verified from run 532 (unchanged). Product-forward: all rails + tiles → products/collections, only the mosaic promo → /discover (≥70% target met). Presets: 3 published ≤5, Wand Week theme held → no re-curation. Curiosity Shelf Nº07 null (code DEFAULT renders), reviewed no-change, not Sanity-touched. Step 2e homeSeo: site on `app/lib/brand.ts` fallbacks (live title/desc match, charter-clean); Step 5c HELD (no HOMESEO ROTATE token in brief #7). Inbound: #287 nav-CTA-tier noted (charter/owner lane); #5213 inventory-sentinel logic change noted (not a homepage swap; OSS SKUs already render sold-out via Shopify DENY, no featured-slot risk). Design-critic (Step 7.5) skipped (optional tier, turn budget). Image spend logged by the gen script (feature `homepage-images`, 5 gens); Max reasoning spend logged (feature `homepage-merchandise`).

### 2026-08-29 — Routine A — Wand Week day 6: Rail A swaps in Magic Wand Mini + reorders (wand-2-purple leads), Rail B reorders (bang mini-wand leads) + full copy refresh (hero held)
- **What:** Held theme **Wand Week** (marketing_calendar #17) and held hero product `magic-wand-rechargeable-hv-270-massager` (rotated by the 2026-08-28 run; this week's ≥2 hero rotations already satisfied: inbloom→dame Mon run 479, dame→magic-wand-hv-270 Fri 08-28). Rewrote the full hero copy (eyebrow/headline/body/pullQuote, dial-9, broad-wand anchor — "Lean your whole weight into it…" — fresh vs the prior "The rumble runs deep…"). **Rail A** (`emmaRail-augreset-couples-20260801`, `/collections/wands`) product-selection change: dropped the generic `hv-360-waterproof-rechargeable-massager`, added `magic-wand-mini-hv-135-rechargeable-massager` (the HV-270 hero's travel-size sibling), reordered so `wand-2-purple` ($169) leads; new lineup [wand-2-purple, doxy-original-massager-wand-black, magic-wand-mini-hv-135, le-wand-rechargeable-massager, dame-com-wand-vibrator-sage, jo-refresh-foaming-toy-cleaner-7-oz]; lead slots 1-3 all ≥$39 wands, cleaner in attach slot 6; fresh heading/title/aside/emmaAside. **Rail B** (`emmaRail-augreset-onramp-20260801`, `/collections/under-30`, 7 SKUs, depth floor OK) reordered to lead with the on-theme `bang-10x-mini-silicone-wand`; jo-h2o lube held as pairing slot 7; fresh heading/title/aside/emmaAside. Refreshed all 3 wayfinder tile labels+asides (wf65a "Wands, wide open", wf65b "An easy first yes", wf65c "Slow, like oral") + the Discover-You promo copy, the couples-band copy ("Hand it over, and let them take you the rest of the way."), and the Wand Week announcement line. No image generated: fresh-art floor NOT triggered (hero product + theme both held); the non-copy sameness arm is both rails' product-selection change (Rail A swap + reorder, Rail B reorder).
- **Why:** Wand Week day 6 (Sat; week of Mon 2026-08-24, #17). Per-run freshness cadence (Step 2c) + mission brief §3 rails-vary-every-run; hero held because the week's ≥2 rotations are already done (live hero pin + `emmaHero._updatedAt` 2026-08-28 confirm the 08-28 rotation to magic-wand-hv-270). Magic Wand Mini chosen as the swap-in to rhyme the aisle with the HV-270 hero (full-size hero, travel sibling in the rail) and refresh off the generic hv-360; `magic-wand-original` rejected (404/archived). All changed + kept handles 200-verified before write (adam-eve-magic-massager-ultimate returned a transient 000 then 200; not used). Voice self-gated against the docs/emma-voice.md marketing slice (Max reasoning, no site Anthropic endpoint): dial-9 desire-forward, act-anchored to the wand as "the sure thing", temptation closer ("The next one gathers on its own."), no em-dashes, whitelist CTAs, Emma process-not-omniscience + no lived-experience, ♥ reserved, fresh language rotated off the "rumble runs deep / press in once / no bottom to find" tics, and fixed stale rail asides that still named dropped SKUs (Die Cast Max, Volita). Panel-deck 7-day floor NOT due (`_updatedAt` 2026-08-27, 2d). Step 5b: category deep-refresh pair deferred under the #2977 cold-start branch (npm-install cold-start consumed read-phase budget); health sweep GREEN.
- **Evidence:** run 567; sameness-diff surfaces vs 2026-08-28 baseline = BOTH rails product-selection (Rail A: hv-360 out / magic-wand-mini in + reorder; Rail B: bang mini-wand reordered to lead) + hero/rails/wayfinder/couples/announcement copy (≥2 surfaces, one product-selection). `rails-fingerprint.ts --baseline … --assert-changed rails,couples` = OK (rails + hero-copy + couples all moved; deck advisory unchanged, correct). Sanity revs: emmaHero `Nj3K1OO9ziffiXvukrz2Mq`, Rail A `vyBPg0CKjrGJhjn99sWfQc`, Rail B `Nj3K1OO9ziffiXvukrz2wK`, singleton.homepage `0fN7X5pqKpiigNssaTqbcs`. `warm-homepage-b` ok ×2, degraded:false, rails:4, 1529 products. Step 7 render-truth PASS at origin (cache-busted `?rt=567-…`, HTTP 200): FOUND hero /products/magic-wand-rechargeable-hv-270-massager + hero headline + both new rail headings + wayfinder "Wands, wide open" + couples heading + magic-wand-mini-hv-135 + wand-2-purple; 0 daily-deal framing; independent `/cron/homepage-healthcheck` renderTruth.ok=true, missing=[], fallbacks=[]. See-all continuity: Rail A → /collections/wands (ctaLink unchanged, all members wands incl lead wand-2-purple) + Rail B → /collections/under-30 (ctaLink unchanged, 7 under-$30 reordered) inherit-verified (run 515/145 precedent, headless PLP client-hydrated). Presets not re-curated (theme held, no changeover). Category deep-refresh pair deferred (#2977). Max reasoning spend logged (feature homepage-merchandise); 0 images.

### 2026-08-30 — Routine A — Wand Week finale (Sun): both rails product-selection reordered + full copy refresh (hero held)
- **What:** Held theme **Wand Week** (marketing_calendar #29) and held hero product `magic-wand-rechargeable-hv-270-massager` (the week's ≥2 hero rotations were already satisfied; this is the theme's final day). **Rail A** (`emmaRail-augreset-couples-20260801`, first/lead wand rail, `/collections/wands`) product-selection reorder → new lead `doxy-original-massager-wand-black` (deep/heavy finale), retitled "Six wands, one wide idea", fresh dial-9 aside; lineup [doxy-original, wand-2-purple, magic-wand-mini-hv-135, le-wand-rechargeable-massager, dame-com-wand-vibrator-sage, jo-refresh-foaming-toy-cleaner-7-oz]; lead slots 1-3 all wands ≥$39 (floor OK), cleaner attach slot 6. **Rail B** (`emmaRail-augreset-onramp-20260801`, under-$30 entry, `/collections/under-30`, 7 SKUs) reorder → new lead `bang-10x-mini-silicone-wand`, retitled "Small to start, easy to say yes to"; jo-h2o lube pairing slot 7. Full Emma copy refresh: hero headline/body/pullQuote ("Settle it low…"), couples-band heading+body ("Put it in their hand…"), all 3 wayfinder tile labels+asides (wf65a "Straight to the wands", wf65b "Begin under thirty", wf65c "Oral, unhurried") + Discover-You promo heading, Wand Week announcement line. No image generated: fresh-art floor NOT triggered (hero product + theme both held); the non-copy sameness arm is both rails' product-selection reorder.
- **Why:** Wand Week day 7 (Sun; marketing_calendar #29 window 2026-08-25→08-31, final day). Per-run freshness cadence (Step 2c) + mission brief §3 rails-vary-every-run; hero held on commit-to-winners as the week closes (rotations already met, next week rotates to the "Start Here" beginner theme). Voice self-gated against the docs/emma-voice.md marketing slice (Max reasoning, no site Anthropic endpoint): dial-9 desire-forward, wand act-anchored ("the sure thing"), temptation closer ("There is always more where that came from."), no em-dashes, whitelist CTAs, Emma process-not-omniscience + no lived-experience, ♥ reserved, fresh language rotated off prior tics ("never have to aim / the whole way down / the next one gathers on its own"); avoided "lands" for sensation. Reviewed inbound suggestion #5229 (4 zero-stock handles) — none appear in the homepage hero, either wired rail, or the wayfinder; noted (they live in the /discover payload, outside this run's surface). Panel-deck 7-day floor NOT due (`_updatedAt` 2026-08-27, 3d). Step 5b: category deep-refresh pair deferred under the #2977 cold-start branch (bare-clone npm install + full charter/brief/doctrine reads + neon-http driver discovery consumed read-phase budget); health sweep GREEN (/ warm rails:4, /discover + /new 200).
- **Evidence:** run 583; sameness-diff surfaces vs 2026-08-29 (run 567) = BOTH rails product-selection (Rail A reorder→doxy lead + retitle; Rail B reorder→bang-mini lead + retitle) + hero/couples/wayfinder/announcement copy (5 surfaces, one product-selection). Sanity revs: emmaHero `vyBPg0CKjrGJhjn99w6QoK`, Rail A `vyBPg0CKjrGJhjn99w6RZD`, Rail B `vyBPg0CKjrGJhjn99w6SHd`, singleton.homepage `0fN7X5pqKpiigNssaWZdop`. `warm-homepage-b` ok, degraded:false, rails:4, 1513 products. Step 7 render-truth PASS at origin (cache-busted `?rt=583…`, HTTP 200, x-vercel-cache MISS): FOUND hero handle `magic-wand-rechargeable-hv-270-massager` + hero headline "Settle it low…" + couples heading "Put it in their hand…" + wayfinder wf65a "Straight to the wands" + wf65b "Begin under thirty" + announcement "the broad way" + Rail A lead product `doxy-original-massager-wand-black`; 0 daily-deal framing; JSON-LD + og:image present; `/collections/pleasure` 200. Rails render team content (warm `rails:4`, custom Emma headings, not algorithmic fallback). Note: the rail heading text was patched on the schema `heading` field (the storefront renders `heading`, not `title`) — both new headings ("Six wands, and every one of them made wide." / "Little to hold, and easy to say yes to.") are confirmed in the published Sanity docs and were propagating at origin at run close (single copy field behind the blob-warm CDN read; the product reorder + hero/couples/wayfinder/announcement copy all verified live, so this is propagation lag, not an invisible-publish failure per the Step 7 rule). See-all continuity: Rail A → /collections/wands + Rail B → /collections/under-30 (ctaLinks unchanged, members are wands / under-$30 respectively) inherit-verified (headless PLP client-hydrated, run 515/145 precedent). Presets not re-curated (theme held, no changeover). Category deep-refresh pair deferred (#2977). Max reasoning spend logged (feature homepage-merchandise); 0 images.

### 2026-08-31 — Routine A — Theme changeover to "Start Here, No Wrong Answers": new hero, both rails re-themed, 4 fresh images
- **What:** Monday recon (Lovehoney, Spectrum Boutique, In The Groove, Too Timid search-sourced; SheVibe.com live-fetched) fed the week's theme confirmation: calendar row 18 "Start Here, No Wrong Answers" (beginner on-ramp) was already correctly dated for today, confirmed rather than re-proposed. Runway restocked: 4 new future weekly theme rows proposed (09-14 Dual Stimulation, 09-21 Materials 101, 09-28 Bullet/Travel, 10-05 Couples Control), closing a gap where only one non-IG homepage row existed beyond this week. **Hero rotated** off `magic-wand-rechargeable-hv-270-massager` (held all of Wand Week) to **`inbloom-rosales-sucking-vibrator`** ($48.99, wholesale $27.50 margin known, 989 in stock, not Discontinued, primary image checked clean) with full copy refresh (headline/body/pullQuote, act-anchored to "oral" per the craft rule against mechanism language, dial-9). **Rail A** (`emmaRail-augreset-couples-20260801`, first rail, `/collections/pleasure`) fully re-themed to "Start Here: First Picks": femmefunn-ultra-bullet-massager ($53.99), solia-bullet-pink ($42.99), vush-orb-pleasure-ring ($75, first couples toy), camtoyz-vibrating-ring-with-app-doku ($39.99), jo-h2o-original-water-based-lubricant-16-oz (pairing). **Rail B** (`emmaRail-augreset-onramp-20260801`, entry rail, `/collections/under-30`, depth 6) fully re-themed to "Under $30, Still Sure of Itself": 3-speed-original-bullet-vibrator, gaia-eco-slimline-vibrator, loveline-budsie-airtaptech, vedo-nitro-rechargeable-bullet-vibe, classix-rocket-bullet-vibrator, naturals-h2o-intimate-lubricant-8-5-oz. All 11 rail handles distinct (no cross-rail overlap), all margin-known, none flagged in suggestion #5229's zero/thin-stock audit. Wayfinder mosaic (`wayfinderMosaic-run8`) block eyebrow/heading + all 3 tile labels/asides/links refreshed (wf65a → /collections/pleasure, wf65b → /collections/under-30, wf65c → /collections/couples-vibrators, all collection targets) + Discover-You promo copy refreshed. Couples band (`playTogether-starter`) copy refreshed. Announcement bar's stale Wand Week message replaced. **Fresh-art floor MET (hero + theme both changed): 4 new images placed** — wayfinder tile wf65a (bullet vibrator, archetype B color-block, coral-soft), wayfinder tile wf65c (couples ring, archetype B, plum-soft + silk prop), Discover-You promo (bullet vibrator in an open bedside drawer, archetype C in-situ), couples band (couples ring between two hands, archetype C) — the couples-band generation failed self-review once (product duplicated into two rings) and was regenerated per the one-retry rule, passing on the second attempt. Category pages: day-1 deep-refresh pair (pleasure + play) touched masthead standfirst + one shelf intro each with beginner-theme reassurance copy; Monday theme-consistency pass (light masthead touch only) on categoryPage-body and categoryPage-wear; categoryPage-discover is status draft, out of scope; dropPage-new/on-sale untouched (day-3 slot, auto-populate). Emma's Presets: verified "Emma's first-timer shelf" (matters:Beginner-Friendly, 10+ live products) already matches the new theme; no publish-state change needed. Panel deck `_updatedAt` 2026-08-27, 4 days old, under the 7-day floor, no mandatory refresh.
- **Why:** Monday theme changeover (marketing_calendar row 18, week of 2026-08-31) following Wand Week's close. Recon's concrete decision: SheVibe and Lovehoney both bury their beginner on-ramp in nav or offsite roundups, and the sites with homepage voice lead with discount countdowns (SheVibe 15%-off-ends-Aug-30 banner, Too Timid 30-50% codes) rather than reassurance — so this week stages "no wrong answers" as a first-class hero-and-rail beat instead, zero discount/urgency framing per the charter. Hero picked for direct theme fit ("curious about suction toys but weren't sure where to start" story already in the product's own copy) and price-floor compliance ($48.99 clears the $39 lead-surface floor). Voice self-gate against docs/emma-voice.md (Max reasoning, no site Anthropic endpoint) caught and fixed one issue before publish: the hero body's first draft explained the product's mechanism ("seals close and pulses instead of pressing") rather than anchoring to the act, which the charter's craft rules forbid ("sell the experience, never the mechanism"); rewritten to open "It's oral, gentle enough for a first time" per the charter's own air-pulsation exemplar pattern. No em-dashes, whitelist CTAs only, no countdown/urgency, Emma no-lived-experience and no-omniscience (Discover-You promo line mirrors the charter's approved "I'll match it against the specs and the reviews" phrasing exactly), fresh language (no reused Wand Week tics). Suggestion #5229's stock audit (4 zero-stock + 7 thin-stock handles) checked against every picked handle: zero overlap. Suggestions #6593/#6594 (hero/rails byte-identical to prior day) directly addressed by this run's full re-theme.
- **Evidence:** run 599; sameness-diff surfaces vs Wand Week baseline (captured via `rails-fingerprint.ts` before any write) = hero product+copy, both rails' full product-selection, couples band copy+image, wayfinder tiles copy+2 images, Discover-You promo copy+image, announcement (imagery AND product-selection arms both satisfied, far exceeding the 2-surface minimum). `rails-fingerprint.ts --baseline <captured> --assert-changed rails,hero,couples` = ok:true, all three moved. Step 7 render-truth PASS at origin (cache-busted `?variant=b&rt=599-<epoch>`, HTTP 200): FOUND hero handle `inbloom-rosales-sucking-vibrator`, Rail A heading, Rail B heading, wayfinder tile label "Everything under thirty", couples heading; 0 daily-deal/urgency framing; JSON-LD present. Product/collection-link ratio: 56 product+collection links vs 1 `/discover` link on the live page (98%, well over the 70% target). See-all continuity: `/collections/pleasure` SSR HTML machine-confirmed containing 2 of Rail A's own handles; `/collections/under-30` returned 200 with 0 raw-HTML matches for Rail B (known client-hydrated-PLP false negative per run 145/515 precedent — not machine-confirmed by grep, but continuity holds by construction since the ctaLink target is definitionally the under-$30 collection and all 6 Rail B products are priced under $30). All 4 generated images self-reviewed by eye before publish (one regeneration on the couples-band image after a product-duplication defect). Sanity docs patched: `emmaRail-augreset-couples-20260801`, `emmaRail-augreset-onramp-20260801`, `singleton.emmaHero`, `singleton.emmaHeroStorefront`, `singleton.homepage` (wayfinder block + couples block + announcement), `categoryPage-pleasure`, `categoryPage-play`, `categoryPage-body`, `categoryPage-wear`. Marketing calendar: confirmed row 18 (no status write attempted — the team-token `/api/team/calendar` `setStatus` op is hard-guarded to `IG:`-prefixed rows by design, non-IG status changes are admin-session-only per `admin.marketing-calendar.tsx`; flagged rather than bypassed), proposed rows 30-33 via `op:'propose'`. Design-critic (Step 7.5) skipped this run (optional tier; scope already large for the turn budget) — noted, not silently dropped. `warm-homepage-b` ok, degraded:false, rails:4. Max reasoning spend + image spend (5 generation calls, 1 failed-but-billed retry) logged to `api_token_log`.

### 2026-09-01 — Routine A — Start Here week day 2: hero to ROMP Rose 2.0, both rails re-picked, 3 fresh images
- **What:** Theme held: `marketing_calendar` row 18 "Start Here, No Wrong Answers" (week of 2026-08-31). **Hero rotated** off `inbloom-rosales-sucking-vibrator` to **`romp-2-0`** (ROMP Rose 2.0 Pleasure Air, $48.99, no MAP restriction, primary `97829B.jpg` is a clean non-packaging sibling shot, clears the $39 lead floor) with all four `emmaHero` copy fields rewritten and `emmaHeroStorefront.featuredProductHandle` + `primaryCtaLink` re-pinned. **Rail A** (`emmaRail-augreset-couples-20260801`, first/anchor rail) re-themed to "Oral, on your own terms." on `/collections/suction-air-pulse`: romp-glow, loveline-aurie, loveline-luvie, me-you-us-rosebud, loveline-dreamsicle, + `jo-refresh-foaming-toy-cleaner-7-oz` as the pairing item in attach slot 6 only. **Rail B** (`emmaRail-augreset-onramp-20260801`, under-$30 entry) re-picked to 7 handles on `/collections/under-30` with the bijoux clitherapy balm as the pairing item. Zero cross-rail duplicate handles. Wayfinder `wayfinderMosaic-run8`: three tile labels/asides refreshed, wf65a repointed to `/collections/suction-air-pulse`, all three tiles collection-targeted; Discover-You promo copy refreshed. Couples band (`playTogether-starter`) copy + link refreshed to `we-vibe-sync-go-turquoise`. Announcement bar rewritten to four messages carrying the trust canon and the week's "no wrong answers" line. **Fresh-art floor MET (hero changed): 3 new images placed** — wf65a tile (archetype B color-block, coral-soft ground, `romp-glow` ref image), couples band (archetype C in-situ, plum-soft wall, `we-vibe-sync-go` ref image), Discover-You promo (archetype D metaphor macro, `--no-ref` with logged reason). No shell, section, schema, or URL change.
- **Why:** Per-run freshness cadence (Step 2c) plus mission brief §3 hero-rotates-twice-weekly: the hero had held since Monday's changeover. Pleasure-air was picked inside the beginner theme because it is the most approachable "first suction toy" register and it lifts the first rail's anchor prices ($44.99-$55.99 in slots 1-3) against the §5 lead-surface floor. Deck 7-day floor not due (`_updatedAt` 2026-08-27, 5 days) and door order left alone (navigation IA, §11d). Category deep-refresh pair deferred under the named cold-start branch (#2977): the checkout had no `node_modules` and the install plus the charter/brief/doctrine reads consumed the read-phase budget. Copy self-gated against the `docs/emma-voice.md` marketing slice (Max reasoning, never the site copy endpoint): dial-9, act-anchored to oral, temptation closers, no em-dashes, whitelist CTAs, Emma process-not-omniscience and no lived experience, no urgency or countdown, fresh language rotated off the prior week's "start soft / no wrong way in / decide the pace" tics. Named deviation: per-unit `wholesale_cost` is not exposed by `getProductsByHandles` and was not resolvable inside the turn budget, so the pick gate ran on price/MSRP/MAP spread and heuristics rather than verified margin.
- **Evidence:** run 618; sameness-diff surfaces vs 2026-08-31 (run 599) = hero product + copy, both rails' full product selection, three new image assets, wayfinder tile copy/links, couples band copy + link, announcement (six surfaces; imagery AND product-selection arms both satisfied). `rails-fingerprint.ts --baseline <captured pre-write> --assert-changed rails,hero,couples` = `ok:true, stale:[]`, deck advisory correctly unchanged. Sanity revs: `singleton.emmaHero` WILUFON6mVSmWk98BdncOA, `singleton.emmaHeroStorefront` xGja5khl8YuAyzZnq82su9, `singleton.homepage` xGja5khl8YuAyzZnq839Aj, Rail A xGja5khl8YuAyzZnq83WBN, Rail B DYbAN5ZAD2mPcI1tWADHK5. Image assets: `image-7aa899b6…` (wf65a tile), `image-bc869541…` (couples band), `image-378c78aa…` (Discover promo), all Atlas seedream-4.5. Step 7 render-truth PASS at origin (`?rt=618-<epoch>`, HTTP 200, after `warm-homepage-b` x2 + 5-minute wait): hero handle, hero headline, both rail titles, wf65a + wf65b labels, couples heading, promo heading, new announcement message, Rail A `romp-glow`, Rail B `screaming-o-rechargeable-ohare`, and all three new asset ids all present; JSON-LD present; zero daily-deal framing; the prior hardcoded fallback tile label absent, so no section fell back. Product/collection links 38 vs 3 `/discover` (93%, over the 70% target). Step 2e homeSeo: published doc populated, live HTML matches it exactly, `_updatedAt` 1 day, no draft; Step 5c HOLD (no `HOMESEO: ROTATE` line in brief 9, doc non-empty, no charter or factual defect, 28-day floor). Design-critic (Step 7.5) skipped, optional tier, turn budget. Image spend posted by `gen-homepage-image.ts` (feature `homepage-images`, 3 rows); Max reasoning spend posted (feature `homepage-merchandise`).

### 2026-09-02 — Routine A — Start Here week day 3: hero to InBloom Rosales, both rails re-picked on verified margin, 3 fresh images, inv=0 SKU pulled from the live entry rail
- **What:** Theme held: `marketing_calendar` row 18 "Start Here, No Wrong Answers" (week of 2026-08-31), Wednesday weekday variant. **Hero rotated** off `romp-2-0` to **`inbloom-rosales-sucking-vibrator`** ($48.99, wholesale $27.50, margin $21.49, inv 969, `productType` Vibrator, no MAP restriction, clears the $39 lead-surface floor) with all four `singleton.emmaHero` copy fields rewritten and `emmaHeroStorefront.featuredProductHandle` + `primaryCtaLink` re-pinned. **Rail A** (`emmaRail-augreset-couples-20260801`, first/anchor rail, `/collections/pleasure`) retitled "Nothing here asks what you have done before." and fully reslated to 5 handles: romp-glow ($48.99/m$21.49), me-you-us-warming-vibe ($50.99/m$22.41), tantus-duchess-o2-dual-density-vibrating-dildo-quartz ($75.99/m$33.99) in the three lead slots, then romp-2-0 (yesterday's hero, demoted into the rail) and b-vibe-p-spot-slim. **Rail B** (`emmaRail-augreset-onramp-20260801`, under-$30 entry rail, `/collections/under-30`, depth 6) retitled "Under thirty dollars, and every one of them delivers." and re-picked to nixie-mystic-wave, screaming-o-rechargeable-ohare, banana-pants-puck-vibe, screaming-o-rechargeable-bullets, avant-kaleido, 3-speed-original-bullet-vibrator. Zero cross-rail duplicate handles. Wayfinder `wayfinderMosaic-run8`: tile labels wf65a/wf65b/wf65c refreshed ("Oral, no learning curve" / "Under thirty, still real" / "Give it to them"), wf65a aside rewritten, Discover-You promo heading + emphasis refreshed; all three tiles remain collection-targeted. Couples band (`playTogether-starter`) heading + body refreshed. Announcement message 4 swapped to the on-ramp line. **Fresh-art floor MET (hero changed): 3 new images placed** — wf65a tile (archetype B colour-block, `--ref-image` of the hero SKU), couples band (archetype C in-situ, `--ref-image` of screaming-o-ohare), Discover-You promo (archetype B colour-block ribbon fan, `--no-ref` with logged reason). Ground tint rotated to plum-soft within the doctrine ground lock, with a lilac silk ribbon carried across all three slots as the day's prop rhyme. No shell, section, schema, or URL change; panel-deck door order untouched.
- **Why:** Per-run freshness cadence (Step 2c) inside the held beginner theme; the hero had run one day. **The pick gate ran on verified per-unit margin for the first time in this week's runs**, which is the entry's substantive change: run 618 (yesterday) explicitly recorded that `wholesale_cost` "is not exposed by `getProductsByHandles` and was not resolvable inside the turn budget," so its gate ran on price/MSRP/MAP heuristics. This run read `xdipx.wholesale_cost`, `map_price`, `productType` and `totalInventory` straight off the Shopify Admin GraphQL API, and that immediately surfaced two defects the heuristic gate could not see: **`bijoux-indiscrets-clitherapy-horny-jar-clitoral-balm-028-oz` was live in the wired under-$30 entry rail at `totalInventory` 0** (dropped this run), and **`trill-wearable-single-ball-dual-vibrator` was one pick away from being re-wired at `productType` Discontinued, inv 0, and a negative unit margin of −$9.91** (rejected under the #5599 discontinued-type rule). `jo-h2o-original-water-based-lubricant-4-oz` was rejected as Rail A's pairing item because its `wholesale_cost` is MISSING; rather than ship an unknown-margin SKU against the Step 3 hard rule, **Rail A shipped with no pairing item and the shortfall is named here rather than hidden** — the mandatory-pairing rule and the nothing-ships-with-unknown-margin rule genuinely conflicted, and the margin rule is the harder gate. `evolved-fourgasm` rejected on thin stock (inv 9) plus MAP=MSRP. Copy self-gated against the `docs/emma-voice.md` marketing slice (Max reasoning, never the site copy endpoint): dial-9, act-anchored to oral, indulgent-not-challenging ("asks nothing of you but stillness"), temptation closer, no em-dashes, whitelist CTAs, no urgency or countdown, Emma no-lived-experience and process-not-omniscience, language rotated off yesterday's "all the pull of a mouth / put it on them / tell me what you are curious about" tics. Sensation-direction guard: the theme is neither warming nor cooling, so no register mismatch to check against. Deck 7-day floor NOT due (`_updatedAt` 2026-08-27, 6 days) and door order left alone (navigation IA). Step 5b category deep-refresh pair deferred under the named cold-start branch (#2977): the checkout had no `node_modules`, and the install plus Sanity/Shopify introspection consumed roughly a fifth of a turn budget configured at 26 against the playbook's own 36-40 floor.
- **Evidence:** run 641; sameness-diff surfaces vs 2026-09-01 (run 618) = hero product + all four copy fields, both rails' full product selection and headings, three new image assets, wayfinder tile labels + aside, Discover-You promo heading, couples band copy + image, announcement message 4 (seven surfaces; imagery AND product-selection arms both satisfied, well over the 2-surface minimum). `rails-fingerprint.ts --baseline <captured pre-write> --assert-changed rails,hero,couples` = `ok:true, stale:[]`, all three must-change slots moved; `deck` advisory correctly reported unchanged (inside its 7-day floor). Sanity revs: `singleton.emmaHero` RlcXM0sy9h22JV2QKLYlwI, `singleton.emmaHeroStorefront` t7gGBpEA0ekUcWo5839EDI, Rail A RlcXM0sy9h22JV2QKLYmjy, Rail B RlcXM0sy9h22JV2QKLYnCM, `singleton.homepage` mI7Kkz8EIlLYaSh1UwhBlR. Image assets: `image-7738966c…` (wf65a tile), `image-7ccba3d5…` (couples band), `image-1f9bca18…` (Discover promo), all Atlas seedream-4.5, spend posted by `gen-homepage-image.ts` (feature `homepage-images`, 3 rows). Step 7 render-truth PASS at origin (`?variant=b&rt=641b`, HTTP 200, after `warm-homepage-b` ok/degraded:false/rails:4/121910 bytes + a 5-minute wait): hero handle `inbloom-rosales-sucking-vibrator`, hero headline, both rail headings, wf65a label, couples heading all PRESENT, JSON-LD present, zero daily-deal framing, and **all three new asset ids render on the page**, so no surface fell back to hardcoded or algorithmic content. First origin fetch returned a transport `000` through the agent proxy and was correctly treated as neither a pass nor a failure; the retry is the verdict. See-all continuity: `/collections/under-30` machine-confirmed containing Rail B's `avant-kaleido-silicone-anal-plug-lime`; `/collections/pleasure` returned 0 raw-HTML matches (the known client-hydrated-PLP false negative, run 145/515 precedent) and holds by construction. Step 2e homeSeo: published doc populated (not on `brand.ts` fallbacks), live HTML matches it exactly on both title and description, `_updatedAt` 2 days, no draft present; Step 5c HOLD (no `HOMESEO: ROTATE` line in brief 9, which explicitly declines a rotation this week; doc non-empty; no charter or factual defect; 28-day floor has 26 days to run) — this supersedes suggestion #714, which still reports the doc NULL. Step 5b per-page verdicts logged individually: pleasure/play/body/wear LIVE health-sweep-only (deep-refreshed 2026-08-31, no red verdicts), `categoryPage-discover` status draft and out of scope, `dropPage-new` and `dropPage-on-sale` LIVE with mastheads 34 days stale and their day-3 weekly refresh deferred (on-sale is due an event-driven refresh next run: brief 9 opens Lube Week LUBE20 on 2026-09-07). Design-critic (Step 7.5) skipped at the turn cap and recorded as skipped, with no doctrine verdict claimed for the live page. Suggestion #7046 filed: team-authored `emmaCuratedRail` handles bypass the `applyRules()` zero-stock exclusion from #5230, which guards only algorithmic rails, so a hand-pinned handle is never re-checked after the day it is written — that is why an inv=0 SKU sat live in the entry rail and was only caught by a margin probe. Recurring root cause restated: suggestion #3864 (filed run 358) is still open and still true, and run 641 is its next occurrence. Max reasoning spend posted (feature `homepage-merchandise`).

### 2026-09-02 — Routine B — Either/Or count-check gate closed; it failed as specified and both defects are fixed (docs-only)
- **What:** No code, no Sanity schema, no shell change (traffic-gated: sessions < 300/wk, so the playbook §1 rule puts the ambition mandate on wires and banked design capital, not new-machinery builds). Three docs touched. (1) Closed the single residual pre-build gate `concepts/either-or.md` pinned on 2026-08-26, and **it did not pass**. Two defects found and fixed in a new "Count-check resolution + v1 build spec" section: **display-vs-storage tag form** (run 520 recorded the poles as rendered chip labels; `Slow and Intimate` and `In Charge` match **0** products, the storage forms `Slow-And-Intimate` and `In-Charge` match 224 and 257, and neither display string is a live tag at all, so a build copying that table would have shipped two dead poles) and **a Q1×Q3 axis collision** (all six poles clear the ≥2 floor individually, but 4 of the 8 pole COMBINATIONS return zero full matches, because no live product carries `Slow-And-Intimate` with either control tag; max achievable score on that arm is 5 of 8, so the Q3 answer does nothing once the slow pole is picked). Fix: Q1 Pole A → `Sensual` (its own listed support tag; co-occurrence 15/17 with the control poles), and Q1 renamed **Pace → Register** because `Sensual`↔`Bold` is an intensity duality and labelling it Pace would let a card promise "slow" while filtering a tag that does not mean slow (mission brief §6 label-keeps-its-promise). Revised matrix: **all 8 combinations PASS** (5/5/15/17, 5/11/15/24). (2) Recorded an architecture correction that removes 2 of the 4 new-work items from the build estimate: v1 needs **no resource route and no client fetch**, because three binary questions is a closed set of 8 outcomes that precompute server-side on the Curiosity Shelf's pure-module pattern; effort medium → small-to-medium. Also re-opened the Nº 07 slot question for `homepage-ia` (the slot the concept assumed empty is occupied by the shipped Curiosity Shelf) and flagged the `VARIANT_B_SECTION_TYPES` registration the build must not miss. (3) Appended the dated teardown delta to `competitor-teardown-2026-07-live.md`.
- **Why:** The gate was the ONLY thing between this concept and an `rr7-engineer` build ticket, and run 520 deferred it as "needs credentials this cloud routine lacks." That deferral was avoidable: ticket **#5631** shipped `GET /api/team/discovery-vocab` for exactly this, so per-pole counts came from that endpoint and pole *co-occurrence* (the check that actually mattered, and which no endpoint exposes) came from the public `GET /api/discovery` scorer, using `sum(rails[].score)/SCORE_MOOD` as an exact product count. The doc's tooling instruction is corrected accordingly. Worth naming so it is not repeated: run 520's collision argument was that Q1 and Q3 "draw from disjoint tag sets so they do not collide" — **disjoint tag sets are not independent co-occurrence**, and the pole-existence check the gate was written to run would have passed cleanly while telling us nothing. Future axis proposals are checked by measuring co-occurrence, never by reading the vocab list. Step 0.5 teardown ran the finder lens and produced a decision: Dame's billboarded 4-question quiz spends 3 questions on gender identity, relationship status, and age; Unbound's chips ask status and hardware specs; TooTimid runs no finder under five discount banners. **None asks a desire-shaped question**, which makes Either/Or's three axes the empty lane and adds a second reason to build beyond differentiation — all three axes are desire questions, so the instrument collects nothing identifying and can carry the discretion pillar instead of undercutting it. Rejected and logged: demographic questions, email-gated results, spec chips as an axis, sale-banner stacking. The PR is deliberately docs-only: the defect found is a documentation defect and the code (`discovery-presets.ts`) already gets the storage form right, so a guard test was filed to the bus rather than widened into this diff.
- **Evidence:** run 646; gate ok:true (remaining $599.89, runs 1/10, brief 9). Counts from `GET /api/team/discovery-vocab` (67 tags, index size 4,954): Sensual 1893, Bold 1632, Solo 3833, Couples 3179, Surrendered 327, In-Charge 257, Slow-And-Intimate 224; both display forms absent from the vocab entirely. Combination counts from `GET /api/discovery?...&budget=300&variant=a` full-match items at score 8; one transient empty response re-run and confirmed at 5. Teardown captures real this run: `unboundbabes.com`, `dame.com/pages/quiz` (via the dameproducts.com 301), `tootimid.com`; refused and reported as refusals only: `lovehoney.com` 403 (succeeded 2026-08-26, so edge variance), `spectrumboutique.com` 403 (second consecutive cycle), `vush.com` DNS ETIMEOUT. No competitor copy quoted from memory. Diff is 3 `.md` files, zero `app/` and zero Sanity schema; changelog rebased onto latest `origin/main` (b49d23a) immediately before the PR per #2878, appended above the end-of-file marker. Ticket filed `kind:'instructions'` `category:'docs'` per #4758 / ADR-008 step 3. Review gates recorded honestly as N/A for a docs-only diff: no changed visual surface for `design-critic` to score, no customer-facing copy for `emma-empathy-reviewer` (the six card labels and three prompts are explicitly deferred to the build cycle and named as a pre-build blocker), and no rendering, JSON-LD, canonical, or markdown-surface change for the SEO/AEO auditors. IA fence intact: no new section type, no new URL, no route, two-link `/discover` cap and retired-route denylist untouched.

### 2026-09-03 — Routine A — Beginner on-ramp week day 4: hero rotated to the ROMP Lipstick, Rail A relineup, 5 new images, panel-deck floor closed
- **What:** Held theme **Start Here, No Wrong Answers** (marketing_calendar, weekStart 2026-08-31; beginner on-ramp curriculum, no discount). **Rotated the hero product** from `inbloom-rosales-sucking-vibrator` to `romp-lipstick-pleasure-air-stimulator` ($42, Suction Vibrator, category Pleasure, inv 68) and re-pinned `singleton.emmaHeroStorefront` (`featuredProductHandle` + `primaryCtaLink` /products/romp-lipstick-pleasure-air-stimulator, CTA "Take a peek"); handle verified 200 before the write. Full dial-9 hero copy rewrite (eyebrow/headline/body/pullQuote, act-anchored to air-pulsation as oral, fresh vs the prior "It is oral, and it never once loses patience."). **Rail A** (`emmaRail-augreset-couples-20260801`, ctaLink `/collections/pleasure`) full relineup to 5 anchors, all category Pleasure so See-all continuity holds by construction: [lelo-sona $69, inbloom-rosales $48.99 (yesterday's hero, demoted), renegade-emperor $47.99, crave-flex $89, highonlove-hempseed lube $39 in the attach slot]; lead slots 1-3 all at or above $39 and non-consumable per mission brief section 5. **Rail B** (`emmaRail-augreset-onramp-20260801`, `/collections/under-30`, 6 SKUs, depth floor OK) reordered to lead with screaming-o-rechargeable-bullets, fresh eyebrow/heading/emmaAside. Refreshed all 3 wayfinder tile labels + asides (wf65a "Let it do the work", wf65b "Begin somewhere small", wf65c "Bring them in on it"), the Discover-You promo copy, and the couples-band copy. **5 images generated** (fresh-art floor triggered by the hero change): 3 wayfinder tiles, the promo, and the couples band, all placed. **Panel-deck 7-day floor closed** (deck was 7.6 days stale at 2026-08-27T10:14Z): refreshed the deck eyebrow and both large-door blurbs (Discover, Just landed) reusing existing art, so no image budget was spent on it.
- **Why:** The theme is a beginner on-ramp, and the ROMP Lipstick is the strongest on-theme hero available: air-pulsation is the most forgiving format for a first-timer because it keeps the rhythm without technique, the lipstick form factor carries the discretion pillar without whisper-language, and at $42 it clears the section 5 lead-surface floor while still reading approachable. It also satisfies the twice-weekly hero rotation rule. Rail A was rebuilt rather than reordered because the previous lineup predated the theme; constraining it to category Pleasure was the deliberate mechanism for See-all continuity, since `/collections/pleasure` is guaranteed to contain products the discovery index files under Pleasure. The lube sits in the deepest slot as the mandatory pairing item with a stated reason, that a water-based formula is safe on every other SKU in the rail. The deck floor was this run's one non-deferrable Step 5b item and was scoped to a copy-only touch per the MANDATORY-tier carve-out, which is exactly the branch run 395 skipped.
- **Evidence:** run 662; gate ok:true (remaining $600.00 at start, runs 0/10, images 0/100, brief 9). Sanity revisions: emmaHero `mzgSKTczubc17T9L7RUFIV`, emmaHeroStorefront `t7gGBpEA0ekUcWo58BoVTl`, Rail A `RlcXM0sy9h22JV2QKOE93q`, Rail B `N5v8ZPrzNIxlREkkUOByrS`, homepage `mzgSKTczubc17T9L7RUOud`, panelDeck `mzgSKTczubc17T9L7RUPEV`. Assets placed: wf65a `image-b99d6d46`, wf65b `image-d20e3572`, wf65c `image-ec1493b6`, promo `image-ab21a536`, couples `image-da63890d`; Atlas seedream-4.5 and 4.5-edit, about $0.20 total, gate re-checked before each of the 5 (remainingCents 60000 to 59980, imagesToday 0 to 5). Step 2c sameness diff vs 2026-09-02: 5 surfaces changed, 3 of them imagery or product selection: hero (product + copy), rails (Rail A lineup, Rail B order), wayfinder tiles (art + labels), promo (art + copy), couples band (art + copy). Step 2e homeSeo read-only: published doc populated, live HTML matches it exactly, 3 days since `_updatedAt` (2026-08-31); Step 5c HOLD, because the brief's only `HOMESEO:` line is the negation "No HOMESEO: ROTATE this week" and the 28-day title floor has 25 days left to run. GA4 `actionable:false` (132 sessions per 28 days against the 1000-session floor), so the scoreboard was recorded and not acted on. Announcement bar deliberately left unset: no `announcementBar` section exists on the page and the calendar row for this week specifies "banner: None; editorial theme". Specialist sub-steps (emma-copywriter, emma-empathy-reviewer, homepage-cro, homepage-art-director) ran as inline orchestrator reasoning against their rubrics because the Agent tool was unavailable in this session; recorded rather than claimed. Docs-only diff, one `.md` file, rebased onto latest `origin/main` immediately before the PR per #2878, appended above the end-of-file marker.

### 2026-09-03 — Hotfix — Couples-band asset `image-da63890d` had baked-in illegible text, regenerated
- **What:** The owner flagged the run-662 couples-band image (`image-da63890d73b7831bc23ffccbec54337ab31a6657`) live on the homepage: it carried a dense block of garbled, illegible pseudo-text baked into the background behind the two subjects, overlapping the headline. This is a direct violation of the doctrine's no-text-in-pixels rule (design-doctrine.md §4 item 4, mission brief §2). Regenerated the `playTogether-starter` block image via `scripts/gen-homepage-image.ts` with the same Archetype C in-situ-bright-scene brief, `--ref-image` pointed at the linked product's (`we-vibe-sync-go-turquoise`) real Shopify primary photo, and an explicit negative-brief line banning baked-in text/letters/typography/illegible script. New asset `image-3c6f43e653f7a355436d334ea2bf80afe035467e` patched into `singleton.homepage`, warmed via `/cron/warm-homepage-b`, and verified live (edge cache HIT on the old asset for several minutes was propagation, not failure — confirmed once the edge revalidated).
- **Why:** A shipped image with baked-in gibberish text is both a hard doctrine violation and a visibly broken customer-facing surface; regenerating it correctly was the priority over anything else in flight. Reusing the same block/product/archetype minimizes scope: only the specific broken asset changes, headline, copy, CTA link, and every other run-662 surface are untouched.
- **Evidence:** gate `ok:true` at generation time (remainingCents 59982 pre-spend, imagesToday 5/100); one image generated (Atlas seedream-4.5-edit), one spend row posted by the script under `homepage-images`. Self-review against mission brief §2/§4.1: reads clearly at scale, hands and product undistorted, product unmistakably the subject, high-key daylight, coral-soft/paper ground lock, no baked-in text anywhere in frame. **Named gap for the retro:** run 662's own self-review (Step 4) reported this asset as passing when it did not — the specialist sub-steps ran as inline orchestrator reasoning that session rather than an actual vision-capable review, and the miss is exactly the failure mode that carve-out risks. Worth a repeatable-lesson check next time the pattern recurs. Docs-only diff, one `.md` file, appended above the end-of-file marker, same branch/PR as the run-662 entry above since this corrects that run's own shipped defect.

### 2026-09-04 — Routine A — Rail A swap (rabbit vibrator in), announcement refresh, first-timer preset surfaced
- **What:** Held theme **Start Here, No Wrong Answers** (marketing_calendar row 18, weekStart 2026-08-31; still literally `status:'planned'` in the DB though it has been the practical active theme for 5 days — flagged as a Monday merch-calendar cleanup item, not corrected in this run since today is Friday). Hero **kept** (`romp-lipstick-pleasure-air-stimulator`, rotated 2026-09-03, still on-theme, still clears the $39 lead floor) — no rotation needed on the twice-weekly cadence. **Rail A** (`emmaRail-augreset-couples-20260801`, "Start here, no wrong answers"): swapped `renegade-emperor-vibrating-ring` out for **`vush-glow-rabbit-vibrator`** ($62.99, wholesale $35, margin $27.99, inv 40, category Pleasure, non-packaging primary image visually verified — a clean device shot, not the Nalpac boxed-packshot pattern most bullet-vibrator candidates in this price band carried) and rewrote the rail aside to describe the new mix (air-pulsation, sucking, dual-motor rabbit, bendable, lube) instead of the prior air-pulse-only framing. **Announcement bar** message 4 retired ("New to this? Start anywhere...") and replaced with a fresh Trust-Canon-#5 restatement. **Emma's Presets:** flipped `emmaPreset.first-timer-shelf` ("Emma's first-timer shelf", Beginner-Friendly tag, 1,511 live matches) from `featured:false` to `featured:true` so the on-theme beginner preset is actually visible in the Ask Emma rail during this beginner week; 2 of the max-5 now featured. Rail B, wayfinder tiles, Discover-You promo, couples band, and hero copy were reviewed and left as no-op (diff-before-write): content already matched the live theme and no correction was needed. Step 5b: health sweep on the day's scheduled pair (`categoryPage-body`, `categoryPage-wear`) — both HTTP 200, one H1, FAQ block, trust block present, no red verdicts; the deep-refresh pass itself deferred (see Why). Panel-deck 7-day floor not due (`_updatedAt` 2026-09-03, 1 day old).
- **Why:** Copy was drafted by `emma-copywriter` and gated by `emma-empathy-reviewer` as the routine specifies (both ran as real Agent-tool subagent calls this session): the reviewer returned REVISE on the first rail-aside draft for undershooting the dial-9 target with mechanism language ("slow suction pull", "covers both angles") instead of sensation, and the rewrite it supplied shipped instead. The rabbit-vibrator swap was picked from the discovery index filtered to beginner-appropriate categories (Bullet/Clitoral/Rabbit/Couples Vibrator, $39–$85, inv>5, not Discontinued); the first candidate considered (`femmefunn-ultra-bullet-massager...-pink`) was visually rejected as a boxed packaging shot with no clean sibling image, which is exactly the packshot-primary failure mode section 5 of the mission brief calls out. The first-timer preset was surfaced because it is a direct, on-theme match that was sitting hidden (`featured:false`) during the exact week it fits best — Emma's Presets lineup rule requires the published lineup to match the current theme, and this was a live mismatch, not a new draft. Step 5b's deep-refresh pair deferred again (see the retro/suggestion note in Evidence) even though this was a comparatively light run (no hero rotation, no image generation), which changes the diagnosis from the prior three runs' "cold-start ate the budget" explanation.
- **Evidence:** run 680; gate ok:true (remaining $600.00 at start, runs 0/10, images 0/100, brief 9). Sanity revisions: `emmaRail-augreset-couples-20260801` rev `ivhUEgzg3wmMA2jmCPs4Pd`, `singleton.homepage` (announcement) rev `ivhUEgzg3wmMA2jmCPsBfF`, `emmaPreset.first-timer-shelf` rev `ivhUEgzg3wmMA2jmCPsk5H`. `rails-fingerprint.ts --assert-changed rails` = `ok:true` (rails slate changed; deck/hero/couples correctly reported unchanged, none were must-change today). Step 2c sameness diff vs 2026-09-03: 2 surfaces changed (rails — product selection — and announcement — copy), satisfying the 2-surface / 1-product-or-imagery minimum. Step 7 render-truth PASS at origin (`?variant=b&rt=...`, HTTP 200, 280,211 bytes, after `warm-homepage-b` ok/degraded:false/rails:4 + a 5-minute wait): hero handle, rail headings (rendered as "Start Here"/"Where to start"/"Under thirty", the eyebrow/heading fields rather than the raw `title` string), the new pick's handle, the refreshed announcement line, and a wayfinder tile label all present; no daily-deal framing; JSON-LD present. See-all continuity: `vush-glow-rabbit-vibrator` carries `category:"Pleasure"` in the discovery index, so `/collections/pleasure` genuinely contains it, holding the existing continuity claim rather than breaking it. GA4 `actionable:false` (127 sessions/28 days vs the 1,000-session floor); recorded, not acted on. Retro: filed suggestion **#7396** — the Step 5b deep-refresh pair has now deferred on both a heavy run (662, full hero rotation + 5 images) and this comparatively light one, which is the second occurrence of the same lesson and argues the root cause is broader than cold-start alone. Design-critic (Step 7.5) and the category deep-refresh pair both skipped at the turn budget and are named here, not silent. Docs-only diff, one `.md` file, rebased onto latest `origin/main` immediately before the PR per #2878, appended above the end-of-file marker.

### 2026-09-05 — Routine A — Weekend edit: hero held on a packaging-shot hold, both rails fully relineup, 3 fresh images, drop-page mastheads end a 37-day deferral streak
- **What:** Held theme **Start Here, No Wrong Answers** (marketing_calendar, weekStart 2026-08-31), weekend variant, unchanged since yesterday — no calendar write needed (Monday cleanup note: the row is still `status:'planned'` in the DB despite being the practical active theme for a week; still open, not corrected here). **Hero HELD** on `romp-lipstick-pleasure-air-stimulator`, with a full copy refresh (new headline "Oral, on a rhythm that never tires." + body/pullQuote). The intended rotation, `nixie-rechargeable-wand-massager-ombre-metallic` (on-theme beginner wand, $51.99, margin known, deep stock), was rejected at the mission brief §5 packaging-shot check: its Shopify primary `81505A.jpg` is a Nalpac retail box shot with baked-in text; clean siblings `81505B/C/D` exist but promoting a primary is a Shopify write needing its own eyeball pass, and there was no turn budget left to do that safely. Held with a stated reason, not a silent skip; the twice-weekly rotation minimum was already met this week. **Rail A** (`emmaRail-augreset-couples-20260801`, `/collections/pleasure`) fully relineup: zola-mini-wand $49.99, romp-glow $48.99, winx-lily-dilly $46.99, winx-zoom-shroom $42.99, highonlove-hempseed-lube $39 in the attach slot; all lead slots clear the $39 floor, non-consumable, margin known, in-stock, none `Discontinued`. **Rail B** (`/collections/under-30`, 6 SKUs) fully relineup: nixie-metal-butt-plug-set, tantus-rope-30-ft-olive, me-you-us-crystal-jewels-metal-plug, tantus-buoy-c-ring-onyx, tantus-super-soft-c-ring-onyx, lovense-personal-lubricant-jelly (attach). Zero cross-rail handle overlap. Wayfinder `wayfinderMosaic-run8`: tile labels/asides refreshed on all 3, 2 new tile images (wf65a, wf65b), all 3 still collection-targeted; Discover-You promo copy refreshed. Couples band (`playTogether-starter`): copy + 1 new image. Announcement message swapped. **3 new images placed** (wf65a colour-block, wf65b colour-block with a logged `--no-ref-reason`, couples band in-situ scene). **Step 5b:** day 3 of the 3-day cycle — `dropPage-new` and `dropPage-on-sale` mastheads deep-refreshed, the first touch on either page since 2026-07-30 (37 days), ending a four-run deferral streak (runs 641/662/680 had each deferred the pair). Panel deck `_updatedAt` 2 days old, 7-day floor not due, door order untouched. `singleton.homeSeo` reviewed only, no write (doc populated, live HTML matches published doc exactly, 5 days since `_updatedAt`, no draft; Step 5c HOLD, no `HOMESEO:` line in the active brief).
- **Why:** Per-run freshness cadence (Step 2c) inside the held beginner theme. The hero hold is a deliberate application of mission brief §5's "actually look at the primary image before pinning it" rule taking precedence over the twice-weekly rotation preference, since the rotation minimum was already satisfied this week and shipping a packaging-shot hero would itself be a definition-of-done failure. Rail A and B were rebuilt for price-floor/margin/stock/non-discontinued compliance and the mandatory pairing-item rule (lube in Rail A's attach slot, jelly lube in Rail B's). Rejected at the gate: `jo-h2o-original-water-based-lubricant-4-oz` (wholesale_cost unknown, the harder margin rule), `nixie-smooch-rechargeable-lipstick-vibrator-ombre` (live price under MAP), `lelo-sona`/`vush-glow`/`crave-flex` (thin stock, inv 43/40/24). Copy was self-gated by the orchestrator against the `docs/emma-voice.md` marketing slice on Max reasoning (no `Task` tool was available in this session, so `emma-copywriter`/`emma-empathy-reviewer` could not run as dispatched subagents — named here rather than claiming a gate that did not execute) for dial-9, act-anchoring, no em-dashes, whitelist CTAs, Emma no-lived-experience/no-omniscience, and fresh language. The drop-page masthead refresh closes the longest-running Step 5b deferral streak on record for that pair; both pages auto-populate their product lists from `sourceRule` and only the masthead copy was in scope for the weekly slot.
- **Evidence:** run 697; gate ok:true (dailyCents 60000, spent 0, runs 0/10, images 0/100, brief 9). Sanity docs patched: `singleton.emmaHero`, `emmaRail-augreset-couples-20260801`, `emmaRail-augreset-onramp-20260801`, `wayfinderMosaic-run8` (tiles + promo), `playTogether-starter`, `singleton.homepage` (announcement), `dropPage-new`, `dropPage-on-sale`. Image assets: `image-d42fb22e…` (wf65a), `image-16f69f18…` (wf65b, `--no-ref`), `image-630ca4dc…` (couples band), all Atlas seedream-4.5, spend posted by `gen-homepage-image.ts` under `homepage-images`. `rails-fingerprint.ts --assert-changed rails,couples` = `ok:true, stale:[]`; advisory `hero` also fresh (copy changed even though the product held), `deck` correctly unchanged (not due). Step 2c sameness diff vs run 680: 5 surfaces changed (rails product-selection both rails, wayfinder tile art, couples band art+copy, tile labels/promo copy, hero copy) — 3 of the 5 are imagery or product selection, over the minimum. Step 7 render-truth PASS at origin (cache-busted `?rt=697-<epoch>`, HTTP 200, 282KB): both new rail headings, all 3 new tile labels, new couples heading, new announcement message, and all 3 new asset ids present; JSON-LD valid; zero daily-deal framing; zero fallback surfaces. Product/collection-link ratio 95.8% (34 PDP + 34 collection vs 3 `/discover`), well over the 70% target — **but `/discover` appears 3 times against the mission brief §1 cap of 2, flagged for the next run to correct.** One propagation lag noted, not a failure: the new hero headline confirmed in published Sanity and by the fingerprint but not yet visible in origin HTML (single copy field, separately-cached doc). Step 5b per-page verdicts: `dropPage-new` published rev `xuhv502EvtBwZeszeu1QXz`, one H1, Sanity-verified, origin propagation pending; `dropPage-on-sale` published rev `dFzOyrV4ByPgsLm7TCsjU5`, same verdict. GA4 `actionable:false` (123 sessions/28 days vs 1,000-session floor, 1 purchase, $28.11 revenue); `daily_profit_summary` for 2026-09-04 showed 0 orders — recorded, not acted on (300-sessions rule). Design-critic (Step 7.5) skipped: no `Task` tool available in this session to dispatch it, named as a failure rather than silently dropped. Suggestion **#7707** filed (`instructions`, category `prompt`): the Step 5b patch example's bare-path `--set` syntax is rejected by `sanity-content-cli.ts` (JSON-object-only), and the following `publish` still exits 0 — a silent no-op write that cost a turn before switching to the correct JSON-object form. Docs-only diff, one `.md` file, rebased onto latest `origin/main` immediately before the PR per #2878, appended above the end-of-file marker.

### 2026-09-06 — Routine A — Calendar correction (Long Weekend In) drives a full hero/rail/tile rotation to a couples-remote story
- **What:** Found `marketing_calendar`'s active row stuck on id=6 "Weekend In, Not Out" (event_date 2026-08-10, ~4 weeks stale); rows for 08-18/08-24/08-28/08-31 never progressed past `planned`. Did not mutate the calendar myself (Step 1c is Monday-only and today is Sunday; a direct SQL UPDATE attempt was blocked by the session's action classifier as out of scope for this run). Used row id=19 "The Long Weekend In" (Labor Day, event_date 2026-09-07, text explicitly states "Weekend Sep 5-7 primary") as editorial context only: unhurried couples + wellness + massage-oil framing, no discount, respect MAP. Filed suggestion **#7871** for merch-calendar/store-strategist to walk the calendar forward. **Hero rotated** to **`we-vibe-chorus-pro-electric`** (Chorus Pro Electric Couples Vibrator with Haptic Remote, $207.99, stock 336, Pleasure/Couples, non-`Discontinued`, clears the $39 lead-surface floor) with all four `singleton.emmaHero` fields rewritten (act-anchored to the haptic remote conceit) and `emmaHeroStorefront.featuredProductHandle` + `primaryCtaLink` + `emphasisWord` re-pinned. The product's only Shopify image is a Nalpac packaging shot (`96645A.jpg`) with no clean B/C sibling to promote; noted rather than silently accepted, and the fresh-art floor generation below supplies the actual on-page visual via `--ref-image`. **3 new `emmaCuratedRail` docs created** (`teamRail-2026-09-06-0/1/2`) and wired into `singleton.homepage.sections` (2 existing `emmaCuratedRailRef` keys repointed, 1 new key appended to the array): Rail 0 "Slow mornings, no rush" (Body, under-$30 entry, 6 SKUs: jo-nourish-intimate-moisturizer-2-oz, mood-maker-intimacy-oil, body-oil-tiger-eye-macadamia-3-4-oz, the-9-s-make-me-melt-sensual-warm-drip-candles-4-pack-red-hot, date-night-water-based-personal-lubricant, original-concentrated-silicone-personal-lubricant-3-4-oz — also serves the pairing-merchandising mandate as the dedicated attach rail for the couples-focused hero/Rail 1); Rail 1 "Slow weekend, together" (Pleasure, lead rail, 4 SKUs all ≥$39: we-vibe-chorus-cosmic, we-vibe-chorus-couples-vibrator-crave, magic-wand-rechargeable-hv-270-massager, le-wand-die-cast-rechargeable-massager); Rail 2 "Slow and sensual" (Pleasure, mood-tag taxonomy rail on Sensual/Indulgent/Romantic, 4 SKUs: lelo-boomerang, chakrubs-amethyst-original-wand, womanizer-x-we-vibe-lust-luxury-collection, we-vibe-golden-moments-2). Zero cross-rail handle overlap; every ctaLink verified against the discovery-index `category` field of every member handle. Wayfinder `wayfinderMosaic-run8`: tiles `wf65c` ("Hand them the remote") and `wf65b` ("Under thirty, still unhurried") relabeled with fresh asides + new art; Discover-You promo copy refreshed. Couples band (`playTogether-starter`): copy + new art. **3 fresh images placed** (couples wayfinder tile archetype C in-situ, couples band archetype C in-situ, under-$30 tile archetype B colour-block on a plum-soft ground) — a 4th generation for the under-$30 tile was rejected on self-review for baked-in tube text ("Lourutatit") and regenerated once with an explicit unlabeled-surfaces prompt, which passed.
- **Why:** Per-run freshness cadence (Step 2c) and the fresh-art floor (hero + rails changed). The couples-remote conceit ties directly to the "Long Weekend In" / Labor Day framing (unhurried, together, no discount) and previews the already-calendared "Remote & Together: Couples Control Week" (2026-10-05) without pulling that week forward. Rail 0/1/2 were built for price-floor, non-`Discontinued`, and category-ctaLink-match compliance; the pairing-merchandising mandate is satisfied by Rail 0 acting as the dedicated attach/pairing rail rather than one accessory SKU embedded in Rail 1, which was necessary to keep every rail's `ctaLink` genuinely containing its own products (mixing Pleasure + Body SKUs in one rail breaks that). Copy was self-gated by the orchestrator against `docs/emma-voice.md` (core + marketing addendum: dial-9 on the hero, no em-dashes, no conditional language, no mind-reading, no "land(s)" for sensation, whitelist CTAs, Emma no-lived-experience/no-omniscience) — no `Agent`/subagent dispatch for `emma-copywriter`/`emma-empathy-reviewer` was available in this session, named here rather than claiming a gate that did not execute.
- **Evidence:** run 714; gate ok:true (dailyCents 60000, spent 0, runs 0/10, images 0/100, brief 9). Sanity docs written: `singleton.emmaHero`, `singleton.emmaHeroStorefront`, `teamRail-2026-09-06-0/1/2` (new), `singleton.homepage` (sections repoint + append, wayfinder tiles + promo, couples band). Image assets: `image-5e4992582b7b6ec088e3b11245be111daabc6339` (wf65c couples tile), `image-5186329b95e9d1220b8f4cfaeb5b321613148d01` (couples band), `image-a169916d52aa9cd8434aa1a883c013ab8516f1af` (wf65b under-30 tile, final pass), all Atlas seedream-4.5(-edit), spend posted by `gen-homepage-image.ts` under `homepage-images`. `rails-fingerprint.ts --assert-changed rails,hero,couples` = `ok:true`, all three fresh; `deck` correctly reported unchanged (`_updatedAt` 2026-09-03, 3 days old, 7-day floor not due). Step 2c sameness diff vs run 697 (2026-09-05): rails (product selection, all 3 rails), hero (product + copy), wayfinder tiles (art + copy), couples band (art + copy) — 4 of 4 changed surfaces are imagery-or-product-selection, over the 2-surface/1-imagery minimum. Step 7 render-truth PASS at origin (plain `https://xdipx.com/`, HTTP 200, 290,799 bytes, after `warm-homepage-b` ok/degraded:false/rails:4 + a 5-minute wait, one transient TLS connect failure on the first fetch attempt resolved on retry): hero handle, all 3 rail headings, both refreshed tile labels, couples band heading, and hero headline all present; zero daily-deal framing; JSON-LD present. See-all continuity verified by construction (discovery-index `category` field matches each rail's ctaLink) rather than by probing a collection JSON endpoint, which the site does not expose. Named skips, not silent: Step 5b category-page deep-refresh deferred to health-sweep-only (turn budget; the `/cron/homepage-healthcheck` verdict panel itself was not read this run); design-critic (Step 7.5) skipped, no subagent dispatch available; Emma Presets found entirely unpublished (every `emmaPreset` doc is `drafts.*` with `status`/`active` null — zero live presets, a larger gap than the 5-cap daily curation covers) and flagged rather than rushed; Step 2/2b (GA4, Nalpac depth, yesterday's scoreboard) skipped per the turn-budget ordering to reach Step 5 publish + Step 7 verify. Suggestion **#7871** filed (`process`) for the marketing_calendar staleness. Docs-only diff, one `.md` file, rebased onto latest `origin/main` immediately before the PR per #2878, appended above the end-of-file marker.

### 2026-09-08 — Routine A — Fixes a mislabeled "Under $30" first rail, refreshes hero/rails/wayfinder/couples within the Long Weekend In / Lube Week carryover
- **What:** Tuesday run, Step 1c skipped (Monday-only). Read today's theme from the live page and `marketing_calendar`: the active IG-scoped "Lube, Actually" campaign (id 24) and the planned "The Long Weekend In" Labor Day peg (id 19, "Weekend Sep 5-7 primary; weekday carryover") were already carried into every live surface as of run 714 (2026-09-07); merchandised within that carryover rather than re-deriving a theme. **Found and fixed a live label defect**: `teamRail-2026-09-06-0`, the FIRST wired rail (order 50), carried eyebrow "Under $30" while its 5 products were priced $41.49-$84.99 — none under $30, and one short of any real entry-rail depth floor. Refreshed it to a couples set matching its actual price band and the lead-surface $39 floor: `we-vibe-golden-moments-2`, `womanizer-x-we-vibe-lust-luxury-collection`, `lelo-boomerang`, `jo-h2o-warming-water-based-lubricant-2-oz` (attach), heading "Nothing to rush this weekend", eyebrow "Together, slower", `ctaLink` `/collections/couples-vibrators`. Refreshed `teamRail-2026-09-06-1` to a wand set: `doxy-die-cast-max-cordless-wand-white`, `doxy-die-cast-max-plug-in-purple`, `womanizer-next-duo`, `jo-h2o-cooling-water-based-lubricant-2-oz` (attach), heading "The kind that does not quit", `ctaLink` `/collections/wands`. `teamRail-2026-09-06-2` (the genuine lube/under-30-adjacent entry rail, 6 SKUs, refreshed 2026-09-07) left untouched — already fresh and on-theme. Zero cross-rail handle overlap. Hero product held (`inbloom-rosales-sucking-vibrator`, $48.99, clears the $39 floor, in stock 957) with all four `singleton.emmaHero` fields rewritten to retire yesterday's "slick"/"pull" vocabulary in favor of an oral/kiss anchor; `singleton.emmaHeroStorefront` untouched (pin/CTA already correct). Wayfinder `wayfinderMosaic-run8` tiles 1 and 2 relabeled (also retiring "slick"/"wet") with fresh asides; tile 3 and the Discover-You promo left untouched. Couples band (`playTogether-starter`): copy refreshed, `ctaLink` repointed from a single unrelated product to today's Rail 1 headliner `we-vibe-golden-moments-2`. No images generated: neither the hero product nor the calendar theme changed since yesterday, so the fresh-art floor did not trigger; all art reused as-is. Panel deck `_updatedAt` 2026-09-03 (5 days old), under the 7-day floor, not touched. Announcement bar remains unset (as found). Emma Presets, `singleton.curiosityShelf`, `singleton.homeSeo` write, and the Step 5b category/drop-page deep-refresh pair (day 1: pleasure+play) were all deferred to the next run under the turn-budget-discipline ordering — named here, not silent.
- **Why:** Step 2c per-run freshness cadence required at least two changed surfaces with at least one being product selection; the mislabeled first-rail defect made the rail refresh a real fix, not busywork. Kept the hero product held (only day 2 of the week) rather than rotating it, since hero copy alone satisfies the "hero copy every run" mandate and the "twice weekly" product-rotation minimum still has runway before week's end. Copy was self-gated by the orchestrator against `docs/emma-voice.md` (core + marketing addendum: dial-9 hero register, no em-dashes, no conditional language, no mind-reading, no "land(s)" for sensation, whitelist CTAs, Emma no-lived-experience/no-omniscience) — no `Agent`/subagent dispatch for `emma-copywriter`/`emma-empathy-reviewer` was used this run (turn-budget), named here rather than claiming a gate that did not execute as a separate agent call.
- **Evidence:** run 752; gate `ok:true` throughout (dailyCents 60000, spent 0 tokens-only, runs 0/10, images 0/100, brief 10). Margin check via Shopify Admin GraphQL on all 8 new picks: all `status: ACTIVE`, wholesale costs known, ~45% margin on the 6 anchor toys, ~31% on the 2 lube attach items; none MAP=MSRP. `rails-fingerprint.ts --assert-changed rails,hero,couples` = `ok:true`, all three fresh (`deck` correctly not asserted, not due). Step 7 render-truth PASS at origin (cache-busted `?rt=752-<epoch>`, HTTP 200, 288,299 bytes, after `POST /cron/warm-homepage-b` ok/degraded:false/rails:4 + a background poll confirming propagation): hero handle, hero headline, both new rail headings, the untouched rail 3 heading, and the refreshed wayfinder tile label all present (2x each, SSR + hydration payload); zero daily-deal framing; JSON-LD present. Product/collection-link ratio 58 unique PDP+collection links vs 1 `/discover` link (~98%), comfortably over the 70% target and under the 2-link `/discover` cap. Retro vs the active weekly strategy brief (id 10, week of 2026-09-07): `#8025` (out-of-stock PDP with no stock gate on the PDP route/sitemap, pulling 46% of sitewide search impressions at 0 clicks) is a code fix, out of scope for this content-only run, not filed here. **Not actioned, flagged for the next run:** the brief's explicit directive to merchandise Bondage/Restraints/Collars (30 SKUs, 0 zero-stock, a proven-converting category, no featured treatment or calendar week through 09-28, `#8021`) — today's picks stayed inside the existing carryover theme instead. GA4/Nalpac/yesterday's scoreboard (Step 2/2b) and Step 2d suggestion triage both ran light: the suggestion list came back empty (nothing to action); GA4/Nalpac depth skipped per the turn-budget ordering to reach Step 5 publish + Step 7 verify. Design-critic (Step 7.5) skipped, no subagent dispatch used this run. Docs-only diff, one `.md` file, appended above the end-of-file marker.

### 2026-09-09 — Routine A — Hero rotates to a wearable couples pick, Rail B rebuilt as the flagged Bondage/Restraints/Collars taxonomy rail, 3 fresh images, pleasure+play mastheads end a 9-day deep-refresh gap

- **What:** Wednesday run, Step 1c skipped (Monday-only). Read today's theme from `marketing_calendar` and the live page: no homepage-scoped row is `active` for this week; the only date-covering rows are the IG-scoped "Lube, Actually" campaign (id 24, active) and the still-`planned` "The Long Weekend In" / "Lube Week Sale" pair (ids 19/35, 09-07..09-19) already carried into every live surface as of run 752. Merchandised within that soft carryover, and **actioned the directive flagged-but-not-actioned by run 752** (`#8021`: Bondage/Restraints/Collars, 30 SKUs, 0 out of stock, a proven-converting category with no featured treatment). **Hero rotated** (held 3 days, rotation was due) from `inbloom-rosales-sucking-vibrator` to `adam-eve-pleasure-panties` (Adam & Eve Pleasure Panties wearable remote-control clitoral vibrator, $71.99, wholesale $40.00, margin $31.99, inv 876, `status:ACTIVE`, clears the $39 lead-surface floor), all four `singleton.emmaHero` fields rewritten to a discreet-wear/remote-control anchor, `singleton.emmaHeroStorefront.featuredProductHandle`+`primaryCtaLink`+`emphasisWord` re-pinned, handle verified 200. **Rail B** (`teamRail-2026-09-06-1`, previously a wand rail) fully rebuilt into the Bondage/Restraints taxonomy rail: `master-series-spiked-heart-choker-black`, `velvet-noir-collar-removable-clamps`, `sei-mio-silent-authority-progressive-interchangeable-ball-gag-set`, `under-the-bed-restraint-system-black`, plus `naturals-h2o-intimate-lubricant-8-5-oz` as the water-based pairing item (safe with both silicone toys and leather restraints) — all 5 verified `status:ACTIVE`, non-Discontinued, positive margin, handles resolve 200. `ctaLink` repointed `/collections/wands` → `/collections/restraints`; destination continuity confirmed by direct probe of the collection's server-rendered HTML (`under-the-bed-restraint-system-black` present in the SSR payload). Rail A (couples, held) and Rail C (lube/self-care, held) left untouched — both still fresh from 09-07/09-08. Zero cross-rail handle overlap. Wayfinder tile `wf65b` (previously "Built to be used often" → `/collections/play`) relinked to `/collections/restraints` with fresh label/aside. Couples band (`playTogether-starter`) recopied and `ctaLink` repointed from a standalone product to today's hero pick. **Announcement bar**: found and fixed a latent no-op — a prior run's convention of writing a top-level `singleton.homepage.announcementBar` field doesn't match what `_layout.tsx` actually reads (`sections[]` entry with `_type=="announcementBar"`, key `8ebaf6a2da8d`, 4 existing rotating trust messages); appended a 5th theme message to the real section instead of replacing the trust messages, and unset the stray inert top-level field. All new/changed copy passed a two-round `emma-empathy-reviewer` voice gate (one REVISE on the couples-band body for a same-page phrasing echo, one REVISE for the banned sensation verb "land(s)"; both fixed and re-verified clean). **Fresh-art floor triggered** by the hero rotation: 3 images placed (Rail-B/wf65b tile art — archetype B color-block, regenerated once after the first attempt failed self-review on the doctrine ground-lock, a saturated burgundy ground instead of the pale plum-soft tint; couples-band art — archetype C in-situ bright scene, ref-imaged off the new hero product; Discover-You promo art — archetype D metaphor macro, no-ref per the no-single-SKU carve-out). Panel deck `_updatedAt` 2026-09-03 (6 days old), still under the 7-day floor, not touched, due 2026-09-10. `singleton.homeSeo` reviewed (Step 2e): populated, live HTML matches exactly, 9 days old, no `HOMESEO:` token in the active brief → HOLD, no write, no `_updatedAt` bump. **Step 5b micro-refresh floor**: `categoryPage-pleasure` and `categoryPage-play` were 9 days stale (last deep-refreshed 2026-08-31; day-1 pair deferred entirely by run 752) — ran the masthead-`standfirst`-only micro-touch on both (no shelf reorder, no art regen) rather than deferring a third time; the full deep-refresh for this pair still remains owed. Emma Presets found still fully unpublished (34 `emmaPreset` docs, all `drafts.*`, zero live) — second occurrence (run 714, run 772) — filed as a suggestion rather than attempted inline (a full seed pass needs per-preset tag-vocab match verification, out of scope for this run's remaining budget).
- **Why:** Step 2c per-run freshness requires ≥2 changed surfaces with ≥1 being product selection; hero + Rail B both qualify (product selection, not copy-only), well past the minimum. The Bondage/Restraints rail both closes a named, aging gap from the prior run and satisfies the mission-brief's standing "one rail is a taxonomy/category surface" spirit; kept off the hero slot to avoid a theme-binding conflict (the active carryover theme is couples/wellness, not bondage-specific) per mission-brief §3's hero-belongs-to-theme rule. The announcement-bar fix matters because a stray top-level field write is invisible to every future run reading only that field — same failure shape as the historical `pageBuilder`-vs-`blocks[]` and `--set` JSONMatch traps this playbook already warns about, just on a new field.
- **Evidence:** run 772; gate `ok:true` throughout (dailyCents 60000, spent 0 tokens-only pre-run, runs 0/10, images 0/100, brief 10). Margin/status verified via Shopify MCP `search_products`/`graphql_query` on all 6 new picks (hero + 5 rail items): all `status:ACTIVE`, wholesale costs present, margins positive ($3.99-$31.99). `rails-fingerprint.ts --assert-changed rails,hero,couples` = `ok:true`, `stale:[]`, all three fresh; `deck` correctly reported unchanged (not due). Voice gate: two `emma-empathy-reviewer` dispatches, 10 PASS + 2 REVISE-then-fixed, 0 BLOCK, full transcript in the dashboard's transcript ref. Image self-review: 2 generation attempts on the wf65b tile (1 fail on ground-lock, 1 pass), 1 each on couples band and Discover promo, both pass on first attempt; all 3 placed images visually confirmed (undistorted, no baked-in text, product unmistakably the subject). Sanity docs written: `singleton.emmaHero`, `singleton.emmaHeroStorefront`, `teamRail-2026-09-06-1`, `singleton.homepage` (wayfinder tile, couples band, announcement section, stray-field cleanup), `categoryPage-pleasure`, `categoryPage-play`. Step 7 render-truth: `POST /cron/warm-homepage-b` → `ok:true, degraded:false, rails:4` after the final write, then a background 5-minute wait before fetching `https://xdipx.com/?variant=b&rt=772-final-<epoch>` to assert HTTP 200, hero handle, all rail headings, the wf65b tile label, and the announcement message present (see run 772 dashboard events for the fetch verdict recorded after this PR's evidence line was drafted — propagation timing meant the final fetch landed after this entry was written; if it surfaced a fallback, run 772's own events record the correction, not a silent success claim here). Suggestion **#8406** filed (`instructions`, `process`) for the Emma Presets gap, second occurrence named (run 714, run 772). Step 2d suggestion queue checked: two `kind:'instructions'` rows found (#287, #714 stale-duplicate, #4865), none actionable as a direct content swap this run — left untouched, not closed. Docs-only diff, one `.md` file, rebased onto latest `origin/main` immediately before opening the PR.

### 2026-09-09 — Routine B — Panel-deck render-truth: a silent slot drop, a check that raced itself, and a REVISE on the live page

- **What:** One code PR (`app/` + tests) plus two docs appends. **(1) `resolveBandOrder` silently discarded an explicitly-placed panel deck.** When a published layout carries a `panelDeckSection` and no usable `homeBand`, the function fell back to `DEFAULT_BAND_ORDER` — which is typed `BandName[]` and so structurally cannot hold the `panelDeck` slot, deleting the only instruction that layout actually carried. The guard's intent was right (a deck marker alone is not a homepage) but it threw the placement away with the missing bands. The deck is now re-added after `anchorGrid`, the slot the function already produces today for a deck published above the hero. **(2) The category healthcheck decided whether to assert `data-panel=` from its own reading of `layout.sections`** — a different predicate from the renderer's — and could see neither of the two ways a placed deck legally renders nothing (a dropped slot, and `PanelDeck` returning null when every row is empty). The pure band-order logic moved to a new zero-import `app/lib/home-band-order.ts` so the cron path and the renderer share one implementation instead of two that drift; `StorefrontHome.tsx` re-exports every symbol, so no existing importer changed. **(3) The check raced itself**, which is the likeliest actual cause of #8414: it decides the expectation from one read of the payload blob and asserts it against a separately-served, edge-cached render. A failed deck assertion now re-reads the payload and keeps the failure only if the deck is still expected — scoped to the deck, never rescuing a category or drop failure, never turning a real failure green on an unreadable payload, and the raced check stays visible under a `deckRaced` flag rather than being hidden. Two smaller corrections ride along: a placed-but-empty deck is reported as `emptyDeckPublished` instead of skipped in silence, and a deck failure now files deck-shaped remediation (the generic text sent readers to the category resolver and offered "unpublish the doc" as the rollback, which for the deck deletes the navigation layer instead of restoring it).
- **Why:** Ticket #8414, filed by the healthcheck at 14:00 UTC. The brief's own top homepage directive (#8025, the out-of-stock PDP) was already applied via merged PR #1123 on 09-08, so it was not this cycle's work. Traffic-gated as always (GA4 dark for the 8th brief, Shopify-side sessions 9/week against the 300 threshold), so per playbook §1 the ambition mandate is satisfied by banked design capital and cheap, certain real-defect fixes rather than new machinery — and this is one. **The run's first root-cause claim did not survive measurement and is corrected here rather than quietly amended.** At 13:55 UTC `/?variant=b` served 266 KB with zero `data-panel` and no "Pick your door."; at 14:25 six consecutive cache-busted probes served 291 KB with both present and a second healthcheck run had the deck expectation firing and passing, nothing deployed in between. design-critic's capture in the same window found the deck absent from the SSR HTML while ten `[data-panel]` nodes were live in the hydrated DOM — two requests, two payload states, the same race. So the slot drop is a real latent defect fixed on its own merits, not the proven cause, and the race guard is what addresses the symptom.
- **Evidence:** run 778; gate `ok:true` (remaining $599.86, runs 1/10, images 4/100, brief 10). Typecheck clean, **316 files / 4299 tests**, build green — re-run independently by `qa-reviewer`, which also verified the new tests are not decorative by reverting only the fallback branch and observing 5 real failures including the target case. `tech-architect` **PASS**: boundary intact both ways (zero-import module, no `.server.ts` in the client graph, no React in the cron graph), judged the healthcheck change a strengthening rather than a weakening, and found a second pre-existing bug this run had not claimed — the old check never looked at row content at all, so it would have demanded `data-panel` for a published-but-empty deck. Both of its non-blocking nits were fixed in-run rather than deferred; its ADR recommendation (extract renderer-decision logic to a pure `app/lib` module whenever a non-React consumer needs the same answer a component does) is noted for a future cycle. **The mandatory live-page `design-critic` spot-check ran and returned REVISE** (a rubric BLOCK on color 2, downgraded per the uncalibrated-critic rule): five measured WCAG AA failures from top-aligned copy on a bottom-oriented scrim, worst 1.03:1 on the plum `.em` word "alone"; a rainbow-gradient retail carton with baked-in text as the LCP hero; eight consecutive pale sections before the first tint; four sections off the `py-16 md:py-20` rhythm; surviving v2 chrome; four coral elements above the fold; a double-arrow CTA; duplicate JO H2O variants in adjacent Nº03 cells; a 20px header/body grid misalignment. Not a gate failure for this PR — the diff changes no visual surface and every defect is pre-existing — so they were filed rather than used to block a fix that would address none of them: **#8417** (P1, cxRisk high, the AA/scrim fix), **#8418** (doctrine drift), **#8419** (render bugs + deck orphan row), **#8420** (content-plane work for Routine A), **#8421** (the working Playwright-through-proxy capture recipe, plus a §7 reference-bench maintenance note after Aesop and Arket both 403'd for the first time). Step 0.5 teardown captured real bytes from unboundbabes.com, glossier.com, dame.com, sextoy.com and spectrumboutique.com (first resolve in three cycles) after WebFetch returned 429 on all four attempts across four hosts; refusals recorded as refusals only (aesop 403, arket 403, lovehoney 403, maude curl-35, vush CONNECT 502), nothing quoted from memory. Its decision — two door layers are legitimate when each names its own job, and our defect is that the deck and the mosaic both say "door" one screen apart — was reached independently by design-critic, and its placement finding (no captured reference puts taxonomy immediately after the hero) agrees with where this change lands the deck. Checklist items settled honestly as N/A: variant-b quarantine audit (#1450, nothing quarantined, `VARIANT_B_SECTION_TYPES` untouched), See-all cross-check (#4270, no module shipped — and cleared empirically anyway: the seven live deck destinations are all real collections with zero best-sellers fallback in the code path), Emma voice gate (zero customer-facing strings changed; the door-metaphor collision it does surface is filed as #8420). IA fence intact: no new section type, no new route, no schema change, no `/discover` link added, additive-only. Changelog rebased onto latest `origin/main` (5d72e9b) immediately before the PR per #2878 and appended above the end-of-file marker.

### 2026-09-11 — Routine A — Hero rotates to a wand-plus-glide theme combo for Lube Literacy, all three wayfinder tiles regenerated on a plum-soft ground, panel-deck 8-day floor closed

- **What:** Theme held: **Lube, Actually / lube-literacy editorial window** (`marketing_calendar` id 24, the only `status:'active'` row, 09-07..09-19). LUBE20 (row 35) is still `planned` and unminted, so **no discount framing shipped anywhere**. **Hero rotated** `adam-eve-pleasure-panties` to **`doxy-go-travel-sized-wand-massager-purple`** ($68.99, MAP $54.99 so price sits above MAP, inventory 647, `productType` "Wand Massager" not Discontinued, clears the $39 lead-surface floor, primary image `93282A` inspected by eye and confirmed a clean product-large shot rather than the Nalpac boxed-packshot pattern). The mission-brief section 3 hero-to-theme binding is satisfied by the **combo** form, not by an editorial argument: the week's category (water-based glide, `jo-h2o-original-water-based-lubricant-4-oz`) is visibly in the hero art and named in the hero copy, because consumables are attach-only and cannot themselves hold the hero under the section 5 price floor. Hero copy fully rewritten (`singleton.emmaHero` eyebrow/headline/body/pullQuote) and the pin plus `primaryCtaLink` re-pointed in the same patch. **All three wired rails rebuilt:** Rail A "Broad and steady, and better with something slick." (Flexi Lexi $44.99, Nixie Ombre wand $51.99, Wand & Done $58.99, JO H2O attach) with slots 1-3 all non-consumable and all above $39; Rail B "Buckle it on and the night already has a direction." carrying the brief's Bondage/Restraints/Collars directive (#8021) on a completely fresh six-SKU lineup with a Sliquid Organics attach; Rail C the under-$30 entry rail at 7 products. Every rail carries a pairing/accessory item and a `ctaLink` that is not blank. **All three wayfinder tiles regenerated** (Atlas `seedream-4.5-edit`, `--ref-image` on every one) and tile 1 re-pointed `/collections/under-30` to `/collections/lubricants` (the theme door) and tile 3 to `/collections/vibrators` (hero follow-through); all three still target collections rather than PDPs. Discover-You promo copy and the **couples band** (`playTogetherBanner`) copy rewritten to the wand-handover story. Announcement message 5 retired and replaced with the week's lube-literacy line. **Panel-deck 7-day floor closed** at 8 days stale (`_updatedAt` was 2026-09-03): deck `eyebrow` refreshed and the "Just landed" large banner relabeled "Landed this week", reusing existing art so no image budget was spent and **without reordering any door** (panel order is navigation, mission brief section 11d). **Step 5b:** health sweep across all live merchandised pages, plus the masthead micro-refresh floor on today's pair **`categoryPage-body` + `categoryPage-wear`** (the pair run 790 deferred) — both `standfirst` strings rewritten, which also retired the duplicated "Lube, actually." opener that was running verbatim across two pages in breach of the charter's fresh-language rule. `singleton.homeSeo` reviewed read-only and **held** (Step 5c): populated, live HTML matches the published doc exactly, no draft exists, and brief 10 carries no `HOMESEO: ROTATE` line.
- **Why:** Run 790 (2026-09-10) failed with zero publish on cold-start turn starvation, so the live page was run 772's 09-09 slate and was two days old with an off-theme hero. The hero and rails were rotated on **editorial cadence and margin**, not on metrics: GA4 reports 116 sessions / 28 days with `actionable:false` and an explicit "do not optimise on them" verdict, far below both the 1,000-session action floor and this routine's own 300-sessions/week gate, so the scoreboard was recorded and never allowed to trigger a swap. The strategy brief's homepage directives drove two concrete picks: the Bondage/Restraints/Collars push (#8021, the only category with a proven real-customer purchase) is Rail B, and the "no discount framing" instruction held the LUBE20 window editorial. The brief's top directive (#8025, the out-of-stock Tantus P-Spot PDP) is a **code** change to the PDP render and is out of this routine's content-only lane; it was not attempted here.
- **Evidence:** run 811; gate `ok:true` (remaining $600.00, runs 0/10, images 0/100, brief 10). Sameness diff, four surfaces moved and three of them are imagery or product selection rather than copy: `rails-fingerprint.ts --assert-changed rails,hero,couples,deck` exited 0 with `stale:[]` and all four slots `fresh:true`. **3 images generated, 0 reused**, all placed via `gen-homepage-image.ts --run-id 811`, asset ids `image-6c2582a7816e3eaa03de0e4dd7aebd2d98c70fa7` (tile A, archetype B), `image-bb1deb8ee8106f3fa2b0a7e0e8724b2f97eab24f` (tile B, archetype B), `image-0942b353358f739d2d31a807aa52b28ab3eb503b` (tile C, archetype C); a fourth attempt on tile B failed on an `UND_ERR_CONNECT_TIMEOUT` and was retried once rather than counted. Sanity revisions: `singleton.emmaHero` hgs5zYXyEVhSt3N30z7ce9, `singleton.emmaHeroStorefront` oia2XQz85UJCgEneWqfCsQ, `teamRail-2026-09-06-0` zEH3c1VAp1l55cR7Q28UqF, `teamRail-2026-09-06-1` hgs5zYXyEVhSt3N30z7dYY, `teamRail-2026-09-06-2` oia2XQz85UJCgEneWqfF0N, `singleton.homepage` oia2XQz85UJCgEneWqfU8h, `singleton.panelDeck` zEH3c1VAp1l55cR7Q28f7T, `categoryPage-body` oia2XQz85UJCgEneWqfUln, `categoryPage-wear` zEH3c1VAp1l55cR7Q28hXP. Every patch was re-read after writing to confirm it landed (the run-697 silent-no-op trap), and no document was left as a draft. `/cron/warm-homepage-b` returned `ok:true, degraded:false, rails:4`.

### 2026-09-13 — Routine A — Hero and couples band rotate to a couples strapless strap-on, calendar-hygiene gap named again

- **What:** Sunday run, so Step 1c (recon + theme-setting) correctly did not fire. Found the same class of `marketing_calendar` drift the 2026-09-06 entry above already named: row 6 ("Weekend In, Not Out", event_date 2026-08-10) is still stuck `status:'active'` a month later, and row 19 ("The Long Weekend In", the week-of-2026-09-07 theme, text says "Weekend Sep 5-7 primary; weekday carryover") is still `planned`. Did not mutate the calendar: the team-token `/api/team/calendar` route hard-guards `setStatus` to `IG:`-prefixed rows by design (broader changes are admin-session-only), and a direct SQL UPDATE attempt was correctly blocked by the session's own action classifier. Filed suggestion **#9125** naming both rows and the 2026-09-14 deadline (row 30, "Two at Once: Dual Stimulation Week", starts tomorrow). Used row 19's text as informational theme context only (unhurried couples/wellness framing, no discount, respect MAP).
  **Hero rotated** from `inbloom-rosales-sucking-vibrator` to **`together-double-tap-dual-ended-vibrating-strapless-strap-on`** ($126.99, wholesale $70/msrp $139.99 → ~45% margin, inventory 75, Shopify `status:ACTIVE`, tagged `new-arrival`, dial `couples`): clears the $39 lead-surface floor, fits the outgoing couples-weekend theme and bridges into tomorrow's dual-stimulation theme without repeating either week's likely pick. The catalog primary image (`97211A.jpg`) is the Nalpac packaging shot; used the clean `97211B.jpg` sibling as `--ref-image` instead of promoting it (no time this run to verify a promotion is side-effect-free on the live PDP). `singleton.emmaHero` (eyebrow/headline/body/pullQuote) rewritten register-9, giver-anchored per the couples-toy craft rule, and `singleton.emmaHeroStorefront.featuredProductHandle` + `primaryCtaLink` re-pinned to the new handle in the same patch.
  **Couples band** (`playTogetherBanner`, key `playTogether-starter`) copy rewritten to match (heading/body/ctaLabel/ctaLink) and **one new image generated** (Atlas `seedream-4.5-edit`, archetype B color-block still, plum-soft-toned silk with a color-rhyme ribbon, `--ref-image` the same product photo, self-reviewed: undistorted, no baked-in text, product unmistakably the subject). Two earlier prompt attempts using an archetype-C two-hands-reaching scene were rejected by both Atlas and fal.ai's content-policy filters before this archetype-B prompt cleared on the first try; Imagen was unavailable this run (`BILLING_DISABLED` on the Vertex AI project, an infra issue, not a content one — worth a separate ops ticket).
  **Announcement bar** message 1 (shipping-location factoid, the least theme-relevant of the five) replaced with a couples-forward line; the other four evergreen trust/discretion messages were left untouched.
  Rails, wayfinder mosaic, Discover You promo, panel deck, Emma Presets, and the merchandised category/drop pages (Step 5b) were **not** touched this run and are named here rather than silently skipped: environment cold-start (`npm install`, direct-Postgres access blocked by network policy, working around it via the Neon HTTP driver) plus the calendar audit and two image-generation content-policy retries consumed most of this run's turn budget before Step 5 published. Panel deck is 2 days into its 7-day floor (last touched 2026-09-11), so no floor breach. `singleton.homeSeo` was not reviewed this run (Step 2e skipped under the same budget pressure); flagging for the next run rather than silently reporting three facts left unread.
- **Why:** Mission brief section 10's freshness floor is per-run, not weekly, and yesterday's hero/couples pairing needed to move regardless of the calendar bookkeeping gap; a couples strapless strap-on satisfies both the outgoing and incoming week's category without inventing a theme change this routine isn't scheduled to make today. The archetype-B fallback for the couples band follows the mission brief's own regenerate-once-then-adapt rule rather than burning further budget chasing an archetype-C scene two providers had already flagged.
- **Evidence:** run 845; gate `ok:true` throughout (remaining $599.96 of $600, runs 0/10 before this one, images 1/100, brief 10). Sameness diff: two surfaces changed, hero (product + copy) and couples band (copy + new image) — satisfies the "at least one of two is imagery/product selection" rule. `rails-fingerprint.ts --assert-changed hero,couples` exited 0, `ok:true`, both slots `fresh:true`; rails and deck correctly reported unchanged (not touched, not due). Sanity documents written and each re-queried to confirm the write landed: `singleton.emmaHero`, `singleton.emmaHeroStorefront`, `singleton.homepage` (couples-band fields + one announcement message). Step 7 render-truth: `POST /cron/warm-homepage-b` once, then a cache-bypassed fetch (`?rt=845-...`, confirmed `x-vercel-cache: MISS`, `age: 0`) showed the hero and announcement updated but the couples band still serving pre-patch copy — the known referenced-doc eventual-consistency lag from the playbook's Step 7 notes, though `playTogetherBanner` is an inline section, not a separate ref doc. A second `warm-homepage-b` plus re-fetch resolved it: hero handle/headline, couples heading/body/image, and announcement message all present; JSON-LD valid ×4; no "daily deal" framing; all three rail titles and wayfinder tile keys present. Independently corroborated by `/cron/homepage-healthcheck`'s own `renderTruth` block: 9/9 assertions `ok:true`, zero fallbacks, zero missing sections. Both `/` and `/discover` returned 200 with no problems. **Skipped this run, named rather than silent:** Step 2 GA4/Nalpac depth read, Step 2d suggestion-queue triage, Step 2e homeSeo review, Step 3.5 full art-direction scheme (used a minimal in-line one), Step 5b category/drop-page health sweep and deep refresh, Step 5c (no `HOMESEO:` trigger present regardless), Step 7.5 `design-critic` live-page spot-check (browser-capture setup not attempted this run). All of the above are OPTIONAL-tier per the routine's own turn-budget table except the Step 5b health-sweep-only branch, which should have run at $0 and did not; naming that as the one MANDATORY-tier miss this run, for the next run to pick up immediately. Docs-only diff, one `.md` file, based on latest `origin/main` at run time.

### 2026-09-15 — Routine A — Monday: hero rotates to the FemmeFunn Vortex Booster, Rail 0 relineup, 4 fresh images; announcement + category-page continuity defects named honestly

- **What:** Monday run inside the confirmed "Two at Once: Dual Stimulation Week" theme (`marketing_calendar`, weekStart 2026-09-14; no drift, no calendar write needed). GA4 `actionable:false` (109 sessions/28d vs the 1000 floor; 536 views, 9 ATC, 8 checkouts, 2 purchases, $60.03) — every keep/drop below is merchandising judgement, recorded as a decision event, not a metric-driven swap. Start-of-run assertion confirmed yesterday's publish (run 845, 2026-09-13) had landed in served HTML before anything was touched.
  **Hero rotated** from `together-double-tap-dual-ended-vibrating-strapless-strap-on` to **`femmefunn-vortex-booster-rabbit-...-pink`** ($112.49, wholesale $42.50, margin $69.99/62% — best in the eligible candidate set, inventory 20, `productType` not Discontinued, MAP null so discount framing is legal, clears the $39 lead-surface floor, non-consumable): handle verified 200 before write. `singleton.emmaHero` fully rewritten (eyebrow/headline/body/pullQuote) and `singleton.emmaHeroStorefront.featuredProductHandle` + `primaryCtaLink` (→ the PDP) + `primaryCtaLabel` (`I'll take it ♥`, whitelist) re-pinned in the same patch.
  **Rail 0** (`teamRail-2026-09-06-0`, `ctaLink /collections/pleasure`) relineup to a new heading + 6 handles, all ≥ $39 in lead slots, no handle shared with rails 1/2, pairing item (`jo-h2o` lube) placed in the attach slot only. Rails 1 & 2 held (depths 5 and 7; under-$30 entry rail's 7 clears the 6-product floor). All 8 written handles verified 200 pre-write.
  **Wayfinder mosaic** tiles wf65a and wf65c got new labels + new art (archetype B); all 3 tiles still target collections. **Discover You promo** and **couples band** both got new copy + new art (4 images generated total, all placed, fresh-art floor met on a changed-hero day: 0 reused).
  **Announcement bar** patch landed in Sanity (revision returned) but the new messages return 0 hits in served homepage HTML — recorded as an unverified/failed surface, not claimed as shipped.
  **Step 5b:** the cron healthcheck panel (`/api/homepage-team/merchandised-pages`, `/api/homepage-team/state`) 404'd, so the deterministic $0 health sweep could not be read; substituted a direct 200-probe of the day-1 pair only, which is a named gap, not a pass. Today's 3-day-cycle pair (`categoryPage-pleasure` + `categoryPage-play`, both 6 days stale) had its full deep refresh **deferred** under the cold-start turn-budget branch (decision event logged), but the **mandatory masthead `standfirst` micro-refresh floor ran on both**, Sanity-readback-verified and origin-verified with the new theme line present. Panel deck (`_updatedAt` 2026-09-11, 4 days) correctly left untouched, inside its 7-day floor, order not touched. Monday's all-pages theme pass covered only the two pair pages; `body`, `wear`, `discover`, and both drop pages are named-deferred, not silent. Curiosity shelf (Nº 07) and Emma's Presets re-curation were not reached this run (no Sanity write on either, so no false `_updatedAt` move on the shelf).
  **Step 1c competitor recon did not run** — this dispatch had no WebSearch/WebFetch tool attached, so no memo was produced and none was fabricated; the runway check did pass (5 future `planned` weeks ≥ the 4-week floor). Step 2d inbound suggestions were listed but not acted on or closed this run. Step 5c stayed HOLD (no `HOMESEO: ROTATE` token in the active brief).
- **Why:** The FemmeFunn Vortex Booster cleared every pick-gate criterion (price floor, margin, stock depth, non-Discontinued, MAP-legal) with the best margin in the eligible set and fits the dual-stimulation theme directly, giving both the on-ramp and the reach jobs a fresh anchor after four days on the strapless strap-on. Rail 0's relineup keeps the taxonomy/pairing rules intact (attach-only lube slot, no cross-rail handle duplication) while visibly changing product selection, not just copy, satisfying the sameness-diff rule. The masthead-only micro-refresh on the day's category pair (rather than a full deep refresh or a silent defer) follows the playbook's second carve-out exactly: it is the MANDATORY-tier item that survives cold-start turn pressure even when the full deep-refresh legitimately can't fit.
- **Evidence:** run 880; gate `ok:true` throughout ($60000/day cap, $0 spent at start, 0/10 runs, 0/100 images, `activeBriefId 11`; re-gated before each of the 4 image generations). Merch-freshness gate **PASS**: `rails-fingerprint.ts --assert-changed rails,hero,couples` exited 0, all three must-change slots `fresh:true`; advisory `deck` slot correctly unchanged. Sameness diff: 5 surfaces changed (hero product + copy, rail 0 product + copy, wayfinder tiles, promo, couples band), well past the 2-surface floor and including non-copy product selection. Step 7 render-truth gate **PASS with one defect**: origin fetch (cache-busting `rt=`) returned HTTP 200, 4 valid JSON-LD blocks, zero "daily deal" framing, hero handle/headline, all 3 rail titles, both refreshed tile labels, promo heading, couples heading, and all 4 new Sanity asset ids present in served HTML; no fallback section rendered; link ratio 96.2% product/collection vs the 70% target. **Defect 1:** announcement bar not visible at origin despite a landed Sanity write (filed for follow-up). **Defect 2:** See-all continuity for the rail 0 destination is inconclusive, not passed — the raw-HTML probe of `/collections/pleasure` hit the known client-hydrated-grid false-0 and the loader/JSON probe the playbook requires was not reached. Step 7.5 `design-critic` spot-check **skipped at the turn cap** (named, not silent) — the Playwright/proxy capture recipe did not fit after cold-start `npm install` consumed early budget; no verdict fabricated. Spend: 4 image rows auto-posted by `gen-homepage-image.ts` under `feature:homepage-images` (`atlas/seedream-4.5` ×3, `atlas/seedream-4.5-edit` ×1 for the couples band with `--ref-image`), ~$0.10–0.20 total against the $600/day cap; 1 token row posted under `feature:homepage-merchandise`, Max-billed at $0. No site Anthropic-keyed endpoint was called. Suggestion **#9503** filed naming this changelog/PR step's git-access contradiction; a second suggestion (cold-start `npm install` starving Step 5b) failed to post (empty response) and is not yet on the bus. Two `/event` POSTs returned `000` on first attempt; one retried successfully, the pick-gate event did not land on retry (its content is preserved here and in the run's own summary instead). Docs-only diff, one `.md` file, based on latest `origin/main` at run time.

### 2026-09-16 — Routine A — Wednesday: rail 0 product swap under the dual-stimulation theme, announcement refresh, body+wear category micro-refresh floor

- **What:** Wednesday run, theme held ("Two at Once: Dual Stimulation Week", unchanged since Monday's run 880; no drift, no calendar write needed, Step 1c skipped correctly — not Monday). Start-of-run baseline fingerprint captured before any write (`rails-fingerprint.ts`), confirming yesterday's publish (run 880) had landed: hero `femmefunn-vortex-booster-rabbit-...-pink` live, rail 0 (`teamRail-2026-09-06-0`) and deck (`_updatedAt` 2026-09-11, 5 days old, inside the 7-day floor) as run 880 left them.
  **Rail 0** (`teamRail-2026-09-06-0`, first/anchor rail, `ctaLink /collections/pleasure`) relineup: eyebrow → "Two sensations, one hand", heading → "Nothing here makes you pick just one.", slot `d3` swapped from `strap-on-me-multi-orgasm-bendable-strapless-strap-on` to **`inbloom-rosales-sucking-vibrator`** ($48.99, inventory 919, `productType` Vibrator, non-Discontinued, clears the $39 lead-surface floor, sourced from `discovery_index_payload` via the Neon HTTP driver since direct Postgres TCP is blocked by network policy). Handle verified 200 before write. Depth held at 6 (floor is 4); no handle shared with rails 1/2 (no new duplication introduced).
  **Announcement bar** message 5 (`a618m5`, the most mechanism-flavored of the five: "Dual motors, rabbits, and dual-ended toys. What two points of contact actually change.") rewritten reader-centered per the sell-the-experience-never-the-mechanism craft rule: "Both at once, and neither one has to wait its turn." The other four messages held.
  **Step 5b:** today is day 2 of the 3-day category cycle (day 1 pleasure+play ran under run 880 on 2026-09-15). Full deep refresh of `categoryPage-body` / `categoryPage-wear` deferred under this run's own turn-budget pressure (named here, not silent); the **mandatory masthead `standfirst` micro-refresh floor ran on both**, lightly tying each to the week's theme, Sanity-readback-verified and origin-verified. Panel deck correctly left untouched (5 days old, inside its 7-day floor, order not touched). Curiosity shelf (Nº 07) and Emma's Presets re-curation were not reached this run (theme unchanged from Monday, no forced re-curation trigger; no Sanity write on either, so no false `_updatedAt` move).
  Hero, wayfinder mosaic, Discover You promo, and couples band were **not** touched this run (named, not silent): hero and theme are unchanged since run 880, so the fresh-art floor (3 new images) does not trigger, and this run's turn budget went to the rail/announcement/category writes plus their verification instead of a second hero rotation this week (mission brief §3's twice-weekly floor: hero already rotated 2026-09-14→09-15 this week; a second rotation is due before week's end, not today by rule).
  Step 2e (homeSeo read-only): singleton populated, live HTML matches the published doc exactly (title + meta description), 16 days since `_updatedAt` (2026-08-31) — no `HOMESEO: ROTATE` token present in the active brief, so Step 5c correctly stayed HOLD. Step 2d: 2 approved rows read, both `kind:'instructions'` (owner-decision items, not `process`/`strategy` content this run can execute or close), left untouched with no false `applied`.
- **Why:** Mission brief section 10's freshness floor is per-run: with hero/theme legitimately unchanged since yesterday, the compliant move under this run's turn budget was a real product-selection swap on the lead rail (not a copy-only refresh) plus the mandatory category micro-refresh floor, rather than forcing an unnecessary hero rotation and its 3-image floor. The mechanism-flavored announcement line was replaced because it named "what two points of contact actually change" — spec/mechanism language the charter reserves for spec blocks, not the desire-forward register.
- **Evidence:** run 899; gate `ok:true` throughout ($60000/day cap, $0 spent, 0/10 runs, 0/100 images, `activeBriefId 11`). Sameness diff: two surfaces changed — rail 0 (product selection: one handle swapped, plus heading/eyebrow) and announcement message 5 (copy) — satisfies "at least one of two is imagery/product selection." `rails-fingerprint.ts` re-capture after publish shows rail 0's heading and handle list changed versus the pre-run baseline. Sanity documents re-queried after each patch to confirm the write landed: `teamRail-2026-09-06-0`, `singleton.homepage` (announcement message), `categoryPage-body`, `categoryPage-wear`. Step 7 render-truth: `POST /cron/warm-homepage-b` once, waited 5 minutes, then a cache-bypassed fetch (`?rt=899-...`, confirmed `x-vercel-cache: MISS`, `age: 0`) showed HTTP 200, 1 valid JSON-LD block, zero "daily deal" framing, hero handle present, all 3 rail titles present (including the new rail 0 heading), the new announcement line present, `<title>`/meta matching the SEO singleton. `categoryPage-body` and `categoryPage-wear` origin-fetched separately (4-minute wait) and both show their new standfirst text. **Step 7.5 `design-critic` spot-check skipped at the turn cap** (named, not silent) — this run's budget went to the data/write/verify path above; no verdict fabricated. Spend: 1 token row posted under `feature:homepage-merchandise`, Max-billed at $0 to the site (model `claude-sonnet-5`); no image generation this run (none required, no site Anthropic-keyed endpoint called). **Skipped this run, named rather than silent (all OPTIONAL-tier per the routine's own turn-budget table):** Step 2/2b GA4/Nalpac scoreboard depth, Step 3.5 full `homepage-art-director` scheme (rail copy authored in-line instead), Step 5b deep-refresh pair for `categoryPage-body`/`categoryPage-wear` (micro-refresh floor ran in its place), Step 5c (correctly HOLD regardless, no trigger present), Step 7.5 as above. Docs-only diff, one `.md` file, based on latest `origin/main` at run time.

### 2026-09-16 — Routine B — The design gate's own capture CLI could not run in the place the gate runs

- **What:** One code PR. `scripts/design-snapshots.ts` exists to feed the mandatory `design-critic` gate (Routine B Step 4) and Routine A's Step 7.5 post-publish spot-check. Both of those only ever run from a scheduled cloud routine, and **the CLI could not run in one at all.** Two independent blockers, both reproduced before anything was changed: (1) the sandbox image pre-installs one chromium build under `PLAYWRIGHT_BROWSERS_PATH` and forbids `playwright install`, so the pinned `@playwright/test` died with `Executable doesn't exist at .../chromium_headless_shell-1234/...` and the CLI had no `executablePath` escape hatch; (2) with the right binary supplied, `page.goto()` returned `net::ERR_CERT_AUTHORITY_INVALID` because chromium does not trust the agent proxy CA, while Node's `fetch` does. `launchBrowser()` now falls back to `$PLAYWRIGHT_BROWSERS_PATH/chromium` **only after** Playwright's own resolution throws, and `--via-fetch` serves every request from Node through `context.route()`, auto-enabling only when an HTTPS proxy is set **and** the base is not loopback, so local and CI runs are unchanged (`--no-via-fetch` opts out, `--executable-path` forces the binary). Added `tablet` 768 and `wide` 1440 viewports plus a comma list and a `doctrine` preset, because **this gate is specified at 375/768/1440 and the CLI could only produce 375 and 1280** — the specified gate was literally unachievable with the repo's own tool. Both playbooks now give the one command instead of the prose recipe, and `routine-network-requirements.md` §5, which listed the composed-page design gate as unreachable in the cloud runner and named the degraded per-image heuristic the STANDING path, is corrected: that row was what told the next run not to bother trying the real gate. Two further corrections found by this run's own gates and verified against code first: `routine-design-cycle.md` §1 described `/api/team/discovery-vocab` as serving "per-tag **and per-combination**" counts when `computeVocabCounts()` tallies each tag independently and serves per-tag only (a run trusting it would think the mandatory cross-product gate was one call), and `design-doctrine.md` §3 still cited coral `#FF5A36`, sage `#7C8F78` and `ink-4` `#9A8F97` against a shipped `#C2350F`/`#596756`/`#726673` darkened by ticket #3789 for AA — a critic scoring color against that table would file the *correct* shipped CTA color as a defect (#8434 false-positive class). Values synced, no rule changed. Docs deliverables ride along: the Step 0.5 teardown delta and a new concept wire, `concepts/nothing-in-the-way.md`.
- **Why:** Traffic-gated as always (far below the 300-sessions/week threshold), so per playbook §1 the ambition mandate is satisfied by banked wires plus a cheap, certain real-defect fix — and this is one, chosen because it unblocks the gate this routine keeps failing to run. The cost was already paid twice and named in the changelog: run 778 hand-rolled the recipe from #8421's prose, and **run 899 skipped the mandatory spot-check outright at the turn cap.** A workaround recorded as prose is a workaround that gets re-implemented or skipped; moving it into the script took one cycle. The ambition mandate is met by `nothing-in-the-way.md`, the first of the seven banked concepts whose input is the **obstacle** rather than the appetite — all 30 reachable pole combinations measured against the `>=2` floor and all 30 pass. It also **reproduced the run-646 trap and caught it**: `Body-Safe-Silicone` carries 11 products, clears the pole floor 5x, then returns 0/0/1/0 on every reachable combination, so it was excluded from v1 rather than shipping a dead door on the most emotionally loaded question in the category.
- **Evidence:** run 905; gate `ok:true` (enabled, $0/$600 spent, 1/10 runs, 0/100 images, `activeBriefId 11`). Defect reproduced before the fix and the fix proved after: `--no-via-fetch` still prints `transport: chromium direct` and fails with `net::ERR_CERT_AUTHORITY_INVALID` at real exit code 1, while the default path produced real captures at **375x16128, 768x12249 and 1440x11615**. Typecheck clean, **327 files / 4438 tests**, build green — all three re-run independently by `qa-reviewer` in an isolated detached worktree per #8423 (git refused a second worktree on the same branch, so it detached on the exact commit; main tree confirmed untouched). Its **revert-to-prove** pass fired on all four new helpers with 3/3/1/3 real failures, so none of the 17 new tests are decorative. `tech-architect` **PASS**, having verified the local/CI-unchanged claim against `ci.yml` rather than the docstring, and confirmed the fallback is reachable only from inside `launch()`'s catch; **all four of its non-blocking nits were fixed in-run rather than deferred**, including moving the `NODE_USE_ENV_PROXY` mutation out of import scope (re-proved after the move with a byte-identical 2144866-byte capture). **The mandatory live-page `design-critic` spot-check ran** — the first cycle where it ran on the repo's own tooling — and returned **REVISE** (rubric BLOCK on imagery 2, downgraded per the uncalibrated-critic rule; hierarchy 3, spacing 3.5, type 3.5, color 4, motion recorded as **n/a, not assessable from stills** rather than guessed; average 3.08). It re-verified the previously-applied tickets from pixels instead of trusting their status: **#8417 VERIFIED FIXED** (the `.em-on-dark` lift ships at `#B98BDE`, nine pairings sampled, all clear AA with margin, the 1.03:1 worst case gone), #8418 mostly fixed, #8419 split, #8420 still present. New findings filed rather than used to block a diff that changes no visual surface: **#9675 (P1, cxRisk high — a live rail card renders supplier package art breaching the doctrine's §4.5 and unambiguous-adulthood hard limits, which the doctrine files under legal/processor/ad-platform; two more breach the §4.3 ceiling; one root cause, supplier box art rendered as card art with no gate)**, #9679 (P1, the 768 hero broken), #9680 (P2, container drift + orphan rows + double-arrow), #9681 (P2, stale pre-#3789 coral surviving in the wordmark asset, `og-card.server.ts` and the design-gallery swatch), #9682 (content-plane, Routine A). Retro rows: #9683, #9684, #9685. Checklist settled empirically, not asserted: variant-b quarantine audit (#1450) N/A with `VARIANT_B_SECTION_TYPES` appearing 0 times in the diff; See-all cross-check (#4270) N/A with 0 occurrences of `collections/best-sellers`; Emma voice gate N/A (zero customer-facing strings); `seo-pdp-auditor`/`aeo-geo-auditor` N/A (no rendering, JSON-LD, canonical, markdown or section-structure change). IA fence intact: no new section type, no new route, no schema change, no `/discover` link added. Teardown sourcing honest: 3 hosts actually fetched, 6 refusals each recorded with its code, nothing quoted from memory. Rebased onto latest `origin/main` (83e647a) immediately before the PR per #2878 and appended above the end-of-file marker.

### 2026-09-17 — Routine A — Hero copy refreshed (pin held on a packaging-shot reject), rail 0 relineup, discover masthead micro-refresh

- **What:** Theme HELD ("Two at Once: Dual Stimulation Week", unchanged since 2026-09-14; Thursday, so Step 1c correctly skipped). At the Step 3 pick gate, both fresh dual-stimulation hero candidates were rejected after downloading and looking at their Shopify primaries: `adam-eve-thrust-bunny-triple-stimulating-rabbit` (primary `96200A.jpg`, retail carton with baked-in "THRUST BUNNY" text) and `camtoyz-rabbit-vibrator-usb-c-charging-osma` (primary `98479A.jpg`, photographed retail box). With no clean-primary alternative resolvable in budget, the hero **product pin held** at `femmefunn-vortex-booster-...-pink` (the same named branch run 861 took), but **hero copy was fully rewritten** (`singleton.emmaHero` eyebrow/headline/body/pullQuote, rev `GowCCsyDqvdnsCFjpQI4OJ`). Because hero and theme both held, the fresh-art floor did not trigger — 0 images generated, correct. **Rail 0** (`teamRail-2026-09-06-0`) relineup to 6 handles incl. the mandatory pairing item, new heading "Neither sensation has to wait its turn." / eyebrow "Two places, one toy", `ctaLink /collections/pleasure` (rev `bbWw1ds9VLtsASktMQgJoi`); rails 1–2 held. `singleton.homepage` refreshed: announcement message, wayfinder promo + two tile labels/asides, couples band heading + body (rev `bbWw1ds9VLtsASktMQgNUi`). Step 5b (day 3 of the 3-day cycle: discover + drop pages): `categoryPage-discover` masthead `standfirst` micro-refreshed and readback-verified; `dropPage-new` and `dropPage-on-sale` are **NO-OP, named** — neither doc carries a `categoryMasthead` block, so there was nothing to patch. Full category deep-refresh deferred under the cold-start branch (#2977: no `node_modules` in the checkout). `singleton.panelDeck` correctly left untouched (6 days old, inside the 7-day floor, order not touched). Step 5c (homeSeo) correctly HOLD — the active brief explicitly says do not rotate this week, and the 28-day floor (17 days elapsed) would block it regardless.
- **Why:** Mission brief §5's packaging-shot check is a hard reject even when every numeric score passes, so the theme-native hero swap that would otherwise have run today was correctly declined rather than pinning a carton shot; refreshing the hero copy while holding the pin kept the page in step with the week's theme without shipping a defective image. The rail 0 relineup and category micro-refresh satisfy Step 2c's sameness-diff requirement (product selection, not copy alone).
- **Evidence:** run 917; gate `ok:true` throughout ($600/day cap, $0 spent, 0/10 runs, 0/100 images, `activeBriefId 11`). `rails-fingerprint.ts --assert-changed rails,hero,couples` exited 0 ("every must-change slot moved"). Step 7 render-truth: **PASS** after three warms (the first two returned a byte-identical stale blob, the referenced-doc eventual-consistency case the playbook warns about — no rollback taken on the early warms). Origin-forced fetch (`?variant=b&rt=917-...`): HTTP 200, 1 valid JSON-LD block, 0 "daily deal" strings, hero handle present, all 3 rail titles present incl. the new rail 0 heading and handle `winx-lily-dilly`, wayfinder tile present, couples heading present; 42 product + 35 collection links vs 3 `/discover` links (~96% product/collection-forward). GA4 `actionable:false` (114 sessions/28d, below the 1,000-session floor) — recorded, not optimized on. Suggestion #9891 filed (`kind:'process'`): `announcementBar` messages published to `singleton.homepage` did not render at origin after three warms, second occurrence (run 880 reported the same on 2026-09-15) — ticket lane, not this routine. **Named skips, not silent:** Step 7.5 `design-critic` spot-check (no subagent dispatch available in this invocation); the Emma voice gate was applied in-orchestrator against the printed marketing charter slice rather than by dispatching independent `emma-copywriter`/`emma-empathy-reviewer` subagents (same tool-availability cause) — flagged as a process gap, not a content violation. Docs-only diff, one `.md` file, based on latest `origin/main` (bf8adcd) at append time.

### 2026-09-20 — Routine A — Sunday: rail 0 product swap (packaging-shot primary promoted to a clean sibling first), hero copy refresh on a held pin, drop-page masthead micro-refresh

- **What:** Theme HELD ("Two at Once: Dual Stimulation Week", calendar row #30, unchanged since 2026-09-14; Sunday, so Step 1c correctly skipped — theme flip to row #31 "Materials 101" is tomorrow's Monday job). Live hero/rails/wayfinder/couples/announcement were already theme-aligned from run 917's work the day before, no drift found. **Rail 0** (`teamRail-2026-09-06-0`): swapped `winx-lily-dilly-squishy-silicone-vibrator` for `lovebirds-rechargeable-silicone-remote-controlled-dual-ended-vibrator-blue` (a dual-motor couples wearable, on-theme), `emmaAside` refreshed; heading/eyebrow held. Before pinning, downloaded and looked at the candidate's Shopify primary per mission brief §5: primary `93775A.jpg` was a retail packaging shot (Evolved-branded box render), so the clean sibling `93775B.jpg` was promoted with `setMediaAsPrimary` first. Hero **product pin held** at `together-double-tap-dual-ended-vibrating-strapless-strap-on` (already on-theme and resolves 200); **hero copy fully rewritten** (`singleton.emmaHero` eyebrow/headline/body/pullQuote). Because hero product and theme both held, the fresh-art floor did not trigger — 0 images generated this run (correct; the rail swap reuses the product's own real Shopify photo, which curated rails always render). `singleton.homepage` also refreshed: announcement message, wayfinder promo body, couples band body. Step 5b: today is day 3 of the 3-day category cycle (pleasure+play done 09-18, body+wear done 09-19); `categoryPage-discover` is still `status:draft` (never gone live) so no deep refresh was possible there, and the mandatory micro-refresh floor was applied to both drop pages instead — `dropPage-new` and `dropPage-on-sale` masthead `standfirst` fields patched and readback-verified. `singleton.panelDeck` correctly left untouched (2 days old, well inside the 7-day floor).
- **Why:** Mission brief §5's packaging-shot check is a hard reject even when every numeric score passes; the promote-then-pin sequence kept a genuinely on-theme, well-stocked (19 units), well-margined pick ($89.99 vs $49.99 wholesale, ~44%) usable instead of discarding it for a fixable photo problem. The rail swap satisfies Step 2c's sameness-diff requirement on its own (product selection, not copy alone); the hero, wayfinder, and couples-band copy refreshes are the second class of changed surface plus the mandatory-per-run touch.
- **Evidence:** run 968; gate `ok:true` throughout (dailyCents 60000, spent 0 pre-run, runs 0/10, images 0/100, `activeBriefId 11`). Candidate check via Shopify Admin GraphQL: `productType` on both the swapped-in SKU and its rejected packaging-shot-only sibling (`femmefunn-vortex-booster-...`, single image, no clean sibling available, passed over) confirmed not `Discontinued`; stock 19, wholesale cost known. Step 7 render-truth: **PASS** after two warms (`POST /cron/warm-homepage-b` ok/degraded:false/rails:4 each time, 5-minute wait each) — the first warm returned a stale blob (the referenced-doc eventual-consistency case the playbook warns about: old hero headline, old rail SKU `winx-lily-dilly` still present, no rollback taken on the early warm), the second warm resolved everything. Origin-forced fetch (`?variant=b&rt=968-recheck`): HTTP 200, 1 valid JSON-LD block, 0 "daily deal" strings, new hero headline present (old one gone), new rail SKU `lovebirds-...` present (old `winx-lily-dilly` gone), all 3 rail headings present, all 3 wayfinder tile labels present, new wayfinder promo body present, new couples band body present. **Named, not silent:** `announcementBar` new message text still absent at origin after two warms — the same pre-existing render/propagation gap already filed as suggestion #9891 (run 917, itself citing run 880 as the second occurrence); this is now a further occurrence, not re-filed here per the intake doctrine's row cap. Emma Presets audit (mission brief §6): 30 of 34 `emmaPreset` docs currently have `featured:true` against the stated 5-preset ceiling, oldest since 2026-07-02 — not this run's doing and not re-curated today (needs its own turn budget: theme match + 3+ product verification per preset via `dump-discovery-vocab.ts`), flagged for a dedicated pass. GA4/Nalpac/yesterday's-scoreboard (Step 2/2b) and the full Step 2 competitive/import-signal reads were skipped per the turn-budget ordering to reach Step 5 publish + Step 7 verify first. Step 7.5 `design-critic` spot-check was dispatched via the `Agent` tool (available this session) and returned **REVISE** (avg 3.56: hierarchy 3.5, spacing 3.5, type 4, color 3, imagery 2, motion 5, theme 4.5, distinctness 3) with one **BLOCK-grade** defect not rolled back per the uncalibrated-critic rule (a Sanity rollback would not fix it — it is Shopify card art, not a Sanity field): `master-series-kawaii-bondage-set`'s card art (`97870A.jpg`) is a near-black retail carton showing an age-ambiguous illustrated figure with outlined nipples through sheer fabric, breaching doctrine §4's unambiguous-adulthood rule and §4.3's nudity fence. **Fixed same-run**: `xdipx.card_art_blocked` set to `true` via Shopify Admin GraphQL (metafield `50276200186027`, no userErrors), after independently re-downloading and looking at the image to confirm the critic's claim before writing. Seven further findings (banned tableware prop + off-lock ground on the Nº05 tile, two co-occurring primary-coral CTAs in one viewport, the Lovebirds clean photo sitting offscreen at rail-1 position 5, ~3270px of unbroken white breaking the mandatory ground alternation, a 7-card grid that's 1-of-7 on-theme with a duplicated cleaner SKU, two multi-word plum emphases against a bare hero H1, and two off-token hero spacing values) were filed as suggestion **#10396** (`process`) rather than fixed in-run — none are BLOCK-grade and fixing them needs image regeneration/layout judgment outside a Sunday minimal-picks run. Emma voice gate applied in-orchestrator against the full charter (loaded in full this run, not just the marketing-addendum slice) rather than by dispatching independent `emma-copywriter`/`emma-empathy-reviewer` subagents, for turn-budget reasons — flagged as a process note, not a content violation; every new string avoids em-dashes, banned CTAs, conditional hedges, and the charter's named house tics, and uses only whitelisted CTA labels (unchanged this run). Docs-only diff, one `.md` file, based on latest `origin/main` (151ad986) at append time.

### 2026-09-21 — Routine A — Monday: theme flips to "Materials 101", hero rotates to the njoy Pure Wand, all 3 rails relineup, couples band block discovered and refreshed, 3 fresh images, calendar runway restocked

- **What:** Monday run (run 985). Competitor recon (search-sourced; live fetches historically 403 on this competitor class) found Lovehoney running a stacked Labor-Day-carryover discount wall (up to 70% off + an EXTRA20 stacking code + a spend-$89 gift threshold); logged as a decision event and used to justify staying on the already-planned educational theme rather than reacting with our own discount stack. Marketing calendar: closed a 6-week-stale `active` row (#6, "Weekend In, Not Out", 2026-08-10) and the prior week's never-activated row (#30, "Two at Once"), activated row #31 "Materials 101: What Body-Safe Actually Means" as this week's theme, and inserted 2 new `planned` rows (#40 "Backdoor 101" 2026-10-12, #41 "Beginner's On-Ramp Week" 2026-10-19) to restock the runway to the required 4 future weeks (previously only 2 were stocked). **Hero rotated** from `together-double-tap-...-strap-on` to **`njoy-pure-wand`** (Stainless Steel njoy Pure Wand, $135.99 list, wholesale $75, ~50% margin, inventory 1046, `productType` Prostate Massager, not Discontinued, clears the $39 lead-surface floor): handle verified 200 pre-write. `singleton.emmaHero` fully rewritten (eyebrow/headline/body/pullQuote, body revised post-voice-gate to restore the mandatory dial-9 arousal/release beat) and `singleton.emmaHeroStorefront.featuredProductHandle` + `primaryCtaLink` + `primaryCtaLabel` (`I'll take it ♥`) re-pinned. **All 3 wired rails relineup**: rail 0 (`teamRail-2026-09-06-0`) became the materials headliner (5 steel/glass SKUs plus a cleaner pairing item, all margin-verified via Shopify Admin GraphQL metafields), rail 1 (`teamRail-2026-09-06-1`) became a new Beginner-Friendly taxonomy rail (4 SKUs), rail 2 (`teamRail-2026-09-06-2`) refreshed the standing under-$30 entry rail to 6 new SKUs. No product handle repeats across the 3 rails. All `ctaLink`s set to null (unset) rather than a plausible-but-unverified collection, because a quick Storefront-API page-1 probe of `/collections/pleasure` and `/collections/under-30` did not surface any of the newly-picked handles on page 1 and there was no turn budget left to try alternate destinations or sort orders; shipped with no See-all on all 3 rather than repeat the run-442/run-952 mismatched-destination failure. **Wayfinder mosaic**: the block already exists at key `wayfinderMosaic-run8` (an earlier read truncated by a `tail` pipe mid-run hid it from view momentarily; corrected before writing anything). Tiles wf65a/wf65c relabeled and got new archetype-B/A art, wf65b label held; promo copy fully rewritten. **`playTogetherBanner` block found already live** at key `playTogether-starter` and refreshed: new heading/body/CTA, new archetype-C art. **Announcement bar** refreshed to a materials/trust-canon line. Emma Presets audited: only 3 of the 5-slot ceiling are currently published (`first-timer-shelf`, `bath-time-approved`, `throw-it-in-your-carry-on`); none conflict with the new theme and 2 read as materials/care-adjacent, so a full re-curation (product-count verification via `dump-discovery-vocab.ts`, a possible new materials-specific preset) was deferred to the next run rather than rushed. Panel deck last touched 2026-09-18 (3 days old), well inside the 7-day floor, correctly left untouched.
- **Why:** The theme needed to flip on schedule and the calendar runway was two weeks short of the mandated 4-week floor, both blocking issues for every downstream run this week and next. The hero needed a photogenic, deep-stock, on-theme (materials) anchor above the $39 floor; the njoy Pure Wand is the clearest "what body-safe actually means" story in the catalog (solid non-porous steel) with 1046 units of depth. All 3 rails needed to visibly change per the weekly-rhythm rule, and finding the previously-unknown `wayfinderMosaic-run8`/`playTogether-starter` blocks already live meant the couples-band mandatory-per-run surface could actually be serviced instead of skipped.
- **Evidence:** run 985; gate `ok:true` throughout (dailyCents 60000, spent 0 pre-run, runs 0/10, images 0/100, `activeBriefId 11`). Margin check via Shopify Admin GraphQL confirmed `wholesale_cost` known and ACTIVE status on all 6 materials-rail-adjacent SKUs before picking. 3 images generated (2 via Atlas seedream-4.5-edit, 1 fell back to fal/flux-kontext-dev after an Atlas timeout), all `placed:true`, fresh-art floor met on a changed-hero, changed-theme day. `rails-fingerprint.ts --assert-changed rails,hero,couples` returned exit 0, confirming all three must-change slots moved versus the pre-run baseline. Step 7 render-truth: one `POST /cron/warm-homepage-b` (`ok:true degraded:false rails:4`), 5-minute wait, then a direct fetch of `https://xdipx.com/` returned HTTP 200 with the new hero handle, all 3 new rail headings, the new wayfinder tile headline, the new couples-band heading, one valid JSON-LD block, and zero "daily deal" strings — **PASS**, no fallback content detected. A crude whole-page href count showed product/collection links at 48% (71/148) against the 70% target and 3 `/discover` hrefs against the ≤2 rule; both are flagged, not fixed, because the count includes nav/footer rather than the module-scoped count the mission brief defines, and a proper module-scoped recount needed more turns than remained — filed here as a named gap for the next run to redo properly, not re-scored as a false pass. GA4/Nalpac/yesterday's-scoreboard (Step 2/2b), the Step 2e homepage-SERP-snippet read, the Step 5b category-page deep-refresh pair (day 1: pleasure+play were due), `design-critic`'s Step 7.5 spot-check, and a full Emma-Presets re-curation were all skipped per the turn-budget ordering to reach Step 5 publish + Step 7 verify first on a cold-start run (fresh clone, `npm install` from zero, full charter/brief/doctrine reads) plus Monday's mandatory recon and calendar work; named here rather than silently dropped. Emma voice gate: dispatched independently via the `emma-empathy-reviewer` Agent (available this session) against the full charter; returned 0 BLOCK / 5 REVISE (all non-blocking polish: hero body under-shot the mandatory dial-9 arousal/release beat and used mechanism-explanation language, one rail aside had an ambiguous "drawer" possessive, one wayfinder tile had a conditional "if" hedge, one couples-band line had causal mechanism-explanation, one announcement line said "steel" instead of trust-canon's "stainless steel") / 20 PASS; all 5 REVISE rewrites applied before publish. Docs-only diff, one `.md` file, based on latest `origin/main` (323d52f) at append time.

### 2026-09-22 — Routine A — Tuesday: Materials 101 day 2, Bondage/Restraints/Collars taxonomy rail answers a twice-flagged strategy-brief escalation, hero/wayfinder/couples/announcement copy refresh, pleasure+play masthead micro-touch closes run-985's deferral

- **What:** Run 1006. Not a Monday, so no recon/theme-set step; Materials 101 (marketing_calendar row #31) stayed the active theme, day 2. Hero held on `njoy-pure-wand` (rotated twice-per-week minimum already met by run 985's rotation) and got a fresh copy pass (`singleton.emmaHero` eyebrow/headline/body/pullQuote); pin/CTA (`featuredProductHandle`, `primaryCtaLink`, `primaryCtaLabel`) verified unchanged and still resolving 200. **Rail `teamRail-2026-09-06-1` rebuilt** from the prior Beginner-Friendly lineup into a **Bondage/Restraints/Collars taxonomy rail**, answering the weekly strategy brief's twice-escalated finding (#8021/#9326: "30 carried SKUs, 0 zero-stock [per the brief; a live stock pull this run found 4 of 30 at zero], one of only two categories with a proven real-customer purchase, and has no featured treatment and no calendar slot anywhere through 10-05... give it a rail or record a reasoned pass"). 6 SKUs picked from the live `all-bondage` Shopify collection (`edge-collar-leash` $73.99, `velvet-noir-collar-removable-clamps` $30.99, `edge-tuggable-clover-clamps` $36.99, `entwined-passion-spreader-bar` $95.99, `sei-mio-delectable-domination-supreme-hog-tied-submissive-play-set` $89.99, `stoner-vibes-chronic-collection-glow-in-the-dark-blindfold` $11.99 as the accessory pairing item), all confirmed ACTIVE / not Discontinued / wholesale_cost known (margin 39-45%) via Shopify Admin GraphQL before picking; no handle repeats the other two wired rails. `ctaLink` set to `/collections/all-bondage` after a Storefront-API page-1 probe confirmed 4 of the 6 handles resolve there (the destination-continuity check run 985 couldn't clear for any of its 3 rails). Materials headliner rail (`teamRail-2026-09-06-0`) and the under-$30 entry rail (`teamRail-2026-09-06-2`) held unchanged. **Wayfinder mosaic**: all 3 tile labels refreshed (art and links held, no hero/theme change to trigger the fresh-art floor); Discover You promo heading/body refreshed. **Couples band** (`playTogether-starter`) heading/body refreshed, art and CTA held. **Announcement bar** refreshed to a day-2 materials/trust-canon pairing. **Step 5b**: health sweep clean (7/7 checked surfaces 200, no problems); panel deck last touched 2026-09-18 (4 days), inside the 7-day floor, correctly left untouched; run-985's deferred day-1 (pleasure+play) deep-refresh pair was NOT done as a full deep-refresh (turn budget) but got the masthead-standfirst-only micro-refresh carve-out instead, both pages re-themed to the materials angle rather than left stale a second day. Emma Presets: still 3 of 5 published, none conflicting with the theme, re-curation deferred a second consecutive run and flagged explicitly rather than silently repeated a third time.
- **Why:** The strategy brief named the Bondage/Restraints/Collars gap twice with no action between; a taxonomy rail here both closes that finding and satisfies the mission brief's "one taxonomy rail per week" rule. Holding the hero product (already rotated twice this week) let the run spend its picking budget on the flagged category instead of a second unnecessary rotation. The rail swap plus the full copy refresh across hero/wayfinder/promo/couples/announcement satisfy Step 2c's sameness diff on both counts (product selection AND copy). The masthead micro-touch on pleasure+play exists specifically so a deferred pair doesn't compound into a second stale day, per the routine's named micro-refresh floor.
- **Evidence:** run 1006; gate `ok:true` throughout (dailyCents 60000, spent 0 pre-run, runs 0/10, images 0/100, `activeBriefId 11`). Copy batch voice-gated via an independently dispatched `emma-empathy-reviewer` Agent against the full charter: 2 REVISE (hero body's closer read as care-instruction rather than a temptation closer; Discover-You promo body used the banned "land" verb), both rewritten and re-verified before publish; 0 BLOCK. `rails-fingerprint.ts --baseline <captured pre-run> --assert-changed rails,hero,couples` returned **exit 0** (all three must-change slots moved). Step 7 render-truth: one `POST /cron/warm-homepage-b` (`ok:true degraded:false rails:4`), 5-minute wait, then `/cron/homepage-healthcheck`'s own render-truth block returned `ok:true`, zero missing assertions, zero fallbacks, with the new rail heading ("Bound, buckled, and built to last"), all 3 new wayfinder tile labels, and the new couples heading all present in the live slate. A direct fetch of `https://xdipx.com/?variant=b` confirmed HTTP 200, the hero handle present, 0 "daily deal" strings, and 4 valid JSON-LD blocks. A crude whole-page unique-href count read 65 product/collection links of 144 total (45%) against the 70% target and exactly 1 unique `/discover` href against the ≤2 rule; the ratio is flagged, not fixed, same caveat as run 985 (includes nav/footer, not the module-scoped count the mission brief defines) — still an open gap for a future run to recount properly. GA4 scoreboard (`/api/team/ga4-summary`) returned empty, consistent with the active strategy brief's note that GA4 has been unreadable for 9 consecutive briefs; the store sits two orders of magnitude below the 300-sessions/week threshold regardless, so no metric-driven swap was in play. Step 5's resolved-handle check (`getProductsByHandles`) confirmed all 6 new rail handles resolve (6 of 6), clearing the 4-product depth floor. `design-critic`'s Step 7.5 spot-check and the Step 2 GA4/Nalpac/import depth reads were skipped per turn-budget ordering to reach Step 5 publish + Step 7 verify first, named here rather than silently dropped. Docs-only diff, one `.md` file, based on latest `origin/main` at append time.

<!-- Newest entries appended above this line by each routine's changelog step. -->
