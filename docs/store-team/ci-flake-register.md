# CI flake / endemic-failure register

One row per check that fails for reasons unrelated to the PR that trips it —
endemic (fails on main / across unrelated branches) or genuinely flaky
(non-deterministic, passes on a bare re-run). Diagnosed once here, then
cited rather than re-investigated on every PR that hits it. Maintained by
R-SHEP (`.claude/agents/pr-shepherd.md`); see its `<flake_register>` section
for the process.

| Check | First seen | Symptom | Evidence | Owner ticket |
|---|---|---|---|---|
| `lighthouse` (PR-lookup permission) | 2026-09-23/24 (PR #1308, #1309, #1310) | The `wait-for-vercel-preview` step's PR-lookup call (`GET /repos/{owner}/{repo}/pulls/{pull_number}`) fails immediately with `Resource not accessible by integration`, so no preview URL is ever resolved and the job exits 1 before Lighthouse runs at all. Distinct from the "no deployment" row below: there the preview genuinely never built; here the preview builds fine (confirmed `Ready` in the Vercel bot comment on all three PRs) but the workflow lacks the scope to look it up. | `.github/workflows/lighthouse.yml`'s `permissions:` block grants only `contents: read` and `statuses: read`; `ci.yml` and `agent-allowlist.yml` both already carry `pull-requests: read` for the identical call shape. Byte-identical error reproduced on three unrelated branches/diffs (#1308 `claude/video-x-upload-valve-gate`, #1309 `claude/video-calendar-learn`, #1310 `claude/voice-charter-v5-6`) — none touch `.github/**`, a route module, or the client bundle. `check` and `migration-dry-run` green on all three SHAs. | ticket #11184 (filed 2026-09-24, R-SHEP) — **RESOLVED**: fix PR #1312 (`fix/lighthouse-workflow-pr-permission`, `pull-requests: read` grant) merged 2026-09-24. See the `deployments:read` row directly below: fixing this scope let execution reach the *next* missing scope on the same action, which is a separate, still-open gap. |
| `lighthouse` (deployments permission) | 2026-09-24 (PR #1316, #1322, #1323) | Once the PR-lookup scope above is granted, the `wait-for-vercel-preview` step's next call, `GET /repos/{owner}/{repo}/deployments?sha=...`, itself 403s on all 150 retries with `Resource not accessible by integration` (`x-accepted-github-permissions: deployments=read`), then exits with `no vercel deployment found, exiting...` — a misleading message, since the deployment may exist; the token was simply never allowed to list it. Distinct from the genuine "no deployment" row below: there the Vercel deployment list is confirmed empty; here every attempt is a blanket 403 before any real answer is possible. | `lighthouse.yml`'s `permissions:` block (post-#1312) still lacked `deployments: read`. Byte-identical 403 + retry-exhaustion reproduced on three unrelated PRs same day (#1316 `ticket/drop-runpod-idle-video-job-cols`, #1322 `ticket/11150`, #1323 `ticket/11151`) — none touch `.github/**`, `vercel.json`, or lighthouse config. `check` and `migration-dry-run` green on all three SHAs. | ticket #11263 (filed 2026-09-24, R-SHEP), fix opened as PR #1326 (`fix/lighthouse-deployments-permission`, one-line `deployments: read` grant) — held protected-path (`.github/**`), owner-merge only. **RESOLVED**: #1326 merged 2026-09-25T03:37:50Z (`merged_by: mbayard-sorp`). Confirmed on two independent PRs afterward: #1348 (`ticket/11463`) ran `lighthouse` green start to finish on the post-fix workflow, and #1322 itself (`ticket/11150`), whose last run before the fix was the 403/retry-exhaustion signature above, went green (`success`) after R-SHEP merged `main` in and pushed at 2026-09-25T10:34 — no re-diagnosis needed, this was the already-registered class clearing as designed. Note added to #11263. |
| `graph` (deleted infra/video-worker) | 2026-09-24 (PR #1316) | `.github/workflows/video-graph.yml` still validates `infra/video-worker/Dockerfile`'s ComfyUI graphs, but PR #1315 (merged 2026-09-24, RunPod/Wan 2.2 pipeline retirement) deleted `infra/video-worker/` in its entirety. The workflow's `COMFY_SHA` extraction step fails immediately (`grep: infra/video-worker/Dockerfile: No such file or directory`). Will red every future PR whose branch carries #1315's history until the workflow is removed. | Confirmed directly against `origin/main` post-#1315: `infra/video-worker/Dockerfile` is gone, `.github/workflows/video-graph.yml` is still present and unchanged. `graph` is not in `release-engine.server.ts`'s `REQUIRED_CHECK` set, so it does not block merges, but it is genuinely red, not flaky. | ticket #11264 (filed 2026-09-24, R-SHEP), fix already opened as draft PR #1317 (`fix/retire-video-graph-workflow`, pure deletion of the now-purposeless workflow file) — held protected-path (`.github/**`), owner-merge only. |
| `lighthouse` (no deployment) | 2026-09-18 (PR #1223) | The `wait-for-vercel-preview` step retries 150x (~5min) looking for a Vercel deployment matching the PR head SHA, then exits 1 with `no vercel deployment found, exiting...`. Distinct from the LCP-budget-overage symptom described in `.claude/agents/pr-shepherd.md`'s `<repo_specifics>` — here Lighthouse never runs at all because no preview ever existed to measure. | PR #1223 (branch `ticket/10116`, commit `1a669667de`): GitHub's commit-status API for that SHA returns `total_count: 0` (Vercel's integration never posted any status), and the Vercel project's own deployment list (`prj_uQmHNx2sipH3pziPYukKRXc7oNhe`) shows zero entries for `ticket/10116` across a 3.5+ hour window spanning the PR's creation, while sibling branches (e.g. `ticket/9891`) deployed normally in the same window. The PR's diff (`app/lib/antithesis-cap.ts` + test, `scripts/check-antithesis-cap.ts`, a 2-line additive change to `app/lib/aphorism-closer.ts`) touches no route module, component, stylesheet, or `vercel.json` — nothing that could affect whether Vercel builds a preview. `check` and `migration-dry-run` were green on the same SHA; local `npm run typecheck` / `npm test` (332 files, 4515 tests) / `npm run build` all green on the branch. Could not confirm via rerun: `actions_run_trigger` `rerun_failed_jobs` and `rerun_workflow_run` both returned 403 (`Resource not accessible by integration`) for R-SHEP's token — the same permission gap already on record against ticket #7244's history. | ticket #10162 (filed 2026-09-18, R-SHEP) — fix is for someone with Vercel project access to check why the GitHub webhook/deployment was never created for this push (Vercel integration logs, GitHub App delivery log), and either the mechanism gets fixed or, if this proves to be a rare one-off, `wait-for-vercel-preview` gains a longer bounded retry window so a slow-to-queue build isn't indistinguishable from a dropped webhook. |
| `check` (vitest) | 2026-09-01 (PR #1012, PR #1011, PR #1010, same day) | `app/lib/storefront-home.server.test.ts > assembleStorefrontHome — pinned-headliner out-of-stock gate > "does not pin a tracked zero-stock product found only via the raw index lookup"` fails non-deterministically: `AssertionError: expected 'y' to be 'x'` at line ~448 (`expect(data.featured[0]!.handle).toBe('x')`), roughly half of all runs. Root cause: the test never mocks `Date.now()` or seeds the rail shuffle, so `hydrateStorefrontPayloadB`'s `reshuffleRailsWithSeed(payload.rails, railSeedBucket())` (`app/lib/storefront-home.server.ts`) shuffles the two-item mocked rail `[x, y]` using the real wall-clock 15-minute bucket (`RAIL_SEED_BUCKET_MS = 900_000`) fed through the `mulberry32` PRNG in `app/lib/homepage-payload.server.ts`. Which item lands in `featured[0]` is therefore a coin flip on the current bucket, unrelated to any PR's diff — including main's own. | Reproduced on three unrelated open PRs (#1012, #1011, #1010; base sha `ba20d8d`) whose diffs touch `app/lib/social-admin-rework.server.ts`, `app/lib/team-gates.server.ts`/`emma-voice.server.ts`/social routes, and `app/lib/video-episodes.server.ts` respectively — none touch `storefront-home.server.ts`, its test, or `homepage-payload.server.ts`. A standalone Node simulation of `railSeedBucket()` + `mulberry32` across 2000 consecutive 15-minute buckets landed `x` first ~50.3% of the time and `y` first ~49.7% — a true coin flip, not a rare edge case. The test passes when run against current main HEAD (`24184e1`) at some wall-clock moments and would fail at others, by design of the mechanism above; it is not a regression introduced by any commit. Introduced by ticket #6751 (PR #1002, commit `a6c4d72`, "gate curated homepage surfaces on out-of-stock"). | ticket #7016 (filed 2026-09-02, R-SHEP; supersedes #7001, which was mistakenly filed with `links` pointing at the three unrelated PRs above and auto-transitioned to `pr_open` as a result — those PRs do not implement any fix) — fix is to make the test deterministic (mock `Date.now()`/`vi.useFakeTimers()`, or add an explicit seed/`now` override the test can pass through `assembleStorefrontHome` → `hydrateStorefrontPayloadB` → `railSeedBucket`), not to touch the assertion itself. |
| `allowlist` | 2026-09-03 (PR #1053) | Fails an `agents/**` PR that legitimately touches `docs/emma-voice.md`/`docs/ads-policy.md` with the two-file voice-charter bullet, citing a missing `voice-charter` label, even though the PR carries that label. Not a content problem: `.github/workflows/agent-allowlist.yml`'s `on: pull_request` uses GitHub's default event types (opened/synchronize/reopened), and agent-editor attaches the `voice-charter` label in a separate follow-up API call after opening the PR. That label-add fires a `labeled` event, which the workflow does not listen for, so a job that already ran (and read the pre-label state) never gets a fresh evaluation once the label lands. | PR #1053 (`agents/suggestion-5862`): job 100532946465 (run 33718614373, started 2026-09-03T05:23:02Z, ~6s after PR creation) read the PR's labels via `gh api repos/.../pulls/1053 --jq .labels` and found none, failing `docs/emma-voice.md needs the 'voice-charter' PR label`. The PR carries the `voice-charter` label now (confirmed via API after the run). R-SHEP could not force a re-evaluation: `rerun_workflow_run` and `rerun_failed_jobs` both returned 403 (`Resource not accessible by integration`) for R-SHEP's token, and an empty commit or close/reopen to resync is against charter. | ticket #7244 (filed 2026-09-03, R-SHEP) — fix is either adding `labeled` to the workflow's `on.pull_request` `types:` (protected-path, needs an engineer PR + owner merge) or having agent-editor attach the label atomically at PR-open time (an agent-editor.md change, not R-SHEP's to author either way). |
| `check` / `migration-dry-run` / `lighthouse` (runner never assigned) | 2026-09-24 (PR #1331, single instance, deliberately not registered yet per that PR's own comment: "may be worth one if it recurs"); recurred PR #1332 (16:30), PR #1333 (20:16), PR #1334 (20:24), this PR #1335 itself (20:47), PR #1336 (22:14), PR #1337 (22:17) — 7/7 pull_request-triggered `ci.yml` runs since 15:43 UTC hit it, none spared. | Every `ubuntu-latest` job in `ci.yml` and `lighthouse.yml` completes in 5-6 seconds with `runner_id: 0` and empty `runner_name` — GitHub never dispatches a runner, so no step (not even `actions/checkout`) ever executes. Distinct from every other row here: those fail mid-run with a real signal; this fails before the run starts. | GitHub Actions job API (`get_workflow_job`) on all 7 instances (jobs 107709695998/107709696528 PR #1331, 107729007382/107729007091 PR #1332, 107815317870/107815317544 PR #1333, 107818373336/107818373621 PR #1334, plus PR #1334's `lighthouse` job 107818372105, 107827497871/107827497562 this PR #1335, 107856854328/107856853955 PR #1336, 107857561236/107857560720 PR #1337) all show `runner_id: 0`. `get_job_logs` 404s on every one (no logs were ever generated, consistent with no dispatch). The prior 25+ `ci.yml` runs before 15:43 UTC (run 1853 down through 1828, 2026-09-24 02:11-15:38) were all normal 196-546s green runs; every `pull_request`-triggered run since 15:43 UTC (run 1854-1858, 1862, 1863 — 7/7) hit this signature — a sharp regime change, not scattered noise. PR #1331 is docs-only (one Markdown file, +63 lines) and still failed identically, which structurally rules out any diff cause. PR #1334's `check` steps (`npm run typecheck`, `npm test`, `npm run build`) reproduce 100% green in a local worktree on the exact failing commit (357 files, 5143 tests, clean build) — the CI failure does not reproduce locally, per the charter's own flake/environment-difference rule. `rerun_failed_jobs`/`rerun_workflow_run` both return `403 Resource not accessible by integration` for this session's token (same gap on record against ticket #7244/#10162), so the standard one-rerun flake confirmation was unavailable; recurrence across 7 unrelated diffs over ~6.5 hours is used as the substitute evidence instead. **RESOLVED, confirmed on `pull_request` events (2026-09-25, ~01:38-01:40 UTC).** `push`-triggered `ci.yml` runs on `main` hit the identical 5-6s/no-runner signature at 20:52-20:59 UTC (runs 1859-1861), then ran normally (196s+, real runner) from run 1864 (00:40 UTC) onward. R-SHEP then pushed a merge-with-main commit to this PR (#1335) itself at 01:38 UTC to test the `pull_request` path specifically: `migration-dry-run` completed in a real 23s (not 5-6s) and `check` ran to full completion — first confirmed `pull_request`-triggered success since the outage began. Six more merge-with-main pushes followed (PR #1331, #1332, #1333, #1334, #1336, #1337, plus the conflict-driven merges on #1322 and this PR), all landing real in-progress/queued/completed runs with actual durations instead of instant `runner_id: 0` failures — #1336 finished fully green (`check`, `migration-dry-run`, `allowlist` all `success`). Root cause (why it broke 15:43 UTC 2026-09-24, why it cleared ~00:40-01:38 UTC 2026-09-25) is still unknown — no agent token can read GitHub Actions runner capacity/billing history — so this is confirmed **cleared**, not diagnosed; re-open this row if the signature recurs. | ticket #11380 (filed 2026-09-24, R-SHEP; noted resolved 2026-09-25), owner blocker #231 (`ci-runner-not-provisioned`, category `decision`; verify with a fresh `pull_request` push before closing — this register update is that verification) — root cause still needs GitHub org admin access to check Actions runner capacity/concurrency and billing history retroactively, if the owner wants to know why it happened rather than just that it's cleared. Compounds with, but is independent of, the release-engine PAT `checks:read` outage tracked in the row directly below — that one also cleared: main landed 5 release-engine merges (#1321, #1324, #1323, #1319, #1320) between 00:40 and 01:34 UTC 2026-09-25, so nothing blocks the ordinary merge lane store-wide as of this writing. |
| `release-engine` (cron, not a PR check) | 2026-09-24 (first confirmed in production logs ~00:00 UTC; still failing as of 11:00 UTC, 11+ hours, ~60+ consecutive runs across 4 deployments) | `GET /cron/release-engine` returns 503 on every 10-minute invocation with `[release-engine] CONFIG ERROR, refusing to run`. Not a PR-specific failure: this stalls the entire ordinary-lane auto-merge pipeline store-wide. Confirmed still green/eligible PRs sitting unmerged with zero release-engine action for 4+ hours (#1318-#1321, all `check`+`migration-dry-run` green, no protected path). | Vercel production runtime logs (`prj_uQmHNx2sipH3pziPYukKRXc7oNhe` / `team_dmGePk3AHHBxCirD2PRm2AOA`, query `CONFIG ERROR`; narrow the time window, e.g. `since=15m` — wide queries time out) show the identical error on every run: `the token cannot read check runs: GitHub GET /repos/mbayard-sorp/xdipx_store/commits/{sha}/check-runs?per_page=100 -> 403: {"message":"Resource not accessible by personal access token", ...}`. Reproduced across 4 different deployment IDs over a 10-hour window, so this is not a stale-deploy artifact — the credential itself (a GitHub personal access token, not the GitHub App token other workflows use) lost `checks:read` scope, or was rotated without it. | No ticket filed (R-SHEP has no team-token in this session to POST /api/team/suggestion or /api/team/blocker) — pushed a mobile notification to the owner directly instead, since this needs a token/PAT permission fix on GitHub's side that only the owner can make. Whoever has GitHub PAT admin access should regenerate/re-scope the release engine's token with `checks:read` (repo permission "Checks" → read), then confirm the next cron cycle clears this row. — **RESOLVED**: confirmed via Vercel production logs, `GET /cron/release-engine` is back to normal `200` responses with real per-PR triage (`skip [ticket-not-verified]`, `escalate-protected`, `undraft [draft-auto-ready]`, post-merge smoke checks) as of 2026-09-25 ~03:10-03:20 UTC — no more `CONFIG ERROR`/503. Outage ran roughly 2026-09-24 00:00 UTC through 2026-09-25 ~03:00 UTC (~27 hours). During the outage the owner hand-merged the backlog directly (`merged_by: mbayard-sorp` on #1316-#1319, #1327, #1328, etc.); it is not confirmed from the outside whether the PAT was re-scoped or the credential rotated back, only that check-runs reads now succeed. **Second occurrence of this exact failure mode**: `release-engine.server.ts`'s own `alertSelfCheckFailure` comment notes "the GitHub token expiring in July took roughly two days to notice because nothing announced it" — that earlier incident is why the self-check + once-daily owner email exists at all, and this September recurrence is the same underlying fragility (a personal access token that can silently lose scope) striking twice despite the alert. The alert did its job (27h this time vs. ~48h in July), but it cannot fix the root fragility — see the standing recommendation filed this session to move the release engine off a PAT onto the same GitHub App token the other workflows already use. |
| `migration-dry-run` | 2026-08-20 (PR #791); recurred 2026-08-22 (PR #838, PR #850); recurred again 2026-08-23 (PR #864, PR #856) | Fails on every PR touching `db/migrations/**`, not just PRs that introduce a bug. `db/migrations/0002_fluffy_james_howlett.sql` ('auto'-classified) creates an index on `order_line_items`, a table only `db/migrations/009_copurchase.sql` ('manual'-classified) creates. `runDryRunMode` (scripts/apply-additive-migrations.ts) correctly skips 'manual' files (mirrors production safety — an owner must run those by hand), but a scratch CI database starts empty, so skipping 009 strands `order_line_items` and 0002 fails with `relation "order_line_items" does not exist` before the PR's own migration file is ever reached. This is a structural gap in the check's design, not a defect in any specific PR's SQL: it reproduces identically on `main` (no migration PR needed — a scratch DB replay of the current `db/migrations/` directory fails the same way) and will keep failing on every future migration PR until resolved. | `0002_fluffy_james_howlett.sql`, `009_copurchase.sql`, `0001_social_posts.sql`, and `005_deal_flow_reengineer.sql` are all already on `origin/main` (confirmed via `git show origin/main:db/migrations/<file>`), i.e. none of this is introduced by PR #791 — the inconsistency predates the check. Reproduced locally against a real `postgres:16` server (not the CI container) with `DATABASE_URL=postgresql://ci:ci@localhost/ci npx tsx scripts/apply-additive-migrations.ts --dry-run` on this branch: fails at `0002` with `relation "order_line_items" does not exist` every time, before this PR's own diff (`.github/workflows/ci.yml` only) is ever reached. Also found in the same repro: `0001_social_posts.sql` and `005_deal_flow_reengineer.sql` both add `deal_history.vault_price` unguarded (no `IF NOT EXISTS`), so a full-history replay hits a second, different failure mode (`column already exists`) immediately after the first is worked around — two independent reasons a naive full-replay design can't reach a clean pass. See PR #791 status comment for the design options considered (seed baseline schema via `drizzle-kit push` against a pre-PR `db/schema.ts` instead of raw-replaying `db/migrations/**`, or reconcile/dedupe the historical migration files themselves). PR #838 (2026-08-22) hit the identical signature; confirmed not caused by that branch (its own migration, `082_capi_outbox_rename.sql`, is manual-classified and never reached — the job dies at `0002` first). PR #850 (2026-08-22, same day) hit it a third time; confirmed not caused by that branch either (its own migration, `083` for the multi-scene video job columns, is fully additive and never reached — same `0002` failure point). PR #864 (2026-08-23) hit it a fourth time (migration 084, Social Studio v2; classifies `auto`, never reached, same `0002` failure point). PR #856 (2026-08-23, same day) hit it a fifth time; confirmed not caused by that branch either (its own migration, also locally numbered `084_social_posts_alt_text_brief.sql` adding `alt_text`/`image_brief`/`subject` columns to `social_posts` — a numbering collision with PR #864's `084` once one of the two merges — is fully additive `ADD COLUMN IF NOT EXISTS` and is never reached, same `0002` failure point, job log shows the identical `relation "order_line_items" does not exist` error). | ticket #4399 shows `applied`/PR #791, but that reflects PR #791 landing the check itself, not a fix — #4399's own note (id 4059) says both fix options were blocked as protected-path/DB-class work no agent can author. Recurrence on PR #838 logged as ticket #4866, noted on #4399. Recurrence on PR #850 noted on #4399 directly (R-SHEP, 2026-08-22). Recurrence on PR #864 and PR #856 both noted on #4399 directly (R-SHEP, 2026-08-23). Escalated as owner blocker `migration-dry-run-baseline-gap-recurred` (needs an owner-attended session; still unresolved as of 2026-08-23, now five PRs deep). |
